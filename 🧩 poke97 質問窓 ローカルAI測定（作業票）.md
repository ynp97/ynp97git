---
種別: 作業票 / AI共通
作成日: 2026-08-08
役割: poke97 カード研究室「AIへの質問窓」を、ローカルAI（LM Studio）で回せるかを測る。
渡す先: Claude Code または Codex（**本機でシェルを直接叩ける環境**）。Cowork Claudeは不向き ── Macのlocalhost:1234へ届かず、実行結果をスクリーンショットで読むことになるため。
---

# 🧩 poke97 質問窓 ローカルAI測定（作業票）

## これは何を決めるための作業か

カード研究室の質問窓は三段構成で、**3段目（候補カードを読んで振り分ける）がいちばん重い**。ここをローカルAIに任せられれば、費用ゼロ・無制限で回せる。任せられないなら、Codexへ渡すか、条件を人が書いて候補一覧だけ出す形にする。

**決めるのは1つだけ ── 3段目をローカルAIに任せられるか。** 「ローカル化そのもの」が目的ではない。

## 前提（読む前に確認する）

- 実体: `アプリ本体/Poke97-Card-Lab/`。仕様の正本は [[アプリ引き継ぎ/W poke97 カード研究室]]
- 質問窓の実装: `ai_ask.py`。設定は `data/ai_config.json`（既定 `http://127.0.0.1:1234/v1`）
- **カード本文のエネルギーは `【水】` の形。**「水エネルギー」では当たらない（2026-08-08に全件取り直し済み）
- LM Studioのモデル一覧は [[🧭 実行環境の能力表（AI共通）]] §5

## 手順

### 1. LM Studioのサーバーを上げる

```sh
lms server status || lms server start
curl -s http://127.0.0.1:1234/v1/models
```

`lms` が入っていない場合は、**LM StudioのGUIでLocal Serverを起動する**（Developerタブ）。ここはシェルからは触れない。GUI操作が必要なら Cowork Claude（computer-use）か本人が行う。

### 2. 正解キー（2026-08-08 にローカルDBで確定済み）

問い:「**水エネルギーを特性で持ってこれるポケモン**」

**A群 ── 特性の文に【水】が出る 5種**

| カード | 特性 | 「持ってくる」に当たるか |
|---|---|---|
| ブロスター | バックリロード | **○** 手札から基本【水】エネルギーを2枚までつける |
| ジュゴン | おしながす | **△** ベンチの【水】エネルギーをバトル場につけ替える（場内移動） |
| ハンテール | ダイバーキャッチ | **△** きぜつしたポケモンの基本【水】エネルギーを残す |
| メガゲッコウガex | ひっさつしゅりけん | **×** 手札の基本【水】エネルギーをトラッシュする（払う側） |
| アマルルガ | ツンドラウォール | **×** 【水】エネルギーがついた味方のダメージ-50 |

**B群 ── タイプ無指定で「基本エネルギー」を持ってくる 7種**（水も持ってこられるので、答えに含めないと漏れになる）

メガレックウザex／ジャラランガ／ハピナスex／ブーピッグ／レアコイル／ロケット団のワナイダー／バシャーモex

**採点の軸は3つ。**

1. A群の○×△を正しく分けられるか（**「つける」「つけ替える」「トラッシュする」の読み分け**がここの核心）
2. B群の存在に気づけるか。気づけなくても `missing` に「タイプ無指定の特性も見るべき」と書ければ及第
3. **候補に無いカードを挙げないか**（挙げたら `unverified` 印がつく。印が出た時点で減点）

### 3. 測る

3モデルで同じことをやる。`data/ai_config.json` の `model` を書き換えて実行する。

| モデル | サイズ | 見どころ |
|---|---|---|
| `qwen3.6-35b-a3b-mlx` | 20.4GB | 現在の本線。**a3b＝実働3B相当**なので、細かい読み分けが不安 |
| `qwen/qwen3-30b-a3b-2507` | 17.2GB | 速度優先の予備 |
| `google/gemma-4-12b-qat` | 7.2GB | 最軽量。JSONを安定して吐けるかが未知 |

```sh
cd '/Users/yoshiakinagumo/Documents/Obsidian Vault/アプリ本体/Poke97-Card-Lab'
time python3 ai_ask.py '水エネルギーを特性で持ってこれるポケモン'
```

**記録するのは4つ。** ①所要時間 ②1段目が出した検索条件（`【水】` を使えたか） ③A群の振り分け ④作り話の有無。

### 4. 1段目が弱いときは、切り離して測り直す

**本人は検索条件を自分で書ける。** 1段目（質問→条件）が下手なら、そこはローカルAIに任せず、3段目だけを測る。

```sh
python3 ai_ask.py --packet-only '水エネルギーを特性で持ってこれるポケモン'
```

> [!warning] `--packet-only` には既知の穴がある
> CLIの `--packet-only` は、**質問文をそのまま全文AND検索に投げる**。助詞まで一致を求めるので0件になる。画面側は検索欄の条件を使うよう直したが、**CLIは直っていない**。条件を引数で受け取る形に直してから使うこと（`search_cards.py` と同じ引数を足せばよい）。

## 判断のしかた

- **A群を正しく分けられ、作り話が出ない** → 3段目をローカルに移す。既定モデルを `ai_config.json` に書き、質問窓の説明を更新する
- **分けられるが1段目が下手** → 3段目だけローカル。画面の「AIに聞く」を、検索欄の条件を使う経路に変える
- **A群を分けられない／作り話が出る** → ローカルは見送り。**Codexへ渡す**（`search_cards.py` を自分で叩けるので、質問票のコピーも要らない）。判断と根拠をこの作業票に書き残して閉じる

**「惜しい」で採用しない。** 間違った候補を自信ありげに出すAIは、この用途では使わないほうがまし。今日の事故（0件でも検索が正常に見えた）と同じで、**間違いに気づけないことが一番高くつく。**

## 終わったら更新する場所

- 結果と採用可否 → [[アプリ引き継ぎ/W poke97 カード研究室]]「AIへの質問窓」節
- モデルの実性能（速度・向き不向き） → [[🧭 実行環境の能力表（AI共通）]] §5
- 3モデルの比較は、日記アプリのレシート読み取り（[[アプリ引き継ぎ/O 自分用日記アプリ]]）でも要る話なので、**測ったら数字をそちらにも残す**
