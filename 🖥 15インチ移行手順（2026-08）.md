---
種別: 作業手順 / 一時（渡航後に要否を判断）
作成日: 2026-08-15
役割: 15インチ MacBook Air（M2・A2941・8GB・SSD 256GB）をクリーンインストールし、渡航用に持ち歩く主機として立て直す手順。M5のディスプレイ破損（2026-08-15）を受けた機材構成の見直し。M5は家の重作業機として残し、両方使う（入れ替えではない）。
期限: 2026-08-18 までに完了（8/23 渡航。8/19-20 は三本立ての締切に充てる）
---

# 🖥 15インチ移行手順（2026-08）

## 背景

- **2026-08-15、M5（MacBook Pro）のディスプレイが破損。** 付属のポリッシングクロスでナノテクスチャー画面を拭いていて割れた。落下・衝撃なし。**表示は出ており使用は可能。**
- AppleCare+ 未加入。正規のディスプレイ交換は**約17万円**規模（M5世代の確定額は未取得）。
- **本人判断（2026-08-15、同日中に着地）＝15インチをクリーンインストールし、渡航用に持ち歩く主機として立てる。M5とは入れ替えない、両方使う。** M5は表示が出ており実用できるため、家に据えて重作業機として残す（**LM StudioはM5でのみ**。**Final Cutと、ベーシック構成のCubaseは2026-08-16に15インチへ移した**）。15インチAirはフィリピン渡航（8/23）にも持っていく。

> [!warning] この判断で変わること
> - **15インチAirの「隔離機」としての位置づけが終わる。** 実データが入るので、[[🧭 実行環境の能力表（AI共通）]] ㋑の壁（DeepSeekへのデータ管理・OpenClaw暴走隔離）は、この機械では成立しなくなる。
> - **DSの実装機がなくなる。** 夜間無人実行・`ds-apps`の`ds/`ブランチ運用は行き先を失う。M5の「zaza」は毎回承認が要るため代替にならない。
> - **本人が「今は使っていないから大丈夫」と判断済み**（2026-08-15）。AIの提案ではなく本人の決定。

## 決まっていること

| 項目 | 決定 |
|---|---|
| Apple ID | **本アカウント**でサインイン。`ynp97apps15` はGitHub用として残す（GitHubはApple IDと無関係） |
| Word | **Microsoft 365**。Microsoftアカウントでサインインするだけ。5台まで |
| Dropbox | 無料プランは接続台数に上限（3台）。**消す前に旧Airのリンクを解除** |
| Cubase | **★入れる（ベーシックな形で）**（2026-08-16 変更。旧＝「入れない・8GBで実用外」）。**フル制作用ではなく、作曲・構成・コード打込＝21工程の1〜3を持ち歩きで進めるための軽い構成。** 重い工程（ドラム音源・多トラック録音・ミックス＝4〜19）は引き続きM5 |
| LM Studio | **入れない**（8GBに載らない。2026-06-24に否決済み） |
| Final Cut Pro | **★入れる**（2026-08-16 変更。旧＝「入れない・現地編集はしない」）。**理由＝本人が以前この15インチで普通にFCP編集をしていた実績があるため、8GBを理由に外す根拠がなかった。** 賛美21工程のうち詰まっているのは**工程20＝FCPで動画化**なので、持ち歩ける機械に入ると月1公開が机の前以外でも進む。4K素材は内蔵256GBに置かず**BENJAMIN（外付け1TB）から読む** |
| Vault | **正本はAirの内蔵SSD。コピーで運ぶ**（3.7GB、256GBに無理なく収まる）。**外付けSSDには置かない**（一時「外付けSSDに置き繋いだほうで使う」案が出たが、常に持ち運びながら作業する運用に抜き差し前提は合わないため撤回）。同期はしない＝競合が起きない |
| 外付けSSD（`BENJAMIN` 1TB） | **Osmo Pocket 4の4K素材専用。** Vaultとは同居させない。**2026-08-15に消去・再フォーマット済み＝APFS／GUIDパーティションマップ／ボリューム名 `BENJAMIN`**（旧 `TRANSCEND`・HFS+ から変更）。装置 `ESD410C TS1TESD410C`。4Kは1分1GB前後で1TBなら約15時間分 |
| DeepSeek APIキー | 記録済み（本人確認・2026-08-15） |

## 手順

### 1. 消す前に（15インチで）

1. `cd ~/ds-apps && git status && git log --branches --not --remotes` — 未pushの作業がないか。出たらpushする。**ここだけは消すと戻らない**
2. ~~Dropboxの旧デバイスをリンク解除~~ → **不要。15インチにDropboxは入っていない**（本人確認・2026-08-15）
3. デスクトップ・書類の残りを確認
4. **Apple IDからサインアウト**（システム設定 → 一番上の名前 → サインアウト）。**忘れるとアクティベーションロック**

### 2. 消す

**Apple Siliconなので「消去アシスタント」を使う。復旧モードもインストーラーも要らない。**

1. 電源アダプタを繋ぎ、Wi-Fiに接続しておく
2. **システム設定 → 一般 → 転送またはリセット → すべてのコンテンツと設定を消去**
3. 管理者パスワードを入力
4. Time Machineバックアップを勧める画面が出る（**スキップ可**）
5. 消えるものの一覧が出る → 続ける
6. **Apple IDのパスワードを求められる**（ここでサインアウトが行われる。事前にサインアウト済みなら出ない）
7. 「すべてのコンテンツと設定を消去」を最終確認
8. 再起動し、**「こんにちは」の初期設定画面**になれば完了

**所要15〜30分。macOS本体は残るので再インストール不要**（バージョンは消去前と同じ。アップデートはセットアップ後に）。

> [!warning] 初期設定で必ず選ぶもの
> 「**移行アシスタント／情報を転送**」の画面では、**「今は情報を転送しない」**を選ぶ。ここで転送すると、手で入れ直す意味がなくなる（2026-06-24の決定「丸ごと戻さず手で入れる」）。

**「転送またはリセット」が見当たらない場合**はmacOSが古い。先にソフトウェアアップデートを当てる。

### 3. 入れる（移行アシスタントは使わない）

2026-06-24の決定「丸ごと戻さず手で入れる」に従う。

1. **本アカウント**のApple IDでサインイン
2. macOSアップデート
3. Chrome
4. Obsidian
5. Claude（Cowork）
6. ChatGPT / Codex
7. Microsoft 365（Microsoftアカウントでサインイン）
8. Dropbox（必要なら）

**8GBなので「一度に一仕事」**（2026-06-24決定）。必要になってから足す。

### 4. Vaultを運ぶ

M5の `書類/Obsidian Vault`（3.7GB）を丸ごとコピーし、Air側も `書類` 直下に置く。

- **`CLAUDE.md` / `AGENTS.md` が中にあるので、コピーした時点で動作モードもそのまま移る**
- `.obsidian`（105MB）も入るのでプラグイン設定も引き継がれる
- **2台で同時に開かない**

### 4-2. 雑TODO（ZatsuTodo）を移す

**Vaultに入っているのはソースだけ。実体は3か所に分かれている。**

| もの | 場所 | 移り方 |
|---|---|---|
| ソース一式 | `アプリ本体/ZatsuTodo`（**Vault内**） | Vaultコピーで自動 |
| **データ80件（ルーティーン7件）** | `~/Library/Application Support/ZatsuTodo/` | **手でコピー。忘れると空で起動する** |
| ジャンル色 | `~/Library/Preferences/com.ynp97.zatsutodo.plist`（UserDefaults） | 手でコピー。無くても主要7色は自動で入る |
| 実行アプリ | `/Applications/ZatsuTodo.app` | 手でコピー |

**手順（Aを試して駄目ならB）**

- **A. アプリごとコピー** … `/Applications/ZatsuTodo.app` をAirへコピー。同じApple Silicon同士なので動く。初回に「開発元を確認できません」が出たら**右クリック→開く**
- **B. Airでビルド** … `xcode-select --install`（Command Line Tools・数GB。**Xcode本体20GB超は不要**）→ Vault内の `install_update.sh` を実行

**順番＝データを先に置いてから起動する。** 空で起動するとバックアップの世代が余計に増える。

### 5. 1日使って確認する（8/18）

実際に半日〜1日使い、足りないものを洗い出す。ここまでで終わらせ、**8/19以降は触らない。**

## 渡航前に別途手当てが要るもの

> [!success] 外付けSSD `BENJAMIN`（**2026-08-15 フォーマット完了**）
> **1TB・USB接続。Osmo Pocket 4の4K素材専用。** 装置 `ESD410C TS1TESD410C`。
> **ボリューム名 `BENJAMIN`／APFS／GUIDパーティションマップ。** 旧 `TRANSCEND`（HFS+・実質空）を消去して切り直した。
> 4K素材は1分あたり1GB前後なので1TBで**約15時間分**。渡航中の撮影量ならこれで足りる想定（Vaultと同居させる前提だった「2TB推奨」は撤回。用途を分けたため）。
> **消去時のつまずき（次回のため）**＝ディスクユーティリティで**ボリューム**を選ぶと「方式」が出ない。**「表示 → すべてのデバイスを表示」で、1段上の装置行（`ESD410C …`）を選ぶ**と、名前・フォーマット・方式の3つが出る。

> [!success] BENJAMINをDesktop等の置き場にも使う（2026-08-15）
> **M5のDesktop（約189GB）・ZatsuTodo・出席簿・ラベル管理・TODOインボックス（合計約188GB）を`scripts/migrate_app_data.sh collect`でBENJAMINへ集め、置いたままにする運営にした。** Airの内蔵SSD（実質200GB程度）に収まらないため。**Vaultはこの対象から除外**（`ITEMS`からVaultの行を削除済み。正本はAir内蔵SSDのまま、上の決定と矛盾しない）。
> `collect`はコピーのみで、M5側の元データは消えない。**M5側を消すか残すか、Air側からBENJAMIN越しに直接開く運用にするかは未決。**`restore`（Air内蔵へ複製）は今回使わない前提。

> [!warning] Vaultのバックアップ（未確定）
> 13年分の日記1,113件と相談ログがAir 1台（持ち歩く機体）に集約されるため、バックアップが必要。**M5に控えを置いて開かない案が出ている**（M5はVaultを触らない重作業機になるので分岐しない）。方式（丸ごとコピー／Time Machine／月1コピー）と実行時期は未決。渡航前に決めるか、少なくとも一度は控えを置く。

> [!note] DeepSeek APIキーの扱い（判断保留）
> キーがVault内に記録されているなら、**Vaultごと渡航先へ渡る**。基準（他人・鍵・金・内面は渡さない）に触れる形。DeepSeekのキーは再発行できるので実害は限定的だが、渡航前に置き場所を把握しておく。

## M5（割れた側）の扱い

- **表示は出ているので急がない。** 外部モニターに繋いでクラムシェルで使えば、開閉によるヒビの拡大も抑えられる
- **写真と記録は今日のうちに**（ヒビの起点が分かるうち）。付属のポリッシングクロスも保管
- **Appleサポート 0120-277-535**（平日9-19時／土日祝9-17時）へ電話。Genius Barへ直接持ち込むと、その場で保証対象外と判定されて終わりやすい
- 伝える順番＝①ナノテクスチャー仕様 ②**付属のポリッシングクロスで拭いていて割れた** ③落下・衝撃なし ④購入2〜3か月。最後に「**この経緯を記録に残してください**」
- Apple製品限定保証は購入から1年（2027年5〜6月まで）。**急がないが、経緯が新しいほうが通りやすい**
- **無償になるとは限らない。約17万円の有償を前提に置いておくのが安全**
- 渡航中（8/23〜）は手元になくてよい期間なので、修理に出すタイミングとしては噛み合う

## 最終確認

- 2026-08-15 作成（未実行）
- 2026-08-15 背景欄・Vault配置・外付けSSDの扱いを訂正（「M5と入れ替え」「外付けSSD同居」の着地前の文面が残っていたため。最終結論＝両方使う／Vault正本はAir内蔵／外付けSSDはOsmo Pocket 4専用）
