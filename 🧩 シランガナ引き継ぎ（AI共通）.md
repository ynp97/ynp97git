---
種別: アプリ単体引き継ぎ / AI共通
作成日: 2026-07-06
対象: シランガナ
役割: 「シランガナ」と言われた時に、このアプリだけを最小文脈で再開する入口。
---

# 🧩 シランガナ引き継ぎ（AI共通）

## このファイルの使い方

ユーザーが `シランガナ` / `Shirangana` / `しらんがな` とだけ言ったら、まずこのファイルを読む。

原則として、この時点では次を読まない。

- `🧩 アプリ開発状況（AI共通）.md` の全文
- 戦略モードの4ファイル
- 他アプリの開発記録

必要になった時だけ、下の「最初に読むもの」へ進む。目的は、他アプリや人生相談の文脈を混ぜず、シランガナのコード作業だけに入ること。

## 最初に読むもの

1. このファイル
2. 直近の経緯が必要なら `2026-06-20_シランガナ開発記録.md` の `BUILD 21` と `現在の状態`
3. 方針確認が必要なら `シランガナ 7月完成ロードマップ.md`
4. コード作業なら `アプリ本体/Shirangana` の該当ファイル

## 実体

- Git管理コピー: `アプリ本体/Shirangana`
- Xcodeプロジェクト: `アプリ本体/Shirangana/Shirangana.xcodeproj`
- Bundle ID: `com.yoshiakinagumo.shirangana`
- 実機: `iphone15proMAX`
- 現在の正: Vault内の `アプリ本体/Shirangana`
- 旧作業場所: `work/Shirangana`。原則ここは正として扱わない。

## 主要ファイル

- `アプリ本体/Shirangana/Shirangana/Views/CameraScreen.swift`
- `アプリ本体/Shirangana/Shirangana/Views/ImageCropScreen.swift`
- `アプリ本体/Shirangana/Shirangana/Views/ResultScreen.swift`
- `アプリ本体/Shirangana/Shirangana/Services/TextRecognitionService.swift`
- `アプリ本体/Shirangana/Shirangana/Services/ImagePreparationService.swift`
- `アプリ本体/Shirangana/Shirangana/Services/ReadingDictionary.swift`
- `アプリ本体/Shirangana/Shirangana/Services/SpeechService.swift`
- `アプリ本体/Shirangana/project.yml`
- `アプリ本体/Shirangana/AppStore/metadata_ja.md`
- `アプリ本体/Shirangana/AppStore/privacy_policy_ja.md`

## 現在地

- 最終ビルド: `BUILD 27`（公開版からBUILD番号・画像/OCR/辞書の診断表示を除外。Debug版では引き続き表示。Simulator向けDebug/Releaseビルド成功、`iphone15proMAX` 向けReleaseビルド・インストール・起動成功）
- BUILD 27で、`#if DEBUG` を使って撮影画面右上のBUILD番号と、結果/失敗画面の画像パターン・OCR候補・辞書照合の診断パネルを公開用Releaseから外した。辞書照合済みの「ほかの候補をえらぶ」は利用者向け機能として公開版に残した。公開版の失敗文言も「診断を確認」から撮影のやり直し案内へ変更。
- BUILD 26で、左右に実線・点線がある縦書き画像向けに、左右15%ずつを避けた中央画像 `左右の罫線を避ける` と、その画像の文字列を上から横一列へ並べ直す `罫線回避・縦文字再配置` をOCRの早い段階へ追加。`重大` / `出身地` をVisionカスタム語にも追加した。
- BUILD 25で、辞書照合できたOCR候補を「ほかの候補をえらぶ」として大きなボタンで表示。自動結果が違う場合や失敗時にも、その候補を押せば読み・意味の結果へ切り替えられる。
- 2026-07-10時点の区切り: ユーザーがここまでで一旦確認。次回はBUILD 25を実機で撮り、候補ボタンから結果へ切り替わるかを確認するところから再開する。
- 2026-07-10エラー導線確認: カメラ初回許可、拒否時の説明＋「設定を開く」、辞書ファイルなし／オープン失敗時の説明、読みなし時の診断＋候補選択、「もう一度」での再撮影をコード上で確認。Simulator向けDebugビルド成功。コード変更なし。実機ではカメラ拒否状態から設定画面へ移ることだけ未実測。
- 2026-07-10実機診断で、`池田亮司` / `電子音楽` はOCRが読めている一方、辞書照合で失敗することを確認。BUILD 24では、読点などを辞書照合前に除去し、`電子音楽` が `子音楽` のような部分語へ落ちないよう修正。実測人名の `池田亮司` / `池田` も補助辞書とVisionカスタム語へ追加。
- BUILD 23では、10個のOCR画像パターン上限が縦書き分割で埋まり、薄い文字向けの高コントラスト画像がOCRに渡らないことがあった順番を修正。`高コントラスト` と `文字だけ強調` を先に評価する。辞書照合も、複数の2文字以上の語に自然に分割できる長い候補を、短い単語の誤検出より優先するよう強化した。
- BUILD 18で、縦書き処理中に固まる/落ちる問題へ対応済み。
- BUILD 19で、`学級新聞` の補助辞書/カスタム語と、縦長画像で1文字だけを誤確定しない安全弁を追加済み。
- BUILD 20で、縦書きの主文字列を優先して切り出す `主文字縦自動` / `文字だけ主縦自動` 画像パターン、ノイズ記号入りOCR候補で早期打ち切りしない判定、`一部` / `一部分` / `場合` / `形` / `値` の補助辞書・Visionカスタム語を追加済み。
- BUILD 21で、`不現` のような2文字だけの縦書き部分誤検出で早期確定しないようにし、長いOCR候補の一部だけを2文字語として結果表示しそうな場合は失敗画面＋診断へ戻す安全弁を追加済み。存在しない語としてユーザーが確認した `旧人` は辞書結果からブロック済み。
- BUILD 22で、実測サンプルの末尾1字落ち/誤挿入に対し、`受動分` → `受動分詞`、`引照個` → `引照個所`、`旧日約聖書` → `旧約聖書` の辞書前補正を追加。`受動分` / `引照個` / `約聖書` は単独の正解として返さない。長い読みが結果画面で省略されないよう、読みボタンを2行表示対応にした。
- 薄い文字認識は未解決。
- 次は実機で、固まらず結果/失敗画面へ戻るか、1〜2文字だけの部分誤答を大きく結果表示しないかを確認する段階。

## 次の一手

開発確認を続ける場合はDebug版の `BUILD 27` を使う。公開前にはRelease版で、右上のBUILD表示と結果/失敗画面の診断パネルが出ないことを画面で最終確認する。

優先テスト:

- 診断に `主文字縦自動` / `文字だけ主縦自動` が出るか
- 薄い文字で、診断に `高コントラスト` と `文字だけ強調` が出るか
- `電子音楽` が `でんしおんがく` になり、`子音楽` に落ちないか
- `池田亮司` が `いけだりょうじ` になるか
- `一部分が` で小さいふりがなだけでなく、`一部` / `一部分` / `部分` が辞書照合に出るか
- `場合は` で `場合` / `ばあい` へ寄るか
- `不快表現` で `不現` のような一部だけを結果表示せず、診断へ戻るか
- `受動分詞` 系の画像で `受動分詞` / `じゅどうぶんし` に補正されるか
- `引照個所` で末尾の `所` まで補われ、`いんしょうかしょ` になるか
- `旧約聖書` で `旧日約聖書` / `約聖書` に落ちず、`きゅうやくせいしょ` になるか
- `学級新聞` で `新` だけを結果表示せず、失敗画面＋診断へ回るか
- `身長と体` が `身` だけに落ちないか
- `御国` が `翻` に落ちないか
- `高群逸枝` が `枝` だけに落ちないか
- 長い縦文字列で `縦文字自動分割` や `文字だけ...` 系の診断画像に正しいOCR候補が出るか
- 標準カメラとの差、シャッター体感、「もう一度」上固定が実用上問題ないか

## 判断の分岐

- Vision候補に正解が出ていて最終結果が違うなら、辞書照合・候補順位づけを直す。
- Vision候補に正解が出ていないなら、撮影/補正/縦書き分割/候補確認UIを検討する。
- BUILD 22でも縦書き語が読めない場合は、Vision OCRだけで一発正解を狙うより、撮影後UIで候補確認・手動補正へ寄せる。
- 薄い文字は完全解決を完成条件にしない。普通の濃い印刷文字の誤認識を先に減らす。

## 作業後の更新

シランガナ作業の終わりには、基本的に次だけ更新する。

1. `🧩 シランガナ引き継ぎ（AI共通）.md`
2. `2026-06-20_シランガナ開発記録.md`
3. 大きな状態変更がある時だけ `🧩 アプリ開発状況（AI共通）.md` のシランガナ節

相談ログや他アプリの台帳は、明示的な依頼がない限り更新しない。
