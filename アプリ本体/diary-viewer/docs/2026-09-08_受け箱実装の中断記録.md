# 2026-09-08｜受け箱実装の中断記録

> この中断は同日中に再開した。最新は [[アプリ本体/diary-viewer/docs/2026-09-08_受け箱と日記表示の検証]]。以下は中断時の履歴。

本人の「一旦記録して」で中断。設計に続きコードを実装し、検査まで進めた。**新しい `.app` の組立て・起動・画面操作確認は完了していない。** 次回は下の「再開するところ」から。機能を再実装しない。

## 実装したもの

- `Sources/DiaryCore/CaptureLibrary.swift`: テキスト原文を受け箱へ保存するSQLite＋原文ファイルの保存処理。UUIDを維持し、日付不明を許容する。日付訂正は改訂番号・履歴を持つ。保存のpending記録からの復旧、同じ操作IDの再試行による二重登録防止、ハッシュ照合、書込ロック、受け箱バックアップを実装。
- **実際の保存場所**: 選んだライブラリの `日記アプリデータ/library.sqlite`、`日記アプリデータ/originals/<UUID>.txt`、保存途中は `日記アプリデータ/staging/`。この段階のバックアップは受け箱専用で、既存の年別日記・写真・動画を含まない。画面にもそう明示してある。
- `Sources/DiaryCore/OriginalJournal.swift`: 生バイト・コメント・空白・改行を保つ日記ドキュメントモデル。件数・ID・ハッシュを検査し、指定ブロックだけを**メモリ内で**差し替える。本人の既存日記へ書き戻すAPIはまだ公開していない。
- `Sources/DiaryViewer/CaptureInboxView.swift`: 文章の追加、原文表示、日付指定／未定、日付更新、受け箱バックアップの画面。ContentViewのツールバーから開く。コードはビルドできたが、クリック等の実機確認はこれから。
- `JournalStore.swift`: 読込年2013〜2026固定を廃止し、4桁年のMarkdownを列挙する形へ変更。検証用の `DIARY_LIBRARY_PATH` を追加。この環境変数がある場合は、そのライブラリを開き、本人の保存済みフォルダ設定を書き換えない。画面には動作確認用の表示を追加。
- `Entry.swift` / `DetailView.swift`: 「bodyが原本そのまま」という誤ったコメントを訂正。
- `Package.swift`: DiaryCoreとそのテストターゲットを追加。既存テストのFixtures参照先を実在させ、架空fixtureを `Tests/DiaryViewerTests/Fixtures/journal/` へコピーした。
- `build_app.sh`: バージョン0.2.0/build 2、`DIARY_BUILD_PATH`・`DIARY_CONFIGURATION` を追加。アプリとして配布可能になったという意味ではない。

## 実施できた検証

**既存27件＋新規14件、計41件の同期検査を実行し、失敗0。** 使用したのは `scripts/run_portable_checks.py`。正本のXCTestテストソースを一時コピーし、このプロジェクトで使う同期assertionを代替実行する。**`swift test`が通ったとは言わない。** ランナーは未知のXCT APIを黙って省略せずコンパイルで失敗させる。非同期テストやXCTest固有機能の代替ではない。

確認内容:

- 原文の空白・LF/CRLF・コメント・引用・絵文字、無編集往復のバイト一致。
- 同日時の複数エントリ、本文の見出しとコード中の例示日付、件数不一致・ID重複・不正UTF-8・古いハッシュでの拒否。
- 保存前準備／原本配置後／確定後の**疑似中断**から再開し、同じIDの再試行で1件だけになること。
- 書込先を妨げたときもstagingの原文が残り、障害を取り除くと復旧すること。
- 日付修正の改訂番号、古い画面からの更新拒否、二つのDB接続の整合性。
- 原文ファイルの改変検出、新しすぎるDB版の拒否、ライブラリ外へ向くシンボリックリンクの拒否。
- 受け箱バックアップを別ライブラリとして開き、原文・日付・ID・改訂が一致すること。
- 通常のアプリ本体 `swift build` は成功。最後の再ビルドも成功。

ログ正本: `検証記録/2026-09-08-portable-checks.log` と `検証記録/2026-09-08-app-build.log`（本書と同じdocs配下）。

## 現在の失敗と限界

1. **このMacはCommand Line Toolsのみで、XCTestモジュールが見つからない。** `swift test` は `no such module 'XCTest'` で停止した。上記の代替検査は実行できた。
2. **`.app` 組立てはアイコン変換で停止。** `iconutil -c icns AppIcon.iconset ...` が `AppIcon.iconset:Invalid Iconset.`。まだ原因調査・修正はしていない。`scripts/make_icon.py` と既存PNGを確認するところから再開する。本人の中断指示後には追っていない。
3. **SwiftPMリソース配置にも確認点がある。** 今回生成された `resource_bundle_accessor.swift` は `Bundle.main.bundleURL/DiaryViewer_DiaryViewer.bundle` を探し、その後に一時ビルド先の絶対パスへフォールバックする。一方、旧build_app.shは `Contents/Resources` にコピーする。**ビルド先が存在する間だけ動くことを、配布成功と誤認しない。** `.app` の同梱リソースだけで解決できるようにして確認する必要がある。
4. `.app` の起動、受け箱画面、保存ボタン、再起動後の表示、バックアップボタンの実機操作は未実施。CUAの操作環境の一覧を取得したところまで。
5. 今回の永続IDは**新規の受け箱記録**。既存閲覧の `Entry.id = UUID()` はまだ置き換えていない。旧日記との関連ID、既存本文への保存、年移動・ゴミ箱、複数ファイルにまたがる保存回復もこれから。保存設計全体を実装済みと扱わない。
6. Wordファイルの展開、日付自動検出、日記本体への転記、レシートOCR、支出台帳、写真・動画の新規投入はこれから。現在の入口は**テキスト貼り付け／直接入力の受け箱**。
7. 本人の日記・写真は変更していない。検証は一時フォルダ内の架空データ。`/Applications` へインストールしていない。git commit／pushもしていない。

## 再開するところ

1. 上記アイコン変換とリソース配置を直し、`.app` を組み立てる。本人の既存アプリを上書きしない。
2. 架空ライブラリ `/private/tmp/DiaryViewer-Review-Library-0907` を使う。2026・2027年の各1件と `journal-baseline.json` を用意済み。この一時フォルダが消えていれば架空データとして作り直す。
3. `DIARY_LIBRARY_PATH` でそのフォルダへ隔離し、CUAで起動・受け箱を開く→文章を貼る→日付未定で保存→日付修正→開き直して同じ原文が見える、を確認。年別日記のSHA-256がbaselineと一致することも照合する。
4. バックアップボタンの結果と復元した受け箱を確認。その後、受け箱から執筆日の日記へつなぐ工程へ進む。

動いたコマンド（最終確認: 2026-09-07の実行ログ、記録は9/8）:

```sh
cd "/Users/yoshiakinagumo/Documents/Obsidian Vault/アプリ本体/diary-viewer"
python3 scripts/run_portable_checks.py

env CLANG_MODULE_CACHE_PATH=/private/tmp/diary-clang-cache-0907 \
  SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/diary-swift-cache-0907 \
  swift build --scratch-path /private/tmp/diary-viewer-build-0907 \
  --cache-path /private/tmp/diary-spm-cache-0907 --disable-sandbox

# このコマンドはアイコン変換で止まっている。解決後に再実行する。
env CLANG_MODULE_CACHE_PATH=/private/tmp/diary-clang-cache-0907 \
  SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/diary-swift-cache-0907 \
  DIARY_BUILD_PATH=/private/tmp/diary-viewer-build-0907 \
  DIARY_CONFIGURATION=debug bash build_app.sh
```

標準のキャッシュ先は権限制限で書けなかったので、一時フォルダへ変更して通した。初回は標準モジュールの再構築に数分かかった。環境変数HOMEやCODEX_HOMEを変える必要はない。

## 会話上の持ち越し

本人は継続を指示していた。Codexが設計終了だけで一度止まったことに「なんで止まった？」と指摘があり、実装へ戻った。今回は**本人の「一旦記録して」による中断**。次回は許可を取り直すための設計説明から戻らず、上記の実装・検証の続きへ進む。推論は保存・復旧工程なので高を推奨する段階のまま。
