---
種別: Word出力仕様 / AI共通
作成日: 2026-07-19
更新日: 2026-07-19
適用範囲: 全モード共通。Word（.docx）を作るときは常にこの書式に従う。
基準原稿: 南雲の説教原稿docx（例「20260118マルコ1:12-20 「イエスに従った。」」）
---

# 📄 Word出力仕様（AI共通）

## 目的

すべてのWord化で、南雲の実際の原稿と同じ組版に揃える。モード（st97/set97/その他）や文書の種類を問わず、`.docx` を作るときは必ずこの書式を適用する。内容を確定してから一回生成し、全ページを一回確認する。

## 用紙・余白・フッター

- 用紙：A4縦。
- 余白：上35mm、下・左右30mm（header 15mm、footer 17.5mm）。
- フッターに中央揃えのページ番号。独立表紙・目次・不要な空行は作らない。

## フォント

- 日本語（見出し・本文とも東アジア字形）：**Meiryo UI**。
  - > [!note] 2026-09-04 修正：`scripts/vault_docx_template.docx` の見出しスタイル（Heading 1〜9）が、テーマ参照（`majorEastAsiaTheme`）のままで**和文がＭＳ ゴシック・欧文がCalibriに解決されていた**。この仕様から外れていたので、**Meiryo UI の直指定に直した**（本人承認・2026-09-04）。元にした `釈義/マルコ3章1-6節 釈義.docx` が持っていた状態を引き継いでいたもので、それ以前の釈義Wordも同じだった。本文（Normal）は初めから仕様どおり（和文 Meiryo UI／欧文 Kozuka Mincho Pro L）。
- 本文の欧文・数字：**小塚明朝 Pro L**。
- 検証環境にこれらのフォントが無くても、DOCX内部の指定は変えない。

## 文字サイズ・スタイル

- 文書タイトル（説教題スタイル）：16ポイント、太字。1行目に日付＋箇所＋題。
- 本文・解説・ポイント：10.5ポイント。
- 段落見出し：太字。本文と区別できれば可。
- 背景・語句、ポイント、適用は実際の箇条書きにし、字下げを浅くする。
- 強制改ページは使わず、見出しだけがページ末尾に残らない設定を使う。

## 聖書本文の色分け・字下げ（重要）

聖書はすべて10.5ポイント。**背景色は付けない。** 2種類を色で区別する。

- **釈義する本文（当日の中心テキスト）**：節番号（ラベル）だけ **青 #006699**、本文テキストは **黒＋太字**。
  - > [!note] 実装値は `scripts/docx_style.py` の `BLUE`（色）と `VERSE_PT`（文字サイズ）。**変えるときはこの1か所を直す。**
  - > [!warning] 2026-09-04：AIが一度ここを 12pt / #004E7A に変更したが、**本人の指示は印刷用HTML／PDF側のことだった。誤りとして元に戻した。** Wordの数値は従来どおり。
- **引用・参照する聖句（他箇所からの引用ブロック）**：ラベル・本文とも **オレンジ #ED7D31**。
  - > [!warning] 2026-08-09 訂正：説教原稿の実物は **#833C0B**
    > 本人の実原稿（20260628マルコ1:12-20／20260614創世記33:1-20）の「引用聖書箇所」スタイルを解体したところ、色は `accent2` の themeShade 80＝**#833C0B**（ED7D31を暗くした色）だった。ラベルは**青 #006699**（本文と別の色）。テーマ名から逆算したときの取り違えと思われる。**説教原稿では [[📄 説教原稿Word出力仕様（AI共通）]] の実測値を使う。**
- **字下げ**：聖書は必ず一段字下げ（左インデント。目安360twips＝約0.25インチ）。普通の本文・解説は字下げせず左端そろえ。これで聖書箇所がはっきり浮く。
- 節ラベルは**日本語書名**で表記する（例「エゼキエル27:26」「マルコ1:12」）。英語書名（Accordance貼り付けの「Mark 1:12」等）は使わず、日本語書名に直す。
- **聖書本文は必ず全文収録する（`…`で省略しない。全スキル共通ルール）。**

## 生成と検証の手順

1. **`scripts/` の固定の道具で作る（2026-09-04 追加）。** 手書きでゼロから組み立てない。
   - 共通の土台＝**`scripts/docx_style.py`**（色分け・字下げ・表組みの実装）＋ **`scripts/vault_docx_template.docx`**（本人の実原稿から本文を抜いた空テンプレート。用紙・余白・フォント指定・見出しスタイル・フッターのページ番号はここが持つ）。
   - **釈義・原稿などの Markdown → Word**: `python3 scripts/exegesis_docx_build.py "<入力.md>"`（引数はmd1つでよい。出力先・表題・副題は自動）。
   - **早天（st97）のペイロードJSON → Word**: `python3 scripts/st97_docx_build.py "<payload.json>"`（→ `output/` に出る）。詳細は [[🌅 早天Word出力仕様（AI共通）]]。
   - どちらも `python-docx` が要る（無ければ `pip3 install python-docx`）。**書式を変えたいときは docx_style.py とテンプレートの側を直す。** 個々の出力で手を入れると、また二重管理になる。
   - 新しい種類の文書で道具が無いときだけ、documentsスキルを読んで作り、**次から使えるよう `scripts/` に道具を残す。**
2. 文書ごとの内容の順序は、そのモード／スキルの指定に従う（早天なら早天Word仕様の順序）。書式はこのファイルに従う。
3. 完成後にDOCXをレンダリングし、全ページを画像で確認する。
4. 文字切れ、重なり、不自然な空白、見出しだけのページ末尾、読みにくさがあれば訂正し、再確認する。

## 品質基準

- ページ数を減らすために本文・背景・原語・釈義上の重要点を削らない。
- ページ数の固定目標を置かず、10.5ポイント未満に縮小しない。
- 最終ページが少量だけなら、余白・行間・見出し間隔を小さく調整できるが、読みやすさを優先する。
- 全ページ確認で問題がなければ、追加の試作や調整を行わない。
