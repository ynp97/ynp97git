---
種別: 能力表 / AI共通
作成日: 2026-08-01
役割: 「この作業に最適な実行環境はどれか」を1枚で引くための表。**全モード共通で、作業を始める前に引く。** 個別の設定値・手順は各引き継ぎが正本で、ここには書かない。
---

# 🧭 実行環境の能力表（AI共通）

> [!important] なぜこの表があるか（2026-08-01 作成の経緯）
> screen-recorder（スクトレル）で、Cowork Claudeが**Swiftをビルドできない**まま何往復もビルド出力を貼らせ、遠回りした。`build.sh` に書かれた証明書IDが実在しないという原因は、`security find-identity` を**1回叩けば分かること**だった。
> 原因は知識不足ではなく**構造**。`app97` は「コードをやるが環境を見ない」、`ai97` は「環境を見るがコード作業中に呼ばれない」設計になっており、**「この作業をできる環境はどれか」がどのモードの担当でもなかった。**
> よってこの表は、**モード分断の唯一の例外**として全モードから引く。詳細は書かない（1画面に収める）。

## 1. できること・できないこと

| | このMac（M5）<br>Cowork Claude | Claude Code<br>（**使える**） | Codex<br>（ChatGPTアプリ） | **DS（M5・本機）**<br>OpenClaw「zaza」 | DS（15インチAir）<br>実装専用機 | LM Studio<br>（ローカル） |
|---|---|---|---|---|---|---|
| **macでシェル実行** | ✗ Linuxサンドボックス | **○ 本機で直接** | ○ 本機で直接 | △ **毎回いちいち承認が要る**（`exec.ask=always`）。本人が使いたがらない | ○ Air上で自由に | ✗ |
| **Swift/Xcodeビルド** | ✗ | **○** | △ **`swift build`等で止まる問題あり**（9.5.1） | ✗ **当てにしない**（上記の承認地獄） | ○ | ✗ |
| **macのファイル読み書き** | ○ Vault内 | ○ | ○ | ○ 本機 | Air側のみ | 読み取り（RAG） |
| **GUI操作（クリック・入力）** | ○ computer-use。**ただしターミナル/IDEは打鍵不可、ブラウザは閲覧のみ** | ✗ | ✗ | ✗ | ✗ | ✗ |
| **画面を見る** | ○ **ただしシステムダイアログとCommand+Shift+5は映らない** | ✗ | ✗ | 画像入力は可（VL切替） | ✗ | Vision対応モデルあり |
| **権限（TCC）の付与** | ✗ **本人のみ** | ✗ | ✗ | ✗ | ✗ | ✗ |
| **Touch ID・パスワード** | ✗ **本人のみ** | ✗ | ✗ | ✗ | ✗ | ✗ |
| **Web検索・取得** | ○ | ○ | ○ | △ | △ | ✗ |
| **費用・枠** | Claudeの枠 | Claudeの枠 | **週間枠（いちばん高い）** | DeepSeek API（数円〜） | DeepSeek API | 無料・電気代のみ |
| **記憶** | memory＋Vault | Vault（CLAUDE.md有効） | Vault（AGENTS.md有効） | **セッション履歴あり**（Control画面に一覧） | **記憶なし**（毎回冷える） | セッション内 |
| **最終確認** | 2026-08-01 | **2026-08-08 本人「動かせます」**（能力の実測はこれから） | 2026-07-25 | **2026-08-01 実画面確認** | 2026-07-30 | **2026-08-01 実画面確認** |

> [!warning] 2026-08-01の訂正
> 当初この表は「DS＝15インチAirだけ」と書いていたが、**本機（M5）にもOpenClawが入って日常稼働している**（アプリ名 `DS` ／ `com.user.ds-openclaw` ／ Chromeアプリとして起動 ／ エージェント名 **zaza** ／ モデル **DeepSeek V4 Pro · Maximum** ／ セッション履歴多数）。本人の指摘で発覚。**「DSは別のMacの話」という思い込みが、今日の遠回りの一因。**

## 2. 作業から引く（逆引き）

| やりたいこと | 最初に検討する環境 |
|---|---|
| **Swift/ネイティブアプリをビルドして実機で試す** | **Claude Code**（本機で完結）。無ければ Codex 直行 |
| **スクリプトを走らせて出力を読む往復**（テスト・データ取得・実測・ログ確認） | **Claude Code**。Cowork Claudeは別ファイルシステム＋外部取得不可で、1回ごとに画面を撮って読むことになる |
| **Macのlocalhostへ届く必要がある**（LM Studio 1234／自作サーバー等） | **Claude Code**。Cowork Claudeのサンドボックスからは届かない |
| 小さなバグ修正 | **Codex直行**（DS委任は固定費で割高。9.5.1） |
| 数時間級の実装・大量の定型変更 | **DS（Air）**＋作業票。`ds-apps` の `ds/○○` ブランチ |
| 差分レビュー（C層） | 記憶のない別DSセッション（1円未満） |
| GUIを触る・画面を見ながらの作業 | **Cowork Claude**（computer-use）。ただし上の制約に注意 |
| 権限付与・Touch ID・物理操作 | **本人のみ。どのAIも不可** |
| 文書作成・調査・戦略相談 | Cowork Claude |
| 費用を気にせず大量に回す | LM Studio（ローカル） |
| **Obsidian内で完結させたい／Vaultを直接書き換えさせたい** | **Obsidian Copilot の自律エージェント**（§4-2）。モデルはGemini/Claude/GPT/ローカルから選べる |

## 3. 使い方の注意

> [!warning] ★2026-08-16 実測 ── 主機がAirに移ったことで、表の前提が2つ崩れている
> この表の列は **本機＝M5** を前提に2026-08-01に書かれている。**2026-08-15に主機が15インチAirへ移ったので、Airでの可否は測り直しが要る。** 同日わかったのは次の2つ。
> - **Cowork Claude の computer-use が使えない**（Air側でmacOSの**画面収録の許可が未付与**のため `request_access` が失敗する）。表では「GUI操作 ○」だが、**現状のAirでは ✗。** GUIを触る仕事を当てにする前に、ここを本人が許可する必要がある。
> - **ターミナルで `claude` が通らない**（`command not found`）。ただし**本人は「Claude Code は入っている」と明言し、実際に実行できた**ので、別経路（デスクトップアプリ等）で動いている。**経路は未確認。** ターミナルから叩く前提の手順を書かない。
> - **一方で Chrome拡張（claude-in-chrome）は接続済み**（2026-08-16 `list_connected_browsers` で確認、macOS・ローカル1台）。
> - **Cowork Claude のファイル系ツールは `/Volumes` を読める**（Glob/Grepは届く。Read/Grepの**内容読み**は接続フォルダ内に限られる）。**書き込み・コピーはできない。**

- **表が古くなると嘘をつく。** 各列の「最終確認」より古い前提で断定しない。Claude Code は 2026-08-08 に本人が「動かせます」と明言したので**使える前提でよい**が、○×の中身はまだ実測していない。
- **「できない」で止めない。** できない環境に当たったら、この表で**できる環境へ移す**ことを先に検討する（`CLAUDE.md` 振る舞いの原則3の㋐「同じAI・アプリを対象端末へ直接入れられないか」）。

> [!important] ★Cowork Claude → Claude Code へ渡す合図（2026-08-08 本人指示「効率が良ければすぐさま切り替えられるように」）
> **Cowork Claudeは、作業の途中で次のどれかが起きたら、そこで手を止めて「Claude Codeへ渡したほうが速い」と言う。** 終わってから振り返るのでは遅い。
>
> 1. **実行結果をスクリーンショットで読んだ**（1回でも）
> 2. **スクリプトや `.command` をGUIのクリックで起動した**
> 3. **Macのlocalhost・実機の環境に届く必要が出た**
> 4. **編集→実行→確認のループが3回を超えそう**
> 5. **本人に出力を貼らせようとした**（原則3の禁止事項。ここへ来る前に切り替える）
>
> **理由**: Cowork ClaudeのシェルはMacと別のファイルシステムで、外部への取得もできない。だから上の状況では、1回の実行ごとに画面を撮って読む往復が必ず入る。Claude Codeなら `python3 ○○.py` を叩いて標準出力をそのまま読むだけで済む。**同じClaudeの枠を使うので、費用ではなく往復回数の差**として効く。
>
> **逆に、渡さないほうがよい仕事**: GUIを見ながらの操作・画面確認（Claude Codeは画面を見られない）、文書作成・調査・戦略相談。
>
> **由来**: 2026-08-08、カード研究室の取り直しを起動するのにFinderをクリックし、ターミナルの中身をスクリーンショットで読んだ。下見が誤判定したときも診断用 `.command` を書いてダブルクリックし、また画面を撮った。screen-recorderでビルド出力を貼らせた件（冒頭warning）と同じ構造の小型版。
- 詳細な手順・設定値はここに書かない。正本は [[🤖 AI関連引き継ぎ（AI共通）]] → 各引き継ぎ。

## 4. 端末構成（2026-08-01 更新）

- **MacBook Pro（本機／Apple M5・32GB）** — 設計・指示・検品。**Cowork Claude ／ Codex ／ DS（OpenClaw「zaza」・DeepSeek V4 Pro Maximum）／ LM Studio** が**すべてここに同居している**。**本機のOpenClawは意図的に制限がきつい**（毎回承認）ので、実作業には向かない。
> [!danger] ★2026-08-15 — **Airの隔離は終了した。この節の以下の記述は歴史的経緯として残す**
> **M5のディスプレイ破損（2026-08-15・ナノテクスチャーを付属クロスで拭いていて割れた）を受けて、本人判断で15インチAirをクリーンインストールし、本アカウントで主機として立て直した。**
> - **実データが入るので、「内部が晒されても構わない機械」ではなくなった。** DeepSeekのデータ管理・OpenClaw暴走隔離の壁は、**この機械では成立しない。**
> - **DSの実装機がなくなる**（夜間無人実行・`ds-apps`の`ds/`ブランチ運用は行き先を失う）。M5の「zaza」は毎回承認が要るため代替にならない。**本人了解済み**（「今は使っていないから大丈夫」）。
> - **これは本人の決定であって、AIが効率を理由に提案したものではない。** ㋑のdangerブロックは「AIが壁を崩す提案をするな」という規則であり、本人が方針を変えることを禁じるものではない。
> - **現在の役割分担＝M5は家での主機**（32GB。**LM Studio**はここでしかできない）**／Airは渡航機と軽い作業。両方使う。**
> - **★Final Cut Proは15インチAirに入れる**（2026-08-16 変更。8/15の「入れない」は撤回。**本人が以前この15インチで普通にFCP編集をしていた実績があるため**）。4K素材は内蔵256GBではなく**BENJAMIN（外付け1TB）から読む**。
> - **★Cubaseもベーシック構成で15インチAirに入れる**（2026-08-16 変更。8/15の「8GBで実用外」は撤回）。**21工程の1〜3（作曲・構成・コード打込）用。** 重い工程4〜19はM5。**⚠ プロジェクトファイルの置き場は未決**（2台で同じ曲を触ると正本が分からなくなる）。
> - **Vaultは外付けSSDに置き、繋いだほうのMacで使う**（同期しない＝競合しない）。Apple IDは2台共有、Air側のiCloud「デスクトップと書類フォルダ」同期はオフ。
> - 手順と経緯: [[🖥 15インチ移行手順（2026-08）]]

- ~~**15インチ MacBook Air（M2・2023年モデル・型番 A2941・メモリ8GB・SSD 256GB）**（`192.168.68.52`、画面共有で本機から操作可） — **暴走前提の隔離機。Appleアカウントも分離済みで、内部が晒されても構わない設計。** ここでOpenClaw＋DeepSeekを**制限なしで動かす**。GitHub `ynp97apps15`。実データを扱わない。~~ ← **2026-08-15に終了（上記）。** スペック（M2・A2941・8GB・SSD 256GB）と `192.168.68.52` は有効。GitHub `ynp97apps15` はGitHub用アカウントとして残る（Apple IDとは無関係なので `ds-apps` への権限は影響を受けない）。
  - > [!important] ★ローカルAI（LM Studio）は「未導入」ではなく**入れないと決めた**（2026-06-24にメモリ確認、日記アプリの回で否決）
    > **理由はコスト論。** 8GBに載る小型モデルはコード品質が落ち、**検品（いちばん高い週間枠）が増えて「DSへ逃がした意味が消える」問題に戻る。** テスト目的ならM5のLM Studioを叩くかダミーで足りる。
    > 8GBなので重い動画編集・Xcode本格運用・Docker・ローカルLLM・大量タブ運用は避け「一度に一仕事」で使う、という運用も2026-06-24に決めてある。
    > **★この項目は少なくとも3回掘り直されている**（2026-06-24決定 → 日記アプリの回で再発見 → 2026-08-15にClaudeが「未確認」として再々提示）。**「未導入」という書き方が「まだやっていないだけ」に読めたのが原因なので、決定として書き直した。**
    > **2026-08-15 本人回答で機種確定＝A2941（15インチ MacBook Air, M2, 2023）・メモリ8GB・SSD 256GB。** メモリ帯域100GB/s、統合メモリ8GBが上限なので、**否決の理由（8GBに載る小型モデルは品質が落ち検品が増える）はそのまま成立する。** ここは決着済みとして、以後は掘り直さない。
    > **SSD 256GBも効く**: M5のLM Studioは7モデルで76.62GB。同じ規模はAirに置けない。

  - > [!warning] ★夜間無人実行の停止要因として、ディスク残量を数える（2026-08-15 追加）
    > **SSD 256GB。** 9.5.8では停止要因を「承認待ち」と「AGENTS.mdの止まれ指示」の2つに整理したが、**ディスクフルという3つ目がある。** `node_modules`・ビルド生成物・Xcode関連・Dockerイメージ・DSが作る架空テストデータが、無人で走るほど溜まる。**埋まれば朝には止まっている。**
    > 送る前の確認（9.5.8）に **④ディスク残量** を足す。溜まりやすいのは使い捨ての作業ブランチなので、**定期的に古い作業ツリーを消す**運用のほうが、容量を気にしながら走らせるより楽。**未検証＝現在の空き容量は未確認。**
- **受け渡し** — `ynp97/ds-apps`（非公開）の `ds/○○` ブランチのみ。アプリごとに別リポジトリを作らない。
- **使い分けの原則**: 実データ・本番作業は**本機**。使い捨ての実装・夜間無人は**Air**。

## 4-2. Obsidian Copilot ── **もう一つの入口**（2026-08-01 `data.json` 実査）

**Copilotは「ローカルAIの入口」ではない。オンラインAIも同じ枠から使える、Vaultに読み書きできるエージェント経路。** 本人談「使っていないが、Gemini も使えるようにしてある」。設定の実体は以下（`.obsidian/plugins/copilot/data.json`、Copilot **v3.3.3**）。

**★これが本人の言う「Obsidianにフルアクセス」の実体**:

- `enableAutonomousAgent = **true**`
- 有効ツール: `localSearch` / `readNote` / **`writeFile`** / **`editFile`** / `webSearch` / `updateMemory` / `pomodoro` / `youtubeTranscription`
- つまり **Vault QA（読むだけ）ではなく、Vaultのファイルを書き換えられるエージェント**が有効になっている。反復上限 `4`。
- 記憶の置き場所 `copilot/memory`（`enableSavedMemory = true`）

**有効になっている回答モデル（ONのみ）**

| 提供元 | モデル |
|---|---|
| Google 直 | `gemini-3.5-flash` / `gemini-3.1-flash-lite` / `gemini-2.5-flash` |
| Anthropic 直 | `claude-sonnet-4-6` |
| OpenAI 直 | `gpt-5.5` / `gpt-5.4-mini` |
| OpenRouter 経由 | `google/gemini-3.5-flash` / `google/gemini-2.5-flash` |
| LM Studio（ローカル） | `qwen3.6-35b-a3b-mlx`（**既定**）/ `qwen3-30b-a3b-2507` / `qwen3-8b` |

（off で待機中: gemini-3.1-pro-preview、gemini-2.5-pro、gpt-4.1系、claude-opus-4-7、claude-haiku-4-5、grok-4.3、deepseek-chat / reasoner ほか）

> [!important] **本人の意図（2026-08-01 本人談）── 設定はこれに合っていない**
> 「**GPT / Claude のトークンが切れたときに相談できる保険として、Gemini の無料枠を使いたい。** ただしObsidianとの連携が欲しいので、その窓口としてCopilotを使うことにした。ついでにローカルも使えないかとQwenを入れたが、**Qwenは精度が低くて使い物にならないと感じている**。**GPT/Claudeが入っている理由は自分でも分からない。**」
>
> **答え: GPT・Claude・Gemini はすべてCopilotの「組み込み（`isBuiltIn`）」リスト。本人が入れたものではない。** 本人が自分で追加したのは **LM Studio の3つ**（qwen3.6-35b / qwen3-30b / qwen3-8b）と、**SiliconFlow の DeepSeek 2つ**（どちらもOFF）だけ。組み込みモデルは一覧に並ぶだけで、**APIキーを入れなければ動かない**。
>
> **Copilotとは何か（3行）**: Obsidianのプラグイン。**AI本体ではなく、Obsidianと外部AIをつなぐ配線。** 接続先（Gemini / GPT / Claude / ローカルのLM Studio）を選び、Vaultのノートを検索して渡したり（Vault QA）、ファイルを読み書きさせたり（自律エージェント）する。一覧のモデルは「配線できる先の候補」であって、繋がっている先ではない。
>
> **意図と現状のズレ3点**
> 1. **既定モデルが Qwen3.6（ローカル）のまま。** 保険はGeminiのはずなのに、使い物にならないと感じている方が既定になっている。
> 2. **「Qwenの精度が低い」の一部は設定が原因の可能性。** 2026-07-16に「内部推論だけで出力上限2000のうち1999を使い、本文ゼロで `Response Truncated`」という事故が記録されており、**現物は今も `maxTokens = 2000`**（§4-2の警告）。**モデルの頭が悪いのではなく出力枠が足りていない**可能性がある。
>    - > [!success] **2026-08-15 追記 ── 「直して試すまで分からない」は解消済み。再テスト不要。**
>      > **`maxTokens = 2000` はCopilot経由だけの設定。** 2026-08-08のpoke97質問窓（**LM Studio直叩き**）で `qwen3.6-35b` は175秒・A群5種を正しく読み分け・作り話なしで**3モデル中唯一の合格**（§5および [[アプリ引き継ぎ/W poke97 カード研究室]]）。**モデル側は別経路で検証済み。**
>      > **「使い物にならない」と「唯一の合格」は矛盾ではなく、仕事の種類が違う。**
>      > - **渡した材料の中で読み分け・分類・整形する** → **合格。**
>      > - **知識を出させる**（カードの収録セット・相場など） → **捏造し、指摘後も撤回せず詳細を増やす**（メガリザードンXex、および本人がジャーナル2026に「多数の質問で検証」と記録）。
>      >
>      > **したがってQwenの再テストを提案しない。用途で切る。** 直す必要があるのは Copilot 経由を使うときの `maxTokens` だけで、それは設定修正であってモデル評価ではない。
>      > **★この一文（「直して試すまで分からない」）が、2026-08-15にClaudeを「もう一度テストしては」へ引っ張った。**
> 3. **Copilot経由でGPT/Claudeを使っても「枠切れの保険」にはならない。** ChatGPT Plus / Claude Pro の定額枠とは別会計の**API従量課金**になるため。**保険＝Geminiの無料枠、という本人の判断は正しい。** GPT/Claudeの組み込み登録は目的に合っていない。
>
> **整理の方向（案・未実行）**: ①既定モデルを `gemini-3.5-flash` にする ②`maxTokens` を6000へ直したうえでQwenを一度だけ再評価する ③GPT/Claude/OpenRouter/copilot-plus 系はOFFにして一覧から消す（使えないものが並ぶと選択を誤る）④**先にGeminiのAPIキーが実在するか確認する**（下記）。

**注意点**

- **APIキーは `data.json` に無く、macOSキーチェーンに保存**（`_keychainOnly = true`。設定画面のAdvancedタブに「API Key Storage: Obsidian Keychain」と表示される）。よって**どのオンラインモデルが実際に使えるかは、ファイルからは判定できない。** 「登録済み＝使える」ではない。
- > [!success] **2026-08-01 検証済み ── Geminiの保険は成立している**
  > 本人がモデルを `gemini-3.5-flash` に切り替えて送信したところ、**応答が返った**（10:47:55）。**キーが無ければエラーで止まるので、これが有効性の証拠。** GPT/Claudeの枠が切れたときの相談先として機能する。
  >
  > **★このとき起きた誤解（覚えておくこと）**: 本人が「geminiでは無いんですね」と尋ねたところ、AIが「Geminiが使用されているか確認できません」と答えた。**これは異常ではない。AIは自分がどのAPI経由で呼ばれたか知らされていないので、身元を聞かれても答えられない。** **身元の確認は、AIに聞くのではなく画面下のモデル名表示で行う。**
  >
  > **同時に判明**: そのときのモードは **`chat (free)`**（Vault QAではない）。`Relevant Notes: No relevant notes found`／文脈 `<1k` で、**Vaultをほとんど読んでいない状態**だった。「Vaultの記録を確認する限り」と答えていても実際は読んでいない。Vaultを引かせるにはモード切替か `@ Add context` が要る。
- **`isPlusUser = false`** ＝ Copilot Plus 非加入。`copilot-plus-*` 系のモデル・埋め込みは表に出ていても使えない。
- **`defaultChainType = llm_chain`** ＝ **既定はVault QAではなく普通のチャット。** Vaultを参照させたいなら毎回モードを切り替える必要がある（2026-07-15に「`ynp97` をポケモンカード097と誤解して架空の商品を生成」した幻覚事故は、この状態で起きている）。
- **Vault QAの索引除外**: `ジャーナル` / `Obsidian_ChatGPT_Context_Vault` / `アーカイブ` / `scripts` / `_査定結果` / `media` / `e-bay PIC` / `output` / `copilot` / `tmp`。**日記・ジャーナルは検索に出てこない。**
- `passMarkdownImages = true` ＝ 画像もモデルへ渡す設定。

> [!warning] 台帳と現物の食い違い（2026-08-01 発見）
> [[🤖 Obsidian×ローカルAI引き継ぎ（AI共通）]] には「Qwen3.6が内部推論だけで1999トークン使い切り本文ゼロで終了したため **`maxTokens` を6000へ戻した**」とあるが、**現物の `data.json` は `maxTokens = 2000`**。事故が起きた設定に戻っている（いつ・なぜ戻ったかは不明）。**Qwen3.6を使うと同じ空回答が再発する可能性がある。**

## 5. LM Studio に入っているモデル（2026-08-01 実画面確認）

**7モデル・合計76.62GB。** 置き場所 `/Users/yoshiakinagumo/.lmstudio/models`。**32GB機なので、大きいものは同時ロードできない。**

| モデル | 規模 | 量子化 | サイズ | 能力 | 位置づけ |
|---|---|---|---|---|---|
| `qwen3.6-35b-a3b-mlx` | 35B MoE | 4bit | 20.4GB | **Vision** ＋ Tool | **現在の本線**（Copilot既定）。`/ynp97` 約3分29秒。**2026-08-08 poke97質問窓3段目で実測: 175秒、A群5種の読み分け（つける／つけ替える／トラッシュする）を正しく分けた、作り話なし → 唯一の合格。3段目の既定モデルに採用**（詳細 [[アプリ引き継ぎ/W poke97 カード研究室]]） |
| `qwen3-vl-30b-a3b-i…` | 30B MoE | 4bit | 18.3GB | **Vision** ＋ Tool | 画像用。OpenClawの画像対応で使う |
| `qwen/qwen3-30b-a3b-2507` | 30B MoE | 4bit | 17.2GB | Tool | **速度優先の予備**。`/ynp97` 約64秒。**2026-08-08 poke97質問窓3段目で実測: 25秒と速いが、「トラッシュする」効果を「持ってくる」と誤読して間違った候補を自信ありげに提示・正解を丸ごと見逃す誤答 → 不採用**（同上） |
| `google/gemma-4-12b-qat` | 12B | Q4_0 | 7.2GB | **Vision** ＋ Reasoning | 導入済み・**画像性能未検証**。**2026-08-08 poke97質問窓3段目で実測: 469秒（既定タイムアウト300秒を超え、初回は無応答で失敗。600秒へ延ばして再実行）。読み分けの方向は合っていたが1件見落とし → 遅すぎて不採用**（同上） |
| `qwen/qwen3-14b` | 14B | 4bit | 8.3GB | Tool ＋ Reasoning | 軽量枠。用途未定 |
| `qwen/qwen3-8b` | 8B | 4bit | 4.6GB | Tool ＋ Reasoning | 最軽量。用途未定 |
| `text-embedding-bge-m3` | 567M (bert) | Q8_0 | 634MB | 埋め込み | **Vault QAの検索担当**。必須 |

- **本線**: `Obsidian → Copilot Vault QA → bge-m3 → LM StudioのQwen`（正本は [[🤖 Obsidian×ローカルAI引き継ぎ（AI共通）]]）
- **起動順**: Qwen3.6（16k）→ bge-m3。逆順だとメモリ保護で失敗することがある。
- **★2026-08-01の発見**: **本線モデルの `qwen3.6-35b` 自体に Vision がついている。** 「画像はGemma 4かQwen3-VLのどちらか」という前提（[[O 自分用日記アプリ]]）は再検討の余地あり。**ただし実際の画像性能は未検証。**
- 14B / 8B は台帳のどこにも用途の記載がない。**何のために入れたか本人に確認が要る。**

## 5-2. 全環境の整理（2026-08-01 着手・**進行中**）

> [!important] 本人の到達目標（2026-08-01 本人の言葉）
> 「トークンの関係で**司令塔を定めることはできない**（優先度は Claude 1、GPT 2）ので、**Obsidianの知識を共有**して、環境の中のAIを**適所で使い**、なるべく**コストカット**、そして**限りなく全自動に寄せて**、目標に達することのできる環境」
>
> 前提として本人が明言していること ── **すべて手探りで構築してきたので、的確でないもの・いらないものが混ざっている。** 失敗が積み上がった今、一度まとめて整理する。

### ㋐ 先に確認しておく3つの制約（設計の前提）

1. **「知識の完全共有」と「コストカット」は原理的に対立する。** 全文を読ませればトークンを食う。だから2026-07-31に索引方式へ移した（95,500字→約4万字）。**目標は「全員が全部を読む」ではなく「全員が同じ入口に着けること」と置き換えたほうが実現できる。** 現に、いま同じ入口を通れていないAIがいる（下記㋑）。
2. **「全自動」の上限は権限。** TCC（画面収録・マイク等）の承認とTouch ID・パスワードは、**どのAIも代行できない**（§1）。全自動に寄せる設計とは、**権限が要る操作を経路から追い出す**設計のこと。
3. **司令塔を置かない代わりに、判断の基準を文書に置くしかない。** 基準がないと、その場にいるAIがその場のノリで環境を選ぶ。**この能力表§2の逆引きが、その基準の第一版。**

### ㋑ 知識の共有範囲 ── **壁は欠陥ではない**

> [!danger] ★最重要 ── 効率を理由にこの壁を越える提案をしない
> **DSにVaultの知識を共有させないのは、本人の意図的な判断。** 正本は [[P OpenClaw + DeepSeek ローカル開発環境]]。
>
> **壁の理由は1つだけ ──「DeepSeekが中国産で、送った内容が中国のサーバーに渡る」こと**（2026-08-01 本人が明言）。
> **暴走リスクは別の手当てで解決済み**なので、壁の理由には数えない（下記の役割分担）。
>
> **15インチAirの位置づけ**: **暴走する前提で設計した使い捨て可能な作業機。** Appleアカウントも分離し、**内部が晒されても構わない状態**にしてある。だからここでは OpenClaw を**制限なしで自由に動かす**。効率・コスト・重複を理由に廃止を検討してはならない。**分けていること自体が目的。**
>
> **本機（M5）のDS「zaza」の位置づけ**: 逆に**制限をきつくかけてある**（`tools.exec.ask=always` ＝ シェルは毎回いちいち承認）。**そのため本人は「あまり使いたいと思えない」と明言（2026-08-01）。実作業の当てにしない。**
>
> **共有してよいのは `🤝 AI連携用（DeepSeek共有可）` に置いたものだけ。**
> **基準（本人の言葉）＝「他人・鍵・金・内面は渡さない／作るもの・技術は渡してよい」。**
> 実データ（出席・住所録・日記）は `~/openclaw-apps` に置かない。壁の本体は「**DeepSeek側がVaultに触れない**」こと。

> [!note] この壁の由来（2026-08-01 本人談・**将来の見直しのために残す**）
> **本人はもともと「大丈夫ではないか」と考えていた。** AI側が「考慮したほうがよい」と推した結果、現在の形になった。つまりこれは**本人の強い確信ではなく、安全側に倒した運用**であり、本人の言葉では「**一応、安全策をとっている**」。
> **だからといってAIが勝手に緩めてよいものではない。** 見直すなら、DeepSeekのAPI利用規約・学習利用の有無という事実確認から始める話であって、効率を理由に崩す話ではない。

| AI | Vaultの知識に届く経路 | 評価 |
|---|---|---|
| Cowork Claude | `CLAUDE.md` 自動注入＋索引 | ○ |
| Codex | `AGENTS.md` 自動注入＋索引 | ○（ただし二重管理） |
| Claude Code（未導入） | `CLAUDE.md` が効く | 未検証 |
| **DS（本機・Air）** | `🤝 AI連携用（DeepSeek共有可）` ＋ `ds-apps/AGENTS.md` ＋ 作業票 | **これで正しい。壁は意図的。埋めない** |
| **LM Studio（Copilot経由）** | Vault QA のRAG断片 | **△ ここは埋めてよい。** ローカル完結でデータは外に出ない |
| Obsidian Copilot のオンラインAI | 同上 | **△ 埋める前に、何を外へ出すかの判断が要る**（Gemini/GPT/Claudeは外部送信） |

**つまり「知識共有を広げてよい相手」と「広げてはいけない相手」がある。** 前者＝ローカルAI（データがMacから出ない）。後者＝DeepSeek系（中国サーバー）。オンラインAI（Gemini/GPT/Claude）は中間で、**何を渡すかを都度決める**。

### ㋒ 「いらないかもしれない」候補（**判断は本人。ここは材料**）

| 対象 | 現状 | 論点 |
|---|---|---|
| **`CLAUDE.md` と `AGENTS.md` の二重管理** | ほぼ同内容の2ファイル | 2026-07-31に**片方だけ古い事故が実際に発生**。原則10で「両方同時反映」と決めたが、根本は分かれていること。**片方を薄い参照だけにできないか** |
| ~~**15インチMacBook Air**~~ | ~~DS実装専用機~~ | > [!danger] **2026-08-01 撤回。この項目を候補に挙げたのはClaudeの誤り。**<br>Airは**OpenClawの暴走リスクとDeepSeekのデータ管理リスクを隔離するための壁**であり、コスト効率で判断するものではない（㋑参照）。**Claudeが `アプリ引き継ぎ/P` を読まずに「重複では」と提案した。効率を理由に安全境界を壊しかけた実例として残す。** |
| **LM Studio の qwen3-14b / qwen3-8b** | 合計12.9GB | 台帳に用途の記載なし。**何のために入れたか不明** |
| **Copilot の使えないモデル登録** | `copilot-plus-*` がONだが `isPlusUser = false` | 使えないものが一覧に並ぶと選択を誤る |
| **`Obsidian_ChatGPT_Context_Vault/`** | Vault QAの索引から除外済み | 旧世代の資産。参照されているか要確認 |
| **アプリ引き継ぎ21本** | A〜U | 全部が現役ではない。**状態（現役／保守／休止）が未設定**のまま（[[🧩 アプリ開発状況（AI共通）]] に明記あり） |

### ㋓ 整理の順序（案・2026-08-01改訂）

1. **現状の可視化**（この能力表。**ほぼ完了**）
2. **㋒の各項目を本人が「残す／やめる」で仕分ける** ← 次はここ
3. ~~DSを索引に接続する~~ → **却下（㋑のとおり、これは穴ではなく壁）。** 代わりに **ローカルAI（LM Studio）側の索引到達を厚くする** ── データがMacから出ないので、ここは広げてよい。
4. **自動化できる範囲を確定する**（権限が要らない作業だけを洗い出す）
5. **判断基準を §2 の逆引き表に集約し、全AIの入口から引かせる**

> [!warning] この整理を進めるAIへ
> **効率・コストカット・重複排除を目的にすると、安全のために意図的に分けてあるものを「無駄」と誤認する。** 2026-08-01にClaudeが実際にやった（15インチAirの廃止を候補に挙げた）。
> **何かを「いらない」と言う前に、それが「なぜあるのか」の記録を探す。** 見つからなければ、無いと決めつけず本人に聞く。

## 6. 未確認・要検証

| 項目 | 状態 |
|---|---|
| Claude Code の実地能力（本機でのSwiftビルド、CLAUDE.mdの効き方、Coworkとの併用可否） | **未検証。導入すれば1回で分かる** |
| ~~本機のDS（zaza）で `swift build` が通るか~~ | **2026-08-01 取り下げ。** 本機のDSは `exec.ask=always` で毎回承認が要り、本人が「あまり使いたいと思えない」と明言。**Claudeが「今日いちばん効いたはずの経路」と書いたのは誤り**（制限の実態を知らずに提案した） |
| Codex が `swift build` で止まる問題は現在も再現するか | 2026-07-25の記録以降**未確認** |
| Cowork Claude から Air への画面共有経由の操作が実用になるか | 経路は確立済み（9.5.3）だが**Claudeが使った実績なし** |
| `qwen3.6-35b` の Vision 実性能（Gemma 4 12B / Qwen3-VL との比較） | **未検証。** 日記アプリの画像方針に影響する |
| LM Studio の `qwen3-14b` / `qwen3-8b` の用途 | **台帳に記載なし。本人に確認** |
| LM Studio 本体に「Update available」表示あり（2026-08-01） | 未更新。更新するかは本人判断 |
| ~~Copilotに登録したオンラインAIのAPIキーが実際に入っているか~~ | **2026-08-01 Geminiは確認済み（応答あり）。保険は成立。** GPT/Claude/OpenRouter側は未確認だが、**Copilot経由では定額枠でなくAPI従量課金になるため目的に合わず、確認する必要も薄い** |
| **`maxTokens = 2000` の食い違い**（台帳は6000） | **要修正の可能性。** Qwen3.6で空回答が再発する条件 |
| Copilot の自律エージェント（`writeFile`/`editFile`）を実際に使うか | **一度も使っていない**（本人談）。Vaultを書き換える権限があるので、使うなら壊れたときの戻し方を先に決める |
