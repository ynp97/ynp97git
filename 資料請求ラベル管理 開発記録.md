---
種別: アプリ開発記録
対象: 資料請求ラベル管理システム
作成日: 2026-06-27
実体: ~/Desktop/AI関係/ラベル管理
Dock: /Applications/ラベル管理.app
---

# 資料請求ラベル管理 開発記録

## 2026-06-27 起動不良再修正（Swiftランチャー化）

### 症状

- Dockの「ラベル管理」を開いても起動しない。
- `curl -I http://127.0.0.1:8502` が接続不能、またはStreamlitの外枠だけ返る。
- `/tmp/label_app.log` に `PermissionError: [Errno 1] Operation not permitted: '/Users/yoshiakinagumo/Desktop/AI関係/ラベル管理/app.py'` が出ることがあった。
- 一時的に `~/Library/Application Support/ラベル管理` 側を見に行く構成になり、履歴DBが二重化しかけた。

### 原因

- 旧AppleScript / シェルランチャーでは、最終的にPython単体が `Desktop/AI関係/ラベル管理/app.py` を読みに行き、macOSのDesktopアクセス制限に当たった。
- さらに途中でApplication Support案を試したため、実行用データの正が一時的にぶれた。

### 修正

- 実運用の正を `~/Desktop/AI関係/ラベル管理` に戻して一本化。
- `/Applications/ラベル管理.app` の実行ファイルを `LabelManagerLauncher` へ変更。
- `LabelManagerLauncher.swift` を作成し、Dockアプリ自身が `~/Desktop/AI関係/ラベル管理/app.py` を確認してから、Python/Streamlitを起動する形にした。
- Streamlitは `--server.address 127.0.0.1` でローカル専用、`--server.fileWatcherType none` でファイル監視を止める。
- `~/Library/Application Support/ラベル管理` は削除せず、`~/Library/Application Support/ラベル管理_使わない退避_20260627` にリネーム退避。

### 確認

- `curl -I http://127.0.0.1:8502` が `HTTP/1.1 200 OK` を返すことを確認。
- 起動中プロセスが `/Users/yoshiakinagumo/Desktop/AI関係/ラベル管理/app.py` を読んでいることを確認。
- 親プロセスが `/Applications/ラベル管理.app/Contents/MacOS/LabelManagerLauncher` になっていることを確認。
- Chromeのアクティブタブが `http://127.0.0.1:8502/`、タイトルが「資料請求ラベル管理」になっていることを確認。
- `/tmp/label_app.log` が正常なStreamlit起動表示のみになったことを確認。

## 2026-06-27 起動不良修正

### 症状

- Dockの「ラベル管理」を開いても起動しない。
- `http://localhost:8502` は一時的に返ることがあったが、Streamlitが `app.py` を読めず、画面が実質起動していなかった。

### 原因

- Dockアプリ内の古いAppleScriptが `~/Desktop/ラベル管理/app.py` を見ていた。
- `~/Desktop/ラベル管理` は `AI関係/ラベル管理` へのリンクだったが、ユーザー方針としてデスクトップ直下に置きたくない。
- macOSのDesktopアクセス権限により、Dockアプリから `~/Desktop/AI関係/ラベル管理/app.py` を読む時に `PermissionError: Operation not permitted` が出ていた。

### 修正

- デスクトップ直下の `~/Desktop/ラベル管理` リンクを削除。
- 実運用の正を `~/Desktop/AI関係/ラベル管理` として確認。
- `/Applications/ラベル管理.app` の実行ファイルを旧AppleScript appletから `launch` シェルランチャーへ変更。
- `launch` は `~/Desktop/AI関係/ラベル管理/app.py` を直接起動する。
- Streamlitは外部公開しないよう、`--server.address 127.0.0.1` を指定。
- `--server.fileWatcherType none` を指定し、Desktop配下の監視エラーを避ける。
- `NSDesktopFolderUsageDescription` をInfo.plistへ追加し、macOSがDesktopアクセス許可を扱えるようにした。
- 先に作成した `~/Library/Application Support/ラベル管理` のコピーは、方針と違うため後で `~/Library/Application Support/ラベル管理_使わない退避_20260627` に退避。

### 確認

- `curl -I http://127.0.0.1:8502` が `HTTP/1.1 200 OK` を返すことを確認。
- 起動中のPythonプロセスが `/Users/yoshiakinagumo/Desktop/AI関係/ラベル管理/app.py` を読んでいることを確認。
- デスクトップ直下に `ラベル管理` リンクが残っていないことを確認。
- `~/Desktop/AI関係/ラベル管理` に `app.py`, `history.sqlite3`, `backup/`, `launch`, `README.md` があることを確認。

### 今後の方針

- 今後のアプリ関係の実体は、原則 `~/Desktop/AI関係/` の中に展開する。
- デスクトップ直下にアプリ用フォルダやリンクを増やさない。
- Vaultの `アプリ本体/ラベル管理` はコードバックアップ。個人情報を含むsqliteはVault管理しない。

---

## 2026-06-27（追記2）不具合「起動後に強制終了が必要」修正

### 症状
- Dockの「ラベル管理」を起動すると、ブラウザは開くが、アプリ自体が「応答なし」になり、終了に**強制終了が必要**になっていた。

### 原因
- Swiftランチャー `LabelManagerLauncher.swift` が、main の末尾で `streamlit.waitUntilExit()` を呼び、`NSApplication` のイベントループ（`NSApp.run()`）を一切回さずにメインスレッドを永久ブロックしていた。
- そのためDockアプリがイベントを処理できず、macOSから「応答なし」と見なされ、普通に終了できなかった（＝強制終了が必要）。
- ブラウザが開いていたのは、URLオープンを `DispatchQueue.global().asyncAfter` の別スレッドで実行していたため。サーバー(Streamlit子プロセス)自体は動いていた。

### 修正
- `LabelManagerLauncher.swift` を、正式に `NSApplication` の run loop を回す方式へ書き換え。
  - `AppDelegate` を作り、`applicationDidFinishLaunching` でサーバー起動→ポート待ち→ブラウザを開く。
  - `app.setActivationPolicy(.regular)` ＋ `app.run()` でイベントループを回す → アプリは常に応答。**Cmd+Q / Dock右クリック→終了で普通に閉じられる（強制終了不要）**。
  - `applicationWillTerminate` で、起動したStreamlit子プロセスを `terminate()` して片付ける。
  - Streamlitが自分で落ちた場合は `terminationHandler` でランチャーも終了。
  - すでに起動中(ポート8502が使用中)なら、ブラウザを開くだけで二重起動しない。
- TCC(デスクトップ権限)の帰属を保つため、Streamlitは引き続き**このアプリの子プロセス**として起動する（権限の出戻り回避）。
- 再ビルド用に `ラベル管理_再ビルド.command`（ダブルクリック実行）を追加。Vaultのswiftを `swiftc -O` でビルドし、`/Applications/ラベル管理.app/Contents/MacOS/` のバイナリを差し替えてアドホック署名する。実体側 `~/Desktop/AI関係/ラベル管理/LabelManagerLauncher.swift` も同内容に更新。

### 反映手順（ユーザーが実行）
1. Finderで `アプリ本体/ラベル管理/ラベル管理_再ビルド.command` をダブルクリック（警告が出たら右クリック→開く）。
2. 「完了しました」が出たらDockの「ラベル管理」を起動。
3. 確認: ブラウザが `http://127.0.0.1:8502` で開く／Cmd+Qで強制終了なしに閉じられる。

### 注意
- swiftc（Xcode Command Line Tools）が必要。無ければ `xcode-select --install`。
- Cmd+Qでランチャーを終了すると、起動中のStreamlitサーバーも一緒に停止する仕様（＝アプリを閉じる＝サーバーも閉じる）。

---

## 2026-06-27（追記3）クリーン再構築：根本解決

### 追記2の修正だけでは不十分だった
- run loopとメニューを足したが、それでもCmd+Q/終了が効かない・もぐら叩き状態になった。
- 原因: このDockアプリは「Cocoa-AppleScript Applet」テンプレート由来で、**CFBundleIdentifierが `com.apple.ScriptEditor.id.cocoa-applet-template` と汎用共有**、実行ファイル名も不定。そのため
  - `killall/pkill -x applet` で殺せない（プロセス名が一致しない）。
  - `open_application`/終了が、同IDの別アプレットに化けて、狙ったインスタンスを閉じられない。
  - 3時間分＋テストで複数インスタンスが残留。

### 根本解決＝独自バンドルで作り直し
- `アプリ本体/ラベル管理/ラベル管理_クリーン再構築.command` を作成。これ1本で:
  1. 旧インスタンスを網羅的に停止（id指定quit / `pkill -9 -f "/Applications/ラベル管理.app"` / streamlit）。
  2. `/Applications/ラベル管理.app` を削除し、**クリーンなappバンドルを新規作成**。
     - `CFBundleIdentifier=com.yoshiakinagumo.labelmanager`（独自・衝突なし）
     - `CFBundleExecutable=labelmgr`（固定名。今後は `killall labelmgr` で確実に止められる）
     - `NSDesktopFolderUsageDescription` 付き、アイコンはAppIcon.icns。
  3. `LabelManagerLauncher.swift`（run loop + 終了メニュー入り）を `swiftc -O` でビルドし `Contents/MacOS/labelmgr` に。
  4. アドホック署名 → `lsregister -f` → `open`。
- 2026-06-27 実機確認: 再構築後、Desktopアクセス許可ダイアログで許可 → アプリ起動成功。ブラウザで「資料請求ラベル管理システム」表示、`history_backup_20260627_114828.sqlite3` 自動バックアップ作成、接続エラーなし。
- バンドルID変更に伴い、初回のみDesktopアクセスのTCC許可が再度必要（許可済み）。

### 今後ラベル管理を触るとき
- 起動アプリの正体: `/Applications/ラベル管理.app`、実行ファイル `labelmgr`、バンドルID `com.yoshiakinagumo.labelmanager`。
- 止めたいとき: `killall labelmgr`（または `pkill -9 -f "/Applications/ラベル管理.app"`）。streamlitは `pkill -f "streamlit run"`。
- 再ビルド: ソース `アプリ本体/ラベル管理/LabelManagerLauncher.swift` を直して `ラベル管理_クリーン再構築.command` を実行。
- データ実体は従来どおり `~/Desktop/AI関係/ラベル管理`（sqlite等の個人情報はVaultへ入れない）。

---

## 2026-06-27（追記4）単体HTML版へ全面移行（現行・正）

### 経緯
旧Streamlit＋AppleScriptアプレット構成は、昨日まで使えていたのに起動不能・終了不能（要強制終了）になり、5時間かけても安定して直せなかった。原因はサーバー常駐＋アプレットランチャー＋Desktop権限という壊れやすい組み合わせ。**本人判断で旧構成は廃止・削除**し、単体HTMLで作り直した。

### 現行の正（これだけ見ればよい）
- 本体: `アプリ本体/ラベル管理/資料請求ラベル管理.html`（単体HTML、サーバー無し）。localStorageキー `label_app_v2`。
- 起動: Dockの**オレンジ地に白い太字ラ**アイコン `/Applications/ラベル管理.app`。中身は一発起動シェル（Chromeを `--app` で独自ウインドウ起動して即終了＝常駐しないので固まらない）。バンドルID `com.yoshiakinagumo.labelmgr.launcher`、実行ファイル `run`。
- アイコン: `アプリ本体/ラベル管理/AppIcon.icns`（Pillowで生成。プレビュー `AppIcon_preview.png`）。
- セットアップ/再構築スクリプト: `アプリ本体/ラベル管理/セットアップ_古い削除とDock追加.command`（旧停止削除→.app作成→Dock追加→起動を一括）。代替起動: `ラベル管理_ウインドウで開く.command`。
- コンセプト: `アプリ本体/ラベル管理/ラベル管理_新規コンセプト.md`。

### 実装済み機能
- ①取り込み: Excel(SheetJS/要ネット) と、Wordの雑テキスト貼り付け。**KG資料請求メール専用パーサ**（■見出しの次行が値・1通=1人・"資料送付先"の氏名/カナ/郵便/都道府県+市区町村+町名+番地を採用）。取り込み後は黄色セルで直接手直し可。
- ②抽出・選択: 条件＝**駅まで移動時間の上限20分**/電車上限/合計上限/手段(徒歩4.8・自転車13km/h)/道路補正/低精度の扱い。判定（一次候補/要確認/除外）。表示フィルタ（既定=全部）＋並び替え（合計/駅まで/電車）。チェックで送付対象選択。
- ③印刷: 「印刷待ちへ」で**自動的に③へ移動**し、印刷待ち一覧＋プレビュー表示。UNIPACK A4 新24面(66×33.9mm,3×8)をCSS実寸で組み window.print()。開始位置/文字サイズ/敬称/上下左右mm微調整。
- 京成駅・辞書: 京成本線 京成船橋〜京成成田 全19駅の電車時間表（八千代台基準・概算）＋住所→最寄り駅 対応表。どちらも編集・JSON入出力。
- 履歴・バックアップ: 送付履歴、全データJSON書き出し/復元。

### 検証（私が手元で実施）
- KGメール実データ2件 → Nodeで解析正常（氏名・カナ・郵便・住所に分離）。
- jsdomでアプリJSをそのまま実行し通し確認: 取り込み2件 → 判定(一次1/要確認1/除外0) → チェック → 「印刷待ちへ」で③自動移動 → 一覧・プレビューにラベル生成（例「〒276-0023 千葉県八千代市勝田台5ー37ー36 石井叶太 様」）。チェックのオン/オフも反映を確認。
- ②でチェックできない問題 → 既定フィルタが「一次候補だけ」で要確認/除外が隠れていたのが原因。既定を「全部」に変更＋チェックボックスCSS調整で解消。

### 未確認・今後（本人テスト/育成）
- サンプルでの実ブラウザ通し操作は追記5で確認済み。本人の実データでの取り込み→候補チェック→印刷位置調整は要確認。
- 京成各駅の電車時間、住所→駅の対応（km・精度）は**私の概算**。実値に合わせ辞書タブで要調整（例: 「井野→京成佐倉」を町名で足すと相田さんが一次候補化）。
- 旧個人データ `~/Desktop/AI関係/ラベル管理` の sqlite等は**削除していない**（個人情報のため本人判断）。

---

## 2026-06-27（追記5）取り込み後の導線改善

### 症状
- ユーザー確認で「取り込んだ後の挙動が分かりづらい」と判明。
- 旧UIでは、貼り付け取り込み後にアラートが出て①の表に残るだけで、次に②で判定するのか、手直しするのか、印刷へ進むのかが画面上に残らなかった。

### 修正
- 上部に `flowBanner` を追加し、現在地と次の操作を常時表示。
- 取り込み成功時は、現在の条件で自動判定して②へ移動するよう変更。
- 自動判定後に、一次候補・要確認・除外の件数と「一次候補は送付対象にチェック済み」を表示。
- ①へ戻って黄色セルを手直しした場合は、古い判定を破棄し、「②で判定する」導線を表示。
- アプリ再読み込み時も、未判定・判定済み・印刷待ちありの状態を上部ガイドで知らせる。
- ②で「印刷待ちへ」を押した後は③へ移動し、一覧とプレビューを見るよう明示。

### 検証
- JS構文チェック: `JS parse OK`。
- Codex内ブラウザでローカルHTTP配信し、サンプル2件で確認。
  - 貼り付け→取り込み後、自動で②へ移動。
  - 上部ガイドに「2件取り込み」「一次候補1件」「要確認1件」「除外0件」を表示。
  - ①へ戻って住所セルを手直し→再判定で変更後の住所が②へ反映。
  - 上部ガイドの「印刷待ちへ」から③へ移動し、キュー件数1、一覧、プレビュー表示を確認。
  - ブラウザconsoleのerror/warningは空。

---

## 2026-06-30（追記6）取り込みリストのリフレッシュボタン追加

### 症状
- 前に取り込んだデータが残り続けているように見え、消す操作が「履歴・バックアップ」タブ内にあって見つけにくかった。

### 修正
- 現行の安定挙動は変えず、ヘッダーに `取り込みリストを空にする`、①取り込み画面に `取り込みリストをリフレッシュ` ボタンを追加。
- ボタンは、取り込み中の名簿と判定を空にして①へ戻すだけにした。
- 消える範囲は従来どおり、取り込み中の名簿と判定のみ。履歴・辞書・印刷待ちキューは残す。

### 検証
- HTML内のJavaScriptを抜き出して構文チェックし、`JS parse OK` を確認。

## 2026-07-06（追記7）二重発送防止台帳と保存分岐対策

### 症状
- 毎回のExcelに前回までと同じ人が繰り返し入っており、同じ人へ複数回発送してしまう。
- 開き方によってウインドウが何個も開き、どこに何を記録したのか分からなくなる。

### 原因
- 送付履歴が「日時・件数・先頭3名」だけで、個人単位の送付記録が無く、重複を検出できなかった。
- ランチャーがChromeのプロファイルを固定しておらず、プロファイルごとにlocalStorageが分かれていた（三本立てTODOと同じ構図）。

### 修正1: 二重発送防止（4段ガード）
- 同一人物判定: 氏名＋郵便番号、または氏名＋住所先頭14字。全半角・空白・ハイフン類を正規化してから比較。
- `state.sent`（送付済み台帳）と `state.seen`（取り込み済み記録・バッチ番号つき）を追加。
- ① 取り込み時、リスト内の同じ人をまとめる（件数を通知）。
- ② 判定時、送付済みは「送付済み」（赤ピル・チェック不可）、過去バッチで取り込み済みの未送付者は「取り込み済み」（既定チェック外・チェックすれば送れる）として自動除外。②の集計に「送付済み」「前回取り込み済み」を追加。
- ③ 印刷待ちへ追加時、送付済みとキュー内重複をスキップして通知。
- ④ 印刷直前、キューに送付済みが混ざっていればconfirmで警告（「送付済み」押し忘れ→再印刷の事故対策）。
- ③の「送付済みにして履歴へ記録」で、キュー全員を個人単位で台帳へ登録。
- 履歴タブに台帳UI: 検索・個別削除（削除で候補に復帰）・過去に送った人の貼り付け手動登録・取り込み済み記録の全消去。
- 注意: この機能を入れる前に発送した人は台帳に居ないため、手動登録で埋めるまでは検出できない。

### 修正2: 保存分岐・多重ウインドウ対策
- ランチャー2種（セットアップcommand内run、ラベル管理_ウインドウで開く.command）を `--profile-directory=Default` ＋ `?app=1` に固定。
- HTMLに直開き注意バナー（`?app=1`なし）、多重ウインドウ検知（localStorageハートビート・赤警告）、ウインドウ復帰時に他ウインドウの新しい保存を読み直す処理を追加。
- Dockの.appへの反映は「セットアップ_古い削除とDock追加.command」の再実行が必要。実行後にデータが空に見えたら、旧データが別プロファイルにある（JSONバックアップで救出可能）。

### 検証
- HTML内JSを抜き出しnode構文チェックOK。
- 関数単体テスト: 表記ゆれ（全角数字・空白・ハイフン）でも同一人物と判定 / リスト内重複がまとまる / 郵便番号なしは住所キーで一致 / 同住所の別名は別人のまま、を確認。

## 2026-07-06（追記8）印刷の空白2枚目を修正・過去送付分の扱い確定

### 症状
- 印刷すると2枚出てくる（2枚目は空白）。

### 原因と修正
- 旧印刷CSSは `body * {visibility:hidden}` 方式で、画面本体のレイアウトが紙面上に残っていた。画面が縦に長くなった（台帳カード追加等）ことで空白の2ページ目が顕在化。
- `@media print` で `header,main` を `display:none` に変更し、`.a4` に `page-break-after:always`（最終ページは除く）を追加。

### 過去送付分の扱い（本人判断）
- 2026-07-02以前の送付分は個人記録が無いため、手動登録せずスルーで確定。
- 二重発送防止台帳が効くのは2026-07-06以降。**印刷後に③「送付済みにして履歴へ記録」を押した分だけが記録される。**

## 2026-07-28（追記9）Dockを押しても見えない／背後で多重起動

### 症状と確認
- Dockのオレンジ「ラ」を押しても、画面が立ち上がらないように見えた。
- 実際にはChrome内で同名の「資料請求ラベル管理」が複数開き、古い画面の後ろに新しい画面が増えていた。
- アプリ内の「別のウインドウでも開いています」警告も15秒以上継続し、古いheartbeatだけでなく実際の重複起動があることを確認した。

### 原因
- 現行ランチャーはChromeを `--app=...` で毎回新規起動する一発ランチャーで、すでに同じ画面が開いているかを確認しない。
- そのため、既存画面が背後にある状態でDockを何度か押すと、起動していないように見えながら同じ画面が増える。

### 今回の復旧
- 重複画面を閉じ、いったんラベル画面をすべて終了。
- Dockアプリから1枚だけ開き直し、15秒の古いheartbeat失効後に多重ウインドウ警告が消えることを実画面で確認。
- 保存済みデータ、HTML本体、旧個人データは削除・変更していない。

### 採用しなかった方法
- HTML側で新しい重複画面を検知し、先に開いている画面を手前へ出して `window.close()` する方法を試した。
- Chromeの制限で新しいapp-modeウインドウを確実に閉じられず、警告だけ消えて重複が残るため不採用。HTMLは試行前の状態へ戻した。

### 現時点の扱い
- 現在は1枚だけ正常起動している。
- 恒久対応には、ランチャー側で既存ウインドウを検出して手前へ出す方式への変更が必要。Chrome操作権限や別ランチャー方式を伴うため、未実装。
