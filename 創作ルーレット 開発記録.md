---
種別: アプリ開発記録
対象: 創作ルーレット
作成日: 2026-06-27
本体: アプリ/創作ルーレット.html
localStorage: music_production_roulette_v1
---

# 創作ルーレット / 音楽制作ルーレット 開発記録

## 2026-06-29 v0.4 タスクルーレットを音楽ドック下へ配置

### やったこと

- ユーザー提示の「タスクルーレット」HTMLを元に、単体版 `アプリ/タスクルーレット.html` を保存。
- `🗓 三本立て毎日TODO（アプリ）.html` の右側をサイドドック化し、上に「音楽制作 今日」、下に「タスクルーレット」を配置。
- タスクルーレットは単体版と同じ `task_roulette_v1` を使うため、単体版と三本立てTODO内の版で候補を共有する。
- 三本立てTODO内のタスクルーレットで、1行1タスクの貼り付け追加、`.txt/.md/.csv` 読み込み、重複除外、ランダム抽選、選ばれた候補の「完了して外す」を実装。
- 三本立てTODOのバックアップJSONに `taskRouletteState` を追加。
- AI用Markdownに `## タスクルーレット` を追加し、候補数・現在の結果・候補一覧を出すようにした。

### 確認

- `node` で `🗓 三本立て毎日TODO（アプリ）.html` 内のJavaScript構文チェックを実行し、`script ok` を確認。
- `node` で `アプリ/タスクルーレット.html` 内のJavaScript構文チェックを実行し、`standalone script ok` を確認。

### 次に見ること

- ブラウザで三本立てTODOを開き、音楽制作ドックの下にタスクルーレットが出ることを目視確認する。
- タスクを数件追加し、抽選→完了して外す→リロード後も保存されるか確認する。
- 右ドックが縦に長すぎる場合は、候補リスト高さや入力欄をさらに圧縮する。

## 2026-06-29 v0.3 三本立てTODO横ドック統合

### やったこと

- `🗓 三本立て毎日TODO（アプリ）.html` に、音楽制作ルーレットの右ドックを追加。
- 三本立てTODO本体は完全保持し、既存のタブ・チェック・追加TODO・AI用出力・バックアップはそのまま使う。
- 右ドックには「音楽制作 今日」だけを表示。
  - 1本目: 賛美固定。
  - 2本目: ルーレット。
  - 各ユニットは大きめのチェックボタンで完了/未完了を切り替えられる。
- ルーレット候補、進行中ライン、抽選対象ON/OFF、工程の戻し/進めは `詳しい進行を開く` の折りたたみ内へ隠した。
- 三本立てTODOのAI用Markdownに `## 音楽制作 今日` を追加。
- 三本立てTODOのバックアップJSONに `musicState` も含めるようにした。

### 確認

- `node` でHTML内のJavaScript構文チェックを実行し、`script ok` を確認。

### 次に見ること

- 三本立てTODOをブラウザで開き、右ドックが邪魔でなく、今日やる音楽だけが見えるか確認する。
- `賛美＋ルーレット` を押し、音楽ユニットのチェック変更が独立アプリ側の `music_production_roulette_v1` と整合するか確認する。

## 2026-06-29 v0.2 音楽制作ルーレット化

### 現在の用途

- `アプリ/創作ルーレット.html` は、画面上は「音楽制作ルーレット」。
- `app97` / `APP97` でアプリ開発を再開するAIは、まず [[🧩 アプリ開発状況（AI共通）]] の「音楽制作ルーレット」節と、この記録を読む。
- 1日の基本は2ユニット:
  - 1本目: `賛美` 固定。毎日行う。
  - 2本目: ルーレットで決める。

### 制作ライン

- `賛美`
  - 毎日固定。
  - 歌もの21工程を順番に進める。
  - 旧 `歌もの` / `vocalSong` から `賛美` / `praise` へ移行。
- `エレクトロ`
  - 10工程。順番進行。
- `作りかけエレクトロ`
  - 8工程。順番進行。
- `ガチ音響`
  - 4工程。順番進行。
- `古い音源サルベージ`
  - 5工程。
  - ここだけ順番ではなく、1〜5を毎回ランダム抽選する。

### 2026-06-29時点の実装

- 画面上部を「今日やること」に寄せ、1本目/2本目の概要を先に表示。
- 「賛美＋ルーレットを作る」ボタンで、1本目に賛美、2本目にルーレット候補を作る。
- 「2本目を振り直す」は、賛美を残して未完了のルーレット枠だけ入れ替える。
- 「抽選を1つ追加」は、賛美ではなくルーレット枠を追加する。
- ルーレットの凡例から賛美を外し、2本目候補だけを表示。
- localStorage keyを `music_production_roulette_v1` に変更。
- 旧 `vocalSong` の進捗や今日ユニットが保存済みの場合、`praise` へ移行する処理あり。

### 確認

- `node` でHTML内のJavaScript構文チェックを実行し、`script ok` を確認。

### 現在の注意

- 保存は `localStorage`。ブラウザや開き方が変わると保存領域が分かれるので、定期的にJSON保存する。
- 完了取り消し時、各ラインの進捗数字は完全自動ロールバックしない。必要なら該当ラインの「一つ戻す」を使う。
- このアプリは現状、ファイル名は `創作ルーレット.html` のまま、画面名と運用名が「音楽制作ルーレット」。

### 次に見ること

- ブラウザでリロードし、「賛美＋ルーレットを作る」を押して、今日の2本が直感的にわかるか確認する。
- 実際に数日使い、2本目候補の抽選バランスと、賛美固定が続けやすいか確認する。
- 必要なら、今日やること部分をさらに大きくし、下の進行中ラインより優先して見えるUIにする。

## 2026-06-27 v0.1

### 作ったもの

- `アプリ/創作ルーレット.html` を新規作成。
- 仕様元は [[🎛 創作オペレーティングシステム（4軸・30分ユニット）]]。
- 賛美 / ファイン・アート・ソング / エレクトロ / ガチ音響 / 作りかけエレクトロを、30分ユニットで回す単体HTMLアプリ。

### 実装内容

- 今日の標準2ユニット抽選。
- 軸はランダム、各軸の工程は上から順番に進む。
- 同じ軸が1日に2回出た場合、工程番号が連続するように仮進行を計算。
- 未完了ユニットの振り直し、1ユニット追加。
- 各軸の曲名、現在作品番号、工程番号、完成数、抽選対象ON/OFF。
- 30分タイマー。
- マイク入力まわりの一回限りセットアップタスク。
- 完了履歴。
- AI用Markdownコピー、Markdown保存、JSONバックアップ/復元。
- ブラウザ内保存。

### 確認

- `node` でHTML内のJavaScript構文チェックを実行し、通過。
- 一時ローカルサーバーで表示確認。
- ブラウザ上で「今日の2ユニットを回す」を押し、2件生成されることを確認。
- 1件を完了し、今日の完了数と完了履歴が更新されることを確認。
- ブラウザのエラーログにアプリ由来のエラーがないことを確認。

### 現在の注意

- 完了取り消し時、各軸の進捗数字は完全自動ロールバックしない。必要なら該当軸の「一つ戻す」を使う。
- 保存は `localStorage`。ブラウザや開き方が変わると保存領域が分かれるので、定期的にJSON保存する。
- `file://` 直開きで使える想定。ただし確認時はブラウザ安全ポリシーにより、一時ローカルサーバーで表示した。

### 次に見ること

- 3日ほど実際に使い、抽選の出方が「4方向を全部満たす」感覚に合うか確認する。
- マイク入力セットアップを最初の1ユニットとして実行する。
- 三本立てTODOや相談ログへ貼りやすい出力が必要なら、AI用Markdownの形式を調整する。
