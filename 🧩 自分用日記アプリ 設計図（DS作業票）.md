---
種別: 設計図 / DS作業票（実装前）
作成日: 2026-07-30
更新日: 2026-07-30
役割: [[自分用日記アプリ 開発記録]]の続き。新規エントリの入力（キャプチャ）・編集・タグ・週次バッチを設計する。実装をDS（OpenClaw/DeepSeek）へ渡す作業票の元になる。
---

# 🧩 自分用日記アプリ 設計図（DS作業票）

## 0. この設計図の使い方（実装者へ）

- **仕様に書いていないことを憶測で埋めない。** 判断が要る箇所は「§9 未確定・要確認」に集めてある。そこは実装前に本人へ確認する。過去に「業務の区分をAIが憶測で埋めて実装に写った」失敗が起きている（学校台帳アプリ 2026-07-28）。
- **完了条件は「コードが書けた」ではなく「`.app`として起動し、実際にボタンを押して動いた」**。ビルドして起動し、貼り付け→保存→Vaultのファイルが正しく書き変わったことを実機で確認するまでが1タスク。
- **既存の実データ（1,113件・写真779枚）を壊さないことが最優先。** 書き込み系の実装では、先に§7のバックアップ・安全弁を入れる。

---

## 1. 位置づけ

既存プロジェクト「自分用日記アプリ」（[[自分用日記アプリ 開発記録]]、[[🧩 アプリ開発状況（AI共通）]] O節）の続き。**新しい別アプリとして作らず、一本化する**（2026-07-30 本人確定）。

- 2026-07-18に、Day Oneの全記録 **1,113件・写真779枚** をVaultの `ジャーナル/` へ移行完了済み。期間は2013-07-09〜2026-07-17。
- 既存計画の閲覧機能（タイムライン・全文検索・写真表示・日記別表示・「この日の記憶」）はそのまま引き継ぐ。
- 今回新たに設計するのは、**入力（キャプチャ）・編集・タグ・週次バッチ**。

### 全体像（三段構え）

| 段 | 何をするか | いつ動くか | AIは使うか |
| --- | --- | --- | --- |
| ① キャプチャ | 雑に書いた文・写真・動画をVaultへ放り込む | 書いた時（都度） | **使わない** |
| ② 週次バッチ | カテゴリタグ付け＋レシート読み取り | 週1回（自動 or 手動起動） | 使う（裏側で完結） |
| ③ 分析 | 着想の追跡、お金の流れ、感情の推移など | 本人が聞いた時だけ | 使う（AIセッションで対話） |

**設計の背骨＝「雑に書き、雑に写メし、放り込む」を絶対に壊さない。** 書く時点で分類・整形・要約・タグ付けを一切要求しない。整えるのは後（②③）で、しかも本人の手を止めない形で行う。

---

## 2. 既存データ形式（実装前に必ず実物を確認すること）

新規エントリは**既存の形式に完全に合わせる**。以下は2026-07-30時点で `ジャーナル/2026.md` を実際に読んで確認した形式。

```markdown
---
title: ジャーナル 2026
type: journal
year: 2026
entries: 32
---

# ジャーナル 2026

[[_ジャーナル目次|← 目次]]

## 2026-01-06（火）6:45:26

> 🌤 1°C ほぼ快晴 ｜ 📍 学研教室 八千代台西口教室, 八千代市, 千葉県, 日本

31（水）大晦日、特に何もなく、リザードンデッキ擦ってる。……
1（木）元日礼拝。……

## 2026-01-07（水）14:14:27

（次のエントリ）
```

- **ファイル**: `ジャーナル/YYYY.md`（年別1ファイル。2013〜2026が存在）
- **エントリ見出し**: `## YYYY-MM-DD（曜）H:MM:SS`（曜日は日本語1文字、時刻はゼロ埋めなしの箇所がある＝パーサーは両方許容すること）
- **引用行**: `> 🌤 気温 天気 ｜ 📍 場所` — これはDay One由来のメタ情報。**新規エントリには付けない**（2026-07-30 本人確定）。ただし既存1,113件には存在するので、**読み取り側は必ずこの行を解釈できること**（本文と混ぜず、メタ情報として扱う）。
- **写真埋め込み**: `![[ファイル名.jpeg]]` 形式。実体は `media/ジャーナル写真/` に置かれている。ファイル名はハッシュ様の一意名（例 `c80534fc8eee12ec783f9f56b83be062.png`）。
- **関連ファイル**: `ジャーナル/_ジャーナル目次.md`、`ジャーナル/ジャーナル写真一覧.md`、`ジャーナル/ジャーナル・ダッシュボード.html`

### 中核部品：エントリ・パーサー

閲覧・キャプチャ・編集の3つすべてがこれに依存する。**最初に作り、最も丁寧にテストする部分。**

- **読み**: `YYYY.md` を `## 見出し` 単位で分割し、`{日付, 時刻, 本文, 添付ファイル一覧, タグ一覧}` の配列にする。
- **書き**: 特定エントリだけを差し替え／追加し、**他のエントリ・frontmatter・写真埋め込みを一切壊さずに**ファイル全体を書き戻す。
- 保存のたびに frontmatter の `entries:` を実件数で更新する。
- **必ず検算する**: 書き戻し後に再パースし、件数が「操作前±期待値」と一致しなければ書き込みを中止して元へ戻す。

---

## 3. UI（Day Oneに寄せる）

本人が現在使っているDay One for macOSの画面を基準にする。**ゼロからデザインを考えず、これに近づける**（2026-07-30 本人が実画面を提示）。

### 3ペイン構成

- **左＝サイドバー**: 今日／この日（過去の同じ日）／ジャーナル一覧と件数／ゴミ箱／設定。
- **中央＝エントリ一覧**: 月ごとにグループ見出し。**表記は西暦**（例「2026年7月」）＝Day Oneは和暦だが、既存Vaultデータが西暦のため西暦に統一する（2026-07-30 本人確定）。各行に、日付の大きな数字＋曜日、本文冒頭2〜3行のプレビュー、右端にサムネイル（写真/動画があれば）。選択行はハイライト。
- **右＝詳細ペイン**: 上部に日付＋曜日＋時刻。本文を読みやすい行間で表示し、写真・動画は本文中の位置にインライン表示。**動画はその場で再生できる**。

### その他UI

- 中央上部に、表示切替（リスト／グリッド／カレンダー）、フィルタ、検索のアイコン列。※Day Oneにある「地図」は位置情報を持たないので**作らない**。
- 右上に「＋」＝新規エントリ。
- ダーク基調。落ち着いた配色。

---

## 4. キャプチャ（入力）── 今回のメイン

### 4-1. 入り口は2つ

1. **貼り付け**: Wordで書いたものを丸ごとペースト（リッチテキスト＋画像混在に対応）。
2. **直接入力**: アプリ内でそのまま書く（外部エディタ必須にしない）。

加えて、**写真・動画を個別に追加**できる（ドラッグ&ドロップ／ファイル選択）。レシートの写メも普通の写真としてここから放り込む。

### 4-2. 日付の決め方（2026-07-30 確定）

1. 貼り付けた内容の**先頭行（タイトル）に日付があれば、それを採用**する。
2. **見つからなければ、放り込んだその日（当日）の日付**にする。
3. 検出した日付は**保存前に画面上に表示し、手で直せる**ようにする（誤検出対策）。

日付検出は崩れた表記に耐えること。本人の実際の書き方の例:

- `２０２６０６１３まずは雑感。` ← 全角・区切りなし・直後に文が続く
- `2026-06-13`, `2026/6/13`, `26.6.13`, `令和8年6月13日`, `6月13日`

年が書かれていない場合（`6月13日` 等）は当年として解釈するが、**その旨を画面に表示して確認できるようにする**。

### 4-3. 保存時の動作

- 本文はプレーンなMarkdownとして `ジャーナル/YYYY.md` の**日付順の正しい位置**に挿入する（末尾追記ではない。過去日付の投げ込みがあるため）。
- 同じ日に複数回投げた場合は、**別エントリとして時刻違いで並べる**（既存データも同日に複数エントリが存在する形式）。
- 写真は `media/ジャーナル写真/`、**動画は `media/ジャーナル動画/`** に一意名で保存し、本文に `![[ファイル名]]` を挿入する。
- `ジャーナル写真一覧.md` に追記する。**動画は `ジャーナル動画一覧.md` を新設して同様に追記する**（2026-07-30 確定）。
- 年ファイルが存在しなければ、既存と同じfrontmatter・見出し・目次リンクを持つ形で新規作成する。
- **保存時にAIは一切呼ばない。** 要約・整理・タグ付け・リンク付けをこの時点でやらない（本人確定 2026-07-30「その都度の要約などは必要ない。引き出せる時に引き出せればいい」）。ネットワーク通信も不要でオフライン完結。

### 4-4. 動画の形式・容量（2026-07-30「おまかせ」→ 以下を既定とする）

- 受け付ける形式: `mp4` / `mov` / `m4v` / `webm`（Obsidianが埋め込み再生できる形式）。それ以外は取り込み時に警告して止める。
- **1ファイル200MBを超えたら警告**を出す（保存自体は可能だが確認を挟む）。iPhoneの4K動画は1分で300MB前後になるため、無警告で溜めるとVaultが膨らむ。
- 自動圧縮はフェーズ2では実装しない（後から足せる余地として残す）。

### 4-5. ゴミ箱（2026-07-30 確定：作る）

削除は**実体を消さず退避する**。

- エントリ削除時は、`YYYY.md`から取り除いたうえで **`ジャーナル/_ゴミ箱.md` へ元の日付・時刻を保ったまま追記**する。
- **添付ファイル（写真・動画）は削除しない。** `media/` に残す（復元と、他エントリからの参照のため）。
- ゴミ箱から**元の位置へ復元できる**こと。
- サイドバーの「ゴミ箱」から中身を一覧できる。

---

## 5. 編集

日常の日記アプリとして使うため、**右ペインで本文を軽く編集できる**（誤字修正・書き足し程度）。新規エントリだけでなく、**既存の移行済み1,113件にも同じように効く**。

- 編集対象は本文テキストと添付の削除／追加。
- 保存時は§2のパーサーで**該当エントリだけを差し替え**、他を壊さない。
- 編集は実データへの破壊的操作になりうるため、§7のバックアップを必ず通す。

---

## 6. タグ

### 6-1. 方式＝Obsidian標準のハッシュタグ

`#映画` `#買い物` のように**本文中にそのまま書く**。新しい仕組みを作らない。

理由:
- Obsidian側でタグをクリックすれば全件一覧できる＝「映画タグですぐ列挙」がタダで手に入る。
- 特別なデータ構造を持たせず、ただのMarkdownのまま保てる。
- **語彙を固定しない**ので、後からタグを増やす・付け足すのが自由（本人確認済み 2026-07-30）。

### 6-2. 粒度＝該当箇所の近くに付ける

1日のエントリ全体にまとめて付けるのではなく、**その話題を書いた行の近くにピンポイントで付ける**。本人の書き方は1日に複数話題が混ざるため、全体に付けると精度が落ちる（映画の話が数行なのに1日全部が「映画の日」になる）。

### 6-3. 初期の語彙（固定リストではない・増やしてよい）

`#買い物` `#ポケカ` `#筋トレ` `#映画` `#体調`

**気分タグ（`#しんどい` `#楽しい` 等）は作らない**（2026-07-30 本人確定）。書く時の負担を増やさないため。感情の推移は§8の分析で本文から読み取る形にする。

**ネットで買ったもの**はレシート写真がないが、本文に `#買い物` と書けば同じタグで一緒に拾える（写真経由のレシート一覧とは別ルートだが、集計時に合流する）。

### 6-4. エポックタグ（本人が手で付ける）

- **表記は `#EPOC`**（2026-07-30 本人確定）。
- **AIに判定させない。本人が自分で付ける。** 「これは！」と思ったものに付ける、振り返り用の目印。運用イメージは「今週のEPOC」。
- 週次バッチは**`#EPOC`を新規に付けない**。既に付いているものを読むだけ（§7のタグ付けルールに明記すること）。

---

## 7. 週次バッチ（AIが裏側でやる）

**南雲さんの操作を増やさないことが絶対条件。** 別アプリを開いてレシートを1枚ずつ読ませる、といった往復は発生させない。

### 7-1. やること

1. **カテゴリタグ付け**: その週の新規エントリを読み、§6-3のタグを該当箇所に書き足す。
2. **レシート読み取り**: その週に貼られた写真のうちレシートらしきものを読み、**店名・日付・品目・金額**を抽出して一覧にする。

### 7-2. 出力先

- タグは**本文中に直接書き足す**（該当箇所の近く）。
- レシート一覧は**普通のMarkdownノートとして出力**する（例 `ジャーナル/レシート一覧.md`、または月別）。
- **レシート一覧は後から本人が編集できる**（追記・読み取りミスの修正・撮り忘れ分の手入力）。AIは叩き台を作るだけで、仕上げは本人（2026-07-30 確定）。

### 7-3. 本人のチェック

週次の結果は**本人が軽く確認する**前提。AIが付けたタグを鵜呑みにせず、目視で流し見して直せる状態にする。

### 7-4. 起動タイミング（2026-07-30 確定）

**毎週日曜の午後に自動実行**を既定とする。本人が「今週分やって」と言う必要をなくす。手動起動も併せて用意する（やり直し・臨時実行用）。

- 実行時にMacがスリープ／電源オフだった場合は、**次に起動した時に取りこぼし分をまとめて処理**する（週をまたいで穴が空かないこと）。

### 7-5. 使うAIモデル

| 用途 | 第一候補 | 備考 |
| --- | --- | --- |
| カテゴリタグ付け | ローカル（LM StudioのQwen） | 文面からの軽い分類。上位モデルにしても伸びしろは小さい見込み |
| レシート読み取り | **ローカルの画像読解モデル。候補＝Gemma / Qwen3-VL。実測して選ぶ** | サンプルを数枚ずつ両方に読ませ、読み取り率を比較してから決める（レコード査定と同じやり方） |

**実行機＝M5（メイン機のMacBook Pro・Apple M5／32GB）**（2026-07-30 本人確定）。

当初は15インチMacで動かす案だったが、15インチにはローカルAIが未導入で、初期化してDS実装機にする準備段階のため見送り。M5には既に **LM Studio＋Qwen3.6 35B／bge-m3／Qwen3-VL** が動作確認済みで入っているので、**追加導入なしで着手できる**。Gemmaを使う場合はM5のLM Studioへ追加する。

> [!important] 実データの取り扱い（既定ルール）
> **ローカルモデルとAPIを混同しないこと。** 判断の線は「実行場所が手元のMacか、外部サーバーか」。
> - **ローカルモデル（Gemma / Qwen3-VL をLM Studio等で動かす）＝可**。データはそのMacから出ないので、15インチで日記の写真を読ませても外部送信は起きない。
> - **DeepSeek API（OpenClaw経由）＝不可**。[[🧩 アプリ開発状況（AI共通）]] P節に「実データ（出席・住所録・日記）は `~/openclaw-apps` に置かない」「DeepSeekに送った内容は中国のサーバーに渡る前提で扱う」と明記されている。
>
> 週次バッチの実装では、**呼び先がM5上のローカルモデル（LM Studio）であることを明示的に固定**し、外部APIへ回らないようにする（エンドポイントをコード上で明記）。
> DSは**実装だけ**を担当し、実際の日記本文・写真は渡さない。

### 7-6. レシートの現実的な制約

感熱紙のレシートは時間が経つと印字が消える。**撮るなら買った直後**が確実、という運用上の注意（アプリ側で解決できる問題ではない）。

---

## 8. 分析（アプリ機能ではない）

**アプリに分析エンジンを作り込まない。** 本人が「知りたい」と指令を出した時に、AIセッション（Claude/Codex等）でVaultの全エントリを横断して読ませる。

固定のレポート様式は作らず、都度自由な角度で問える状態にする。本人が挙げた角度の例（2026-07-30）:

- 着想（やりたいこと）が**いつ生まれ、いつ実現したか**。
- お金がどう動いていたか。
- 「苦しい」と言っていたのはいつ頃か。
- 「楽しい」と言っていたのはいつの時期で、何に関してか。**興味の移り変わり**。
- その他、都度指定する角度。

この工程が成立する条件は、**日々のエントリが日付付きで欠けなくVaultに入っていること**だけ。アプリの責務はそこまでで、分析ロジックは実装しない。タグとレシート一覧は、この分析の精度を上げる補助線として効く。

---

## 9. 未確定・要確認（実装前に本人へ聞く）

**2026-07-30時点で、仕様上の未確定はすべて解消済み。** 以下は決着の記録（詳細は各節に反映済み）。

| 項目 | 決着 |
| --- | --- |
| A. 気温・場所の引用行 | **新規エントリには付けない**（既存分の読み取りには対応する） |
| B. 一覧の月見出し | **西暦**（例「2026年7月」） |
| C. 動画一覧ノート | **`ジャーナル動画一覧.md` を新設する** |
| D. 気分タグ | **作らない**（書く負担を増やさない） |
| E. 週次バッチの起動 | **毎週日曜の午後に自動実行**（手動起動も併設） |
| F. エポックタグの表記 | **`#EPOC`** |
| G. 動画の形式・容量 | おまかせ→ mp4/mov/m4v/webm、200MB超で警告、自動圧縮は後回し |
| H. ゴミ箱 | **作る**（`_ゴミ箱.md` へ退避・添付は消さない・復元可） |
| I. 週次バッチの実行機 | **M5（メイン機）**。15インチはローカルAI未導入のため見送り |

**実装中に新たな判断が必要になったら、埋めずに止めて本人へ聞くこと**（§0）。

---

## 10. 実装の順番（フェーズ分け）

一度に全部作らない。**各フェーズごとに `.app` を起動して実機確認するまでを完了条件**とする。

- **フェーズ1｜読む**: エントリ・パーサーを作り、既存 `ジャーナル/*.md` を読み込んで3ペインで一覧・詳細表示できる。**書き込みは一切しない**（安全に既存データを確認するため）。1,113件が正しく読めることを検算する。
- **フェーズ2｜書く（新規）**: 貼り付け／直接入力＋日付検出＋写真・動画添付＋Vaultへの保存。バックアップと検算の安全弁を必ず入れる。
- **フェーズ3｜直す**: 既存エントリの編集・書き戻し。
- **フェーズ4｜探す**: 全文検索、タグ絞り込み、「この日の記憶」、カレンダー表示。
- **フェーズ5｜週次バッチ**: タグ付けとレシート読み取り。まずレシートの読み取り率をサンプルで実測してから実装する。

### 安全弁（フェーズ2以降で必須）

- 書き込み前に `ジャーナル/` 一式をバックアップする（学校台帳と同じく2か所：アプリ内退避＋Vault内 `バックアップ/`）。
- 読み込みに失敗した場合は、**移行も保存も行わず**警告を出して止める（学校台帳で実データを空で上書きしかけた事故と同型の防止策）。
- 書き戻し後に再パースして件数を検算し、想定と違えば元に戻す。

---

## 11. 次の一手

1. [[🧩 アプリ開発状況（AI共通）]] O節を更新する。
2. **フェーズ1だけ**をDS向け作業票に切り出して渡す（完了条件＝`.app`で起動し、既存1,113件が3ペインで正しく読めること）。
3. フェーズ1が実機で動いたら、フェーズ2へ進む。

> 8/23-29のフィリピン渡航まで約3週間。三本立て（シランガナ・ヤフオク・学校）の8/20締切が動いている期間なので、**このアプリは主役タスクを崩さない範囲で進める**。フェーズ1だけでも動けば、Day Oneを開かずに過去の日記が読める状態にはなる。
