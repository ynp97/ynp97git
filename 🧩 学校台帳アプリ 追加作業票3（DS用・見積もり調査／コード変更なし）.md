---
種別: 調査・見積もり作業票 / 外部AI（DS＝OpenClaw＋DeepSeek）向け
作成日: 2026-07-28
対象: `~/openclaw-apps/SchoolLedger`
渡し方: このファイルだけを渡す。Vaultの他は読ませない。
重要: **これは調査だけの作業票。コードを変更しない。**
---

# 学校台帳アプリ 追加作業票3 ── 設計変更の見積もり調査

> [!danger] レッドライン（必ず守ること）
> 1. **コードを1行も変更しない。** これは調査と見積もりだけの作業票。実装は次の作業票で指示する。
> 2. **`~/Library/Application Support/SchoolLedger/data.json` を開かない・読まない・コピーしない。** ここには実在する家庭の**氏名と住所**が入っている。中身を見る必要はない。構造を知りたい場合は**ソースコードの型定義から読む**こと。
> 3. 実データを外部へ送らない。動作確認が要る場合も架空の名前だけを使う。

---

## 0. なぜ調査だけなのか

初版（あなたが実装したもの）は骨格として良くできている。しかしその後、運営側で**データの持ち方そのものの設計が変わった**。

初版を捨てて作り直すか、必要な部分だけ直すかを決めたい。そのために、**まず現状のコードを読んで「どこをどれだけ直す必要があるか」を報告してほしい。** 判断は運営側でする。

---

## 1. 何がどう変わったか（新しい設計）

### 変更1：`importedSends` を廃止し、すべて `people` に統合する

**旧設計（初版で実装済み）**

- ラベル管理アプリから書き出したJSONを読み、`importedSends` という**別のテーブル**に貯める。
- あとでその人から問い合わせが来たら、`people` のレコードと**手作業で紐づける**（`matchedPersonId`）。
- 紐づかない `importedSends` は「送ったが反応がなかった人」として集計する。

**新設計（これから）**

- 取り込んだ瞬間に、`sends[]` の1件ずつを**そのまま `Person` として `people` に登録する**。
- `importedSends` というテーブルは**なくす**。`matchedPersonId` も**なくす**。
- 登録時に入るもの：
  - `source`（タグ）＝既定「データパレット」
  - `events` の1件目＝ `{ date: sentAt, stage: "送付" }`
  - 距離帯 `zone`（`station` / `trainMin` / `walkMin` / `totalMin` / `band` / `accuracy`）＝ **`Person` の項目として保持**する
- **紐づけボタン・突き合わせ画面は不要になる**（機能ごと削除）。

**なぜ変えるか**：送った相手が鹿島経由でもネット経由でも、こちらがやることは「パンフを送る」で同じ。だったら全員を同じ台帳に同じ形で並べ、あとから**タグで好きな角度に切り分けて集計**するほうが実態に合う、という運営側の判断。

### 変更2：ステージを8段階 → 6段階にする

**旧（初版で実装済み）**

`新規問い合わせ` / `資料請求` / `見学・個別相談予約` / `見学・個別相談実施` / `体験` / `入学・入会` / `保留` / `見送り`

**新**

`送付` / `電話・メールあり` / `見学予約` / `見学・個別相談実施` / `体験` / `入学`

- `資料請求` は `送付` に統合。
- `保留` / `見送り` は**廃止**。「まだ望みがあるか／もう終わったか」は判断できないことが多いので、**今どのステージで止まっているかが、そのままその人の状態**とする。
- `feel`（手応え1〜5）と `comment`（ひとこと）は**そのまま残す**。特に `見学・個別相談実施` で使う。
- イベント履歴型（進むたびに1行足す・上書きしない）という骨格は**変えない**。

### 変わらないもの（作り直さないこと）

- イベント履歴型の考え方
- 実ファイル保存（`~/Library/Application Support/SchoolLedger/data.json`）と日次バックアップ
- タグ（`data.tags`）が可変で本人が増やせる仕組み
- CSV書き出し（BOM付き）
- 距離推定のロジック**そのもの**（ラベル管理アプリが正。こちらでは計算しない。受け取った値を持つだけ）

---

## 2. 調べて報告してほしいこと

### 2-1. 影響範囲

`importedSends` と、8段階のステージ（`stage` の enum やそれに相当する定義）が、**どのファイルの、どのあたりで使われているか**を洗い出す。

ファイルごとに、次の形で報告してほしい。

| ファイル | 何に使っているか | 直す必要のある箇所（概算行数） | 難易度 |
| --- | --- | ---: | --- |

対象は最低限これら（実際の構成に合わせて増やしてよい）。

- `Models/DataModels.swift`
- `ViewModels/DataStore.swift`
- `ViewModels/StatisticsCalculator.swift`
- `Views/` 配下すべて

### 2-2. 消える機能・残る機能

- 紐づけ（突き合わせ）まわりで**丸ごと消せるコード**はどれか。画面ごと消えるものはあるか。
- 逆に、`importedSends` を消すと**壊れる／作り直しになる**箇所はどこか。特に統計まわり。

### 2-3. 統計への影響

`StatisticsCalculator` が今どんな数字を出しているかを列挙し、それぞれについて：

- 新設計でもそのまま出せるか
- 出せないなら、何が足りないか

を書く。特に「送ったが反応がなかった人」を今どうやって数えているか（`matchedPersonId == nil` で数えているはず）と、それが新設計だとどう変わるかを見てほしい。
※新設計での考え方＝**ステージが `送付` のまま止まっている人＝反応なし**、`電話・メールあり` 以降へ進んだ人＝反応あり。

### 2-4. 既存データの移行（ここが一番大事）

**すでに実在するデータが入っている。消してはいけない。**

`data.json` の**中身は見ずに**、**ソースコードの型定義から**、次を検討して報告してほしい。

1. 旧形式（`importedSends` あり・8段階）から新形式（`people` のみ・6段階）へ変換するとき、**どういう手順が要るか**。
2. 変換で**情報が失われるところ**はどこか。
3. アプリ起動時に自動変換する方式と、変換用の小さなコマンドを別に作る方式で、**どちらが安全か**（あなたの意見）。
4. 変換前に自動でバックアップを取るなら、どこへどう取るのが良いか。

> [!warning] ステージの読み替えは運営側が決める
> 旧8段階を新6段階へどう読み替えるかは**運営側が決める。あなたは決めないこと。**
> 特に `新規問い合わせ` / `保留` / `見送り` の3つは、そのまま対応する新ステージがない。
> 報告の中で「ここは指示がないと変換できない」と挙げてくれれば良い。

### 2-5. 作り直しか、部分修正か（あなたの意見）

上を踏まえて、次のどちらを勧めるかと、その理由を書いてほしい。

- **A：必要な部分だけ直す** — `importedSends` の削除とステージの変更に絞る
- **B：正本から作り直す** — 新設計で書き直したほうが結果的に安全

判断材料として、それぞれの概算の作業量（ファイル数・行数・想定時間）も添えること。

---

## 3. やらないこと

- コードの変更（**1行も**）
- `data.json` を開くこと
- 距離推定ロジックの実装（ラベル管理が正）
- 新しい機能の提案（今回は既存の設計変更の見積もりだけ）

---

## 4. 報告の形式

次の順で、文章で報告してほしい。表は表のままでよい。

1. **結論**：AかBか、なぜか、概算の作業量
2. **影響範囲の表**（2-1）
3. **消える機能・壊れる箇所**（2-2）
4. **統計への影響**（2-3）
5. **移行の手順案と、指示待ちの項目**（2-4）
6. **読んでいて気づいた懸念**（設計変更と関係なく、初版で気になっている点があれば）

---

*関連（あなたは読まなくてよい。運営側の記録）: `🧩 学校台帳アプリ 設計図（DS作業票）` / `🧩 学校台帳アプリ 追加作業票2` / `🧩 アプリ開発状況（AI共通）` T節*
