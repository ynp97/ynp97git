---
title: シランガナ開発記録
date: 2026-06-20
project: シランガナ
tags:
  - app-development
  - iOS
  - OCR
  - SwiftUI
  - AppStore
---

# シランガナ開発記録

## 概要

「シランガナ」は、スマホのカメラで読めない漢字を撮り、読み仮名と代表的な意味をすばやく表示するiPhoneアプリとして検討・実装した。

最初の方向性は「小学生向け」ではなく、「子供でも使える」シンプルさ。難しい漢字にも対応する。最終目標はApple Storeで販売・公開すること。

## プロダクト方針

- 無料または安価な単機能MVPから開始する。
- サブスクではなく買い切りを基本にする。
- 最初は無料単機能で公開まで行く。
- 代表的な意味も入れる。
- 将来的に「シランガナPRO」として英語など多言語の読み・意味表示も検討。
- 後から運営コストが増えない設計を優先する。
- AIを使う場合はサーバー費用が発生するため慎重に扱う。

## 辞書・意味

読みと意味の基盤として、オフライン辞書を採用した。

- JMdict: 読み
- Japanese WordNet: 代表的な意味
- SQLite化してアプリに同梱
- サーバー不要、サブスク不要
- ただしJapanese WordNetには一定の誤りがあるため、正確性の限界は残る

主な辞書ファイル:

- `work/Shirangana/Shirangana/Resources/JMdict.sqlite`
- 読み: 約245,771件
- 意味: 約73,054件

## 価格検討

ライト版:

- 読みだけ
- 無料または安価

通常版・買い切り:

- 読み + 代表的な意味
- 300円から500円程度を検討

PRO案:

- 多言語の意味理解
- 英語などの単語を撮って読み方・意味を出す
- 高くてもよいが、正確性を高める必要あり

## デザイン方針

最初はシンプルでポップなデザインを想定。

その後、8bit風・初代ファミコン風の画面イメージへ変更。

主なデザイン要件:

- 子供向けではなく、子供でも使える
- 大きなボタン
- 丸いボタン
- 押すのが楽しい、ぷにゅっと凹むアニメーション
- 上部タイトル: `シランガナ：よみがなキャッチャー`
- ロゴに「シランガナ」の文字
- 遊び心のあるロゴ案
- 開発中は大きくビルド番号を表示

生成・確認した主な画像:

- `outputs/shirangana_round_button.png`
- `outputs/shirangana_crop_step.png`
- `outputs/shirangana_logo_concepts.png`
- `outputs/shirangana_8bit_camera.png`
- `outputs/shirangana_8bit_result.png`

## 実装したアプリ

ネイティブiOSアプリとして実装。

プロジェクト:

- `work/Shirangana/Shirangana.xcodeproj`
- Bundle ID: `com.yoshiakinagumo.shirangana`
- iOS 17+
- SwiftUI + AVFoundation + Vision + SQLite
- 実機: iPhone 15 Pro Max
- Personal Team: `UJV34TVHWC`

主な画面:

- カメラ画面
- 撮影後の選択画面
- 処理中画面
- 結果画面
- About画面

## カメラ操作の変遷

当初は「カメラを開く」から撮影する構想だったが、シンプルさを優先し、アプリ画面内でシャッターを押すとすぐ撮影できる形へ変更した。

撮影後に読みたい漢字を選ぶ必要があることが判明。

問題:

- ピンチズームできないと読みたい漢字を絞れない
- OCRの当たり範囲が大きすぎて、目的の漢字にたどり着けない
- 縦書きにも対応が必要

対応:

- 撮影後にSTEP 2の選択画面を追加
- 写真をズーム・移動して黄色い枠へ合わせる
- 横書き・縦書き切り替え
- 枠内だけをOCRへ渡す

## 操作性の問題と修正

### 問題1: 1本指で写真を動かせない

SwiftUIジェスチャーの競合により、ズームやドラッグが効かない問題が発生。

対応履歴:

- SwiftUIのDragGesture + MagnificationGestureを試行
- UIKitのジェスチャー認識へ変更
- 透明な操作面を追加
- 最終的にUIScrollViewベースへ置き換え

### 問題2: 動き出すまで15秒ほどかかる

原因候補:

- フル解像度写真の遅延デコード
- 撮影後の巨大画像処理
- SwiftUI再描画による遅延

対応:

- 撮影を速度優先に設定
- 撮影後、最大2400pxへバックグラウンド縮小
- 即時キャッシュ・向き補正
- 選択画面には準備済み画像だけを渡す

### 問題3: 枠サイズが固定

ユーザー指摘:

- 読みたい文字を正確に捉えるには、枠の横幅と高さを別々に変える必要がある

対応:

- 横幅スライダー
- 高さスライダー
- プラス・マイナスボタン

## 撮影枠と選択枠の不一致

ユーザー指摘:

撮影画面でせっかく枠に合わせても、選択画面へ移ると枠サイズが変わるのはよくない。

対応:

- 撮影画面の枠: `250×180`
- 選択画面の初期枠も`250×180`へ統一
- 選択画面の初期表示をカメラと同じ「画面を埋める倍率」へ変更
- 中央に合わせたものが、画面遷移後も中央から始まるようにした

## OCRと誤読

問題:

- 「学研教室」を選ぶと「考える」と出る
- 固有名詞やロゴが一般語へ補正される

対応:

- Visionの言語補正を無効化
- OCR第1候補だけでなく最大5候補を辞書照合
- 長い複合語を優先
- 「学研」+「教室」のように辞書語へ分割し、読みを合成

例:

- `学研` → `がっけん`
- `教室` → `きょうしつ`
- `学研教室` → `がっけんきょうしつ`

## 薄い文字の未解決問題

ユーザー指摘:

コントラストの薄い場所の文字を認識しない。

対応したこと:

- OCR対象画像を高コントラスト化
- グレースケール化
- 鮮明化
- 強い白黒コントラスト化
- 最小文字サイズ条件を緩和
- 背景の明るさムラを除去
- 複数画像を並列OCR

それでも未解決。

現時点の結論:

- `BUILD 11`でも薄い文字の認識問題は解決していない
- 次に再開する場合は、Vision OCRの限界を疑い、以下を検討する

再開時の検討案:

- 枠内画像のプレビュー表示
- 補正後画像の確認表示
- OCR前の手動コントラスト調整
- Apple Vision以外のOCR候補比較
- 端末内ML OCRまたは軽量モデル
- 薄い文字専用の二値化・局所閾値処理
- そもそもカメラ撮影時に露出・フォーカスを調整するUI

## 2026-06-25 BUILD 12

実機ビルド・インストール・起動を再開し、`iphone15proMAX` で起動成功を確認した。

対応したこと:

- 失敗/結果画面に `BUILD 12 診断` パネルを追加
- 枠内画像、高コントラスト、背景ムラ除去、二値化の各画像を表示
- Vision OCR候補を画像パターンごとに表示
- 辞書照合候補を表示
- 撮影画面でピンチズームできるように変更
- 撮影画面で文字部分をタップするとピント/露出を合わせるように変更
- 撮影画面にズーム倍率表示と黄色いフォーカス表示を追加

次の確認:

- 失敗した2サンプルを、ピンチで文字を大きくし、文字部分をタップしてピント/露出を合わせてから撮り直す
- 失敗した場合は診断パネルで、枠内画像・補正画像・Vision OCR候補・辞書候補を見る
- 失敗原因を「撮影/ピント」「切り抜き」「画像補正」「Vision OCR」「辞書照合」「辞書未登録」に分ける

## 2026-06-25 BUILD 13

BUILD 12を実機で試したところ、普通に字が見えているのに `OCR候補なし` になる報告があった。また、縦書きも多いことが確認された。

対応したこと:

- Vision OCRを日本語固定だけでなく、日英併用・自動言語検出・fast認識でも再試行
- OCR画像パターンに `余白つき拡大` を追加
- OCR画像パターンに `反転＋余白` を追加
- 縦長の切り抜きでは、画像を上から2〜7分割して横一列へ並べ直す `縦書きN分割` を追加
- `BUILD 13` として実機ビルド、インストール、起動成功

狙い:

- 人間には見えるがVisionが検出ゼロにするケースを、余白・言語設定・認識レベルの違いで拾う
- 縦書きの1列テキストを、横書きのように並べ直してOCRに渡す

## 2026-06-25 BUILD 14

BUILD 13の実機診断スクショで、`漢字` がはっきり見えているにもかかわらず、Vision OCRが `I` や `H` として拾い、辞書照合に届かないことを確認した。さらに、期待していた `縦書きN分割` が診断に出ていなかった。

対応したこと:

- 縦書き分割の判定条件を緩和
- ほぼ正方形の切り抜きでも `縦書き2〜4分割` を出す
- 縦長の切り抜きでは `縦書き2〜7分割` を出す
- 高コントラスト画像にも `縦強調N分割` を追加
- 診断見出しを `BUILD 14 診断` に更新
- 実機ビルド、インストール、起動成功

次の確認:

- `漢字` サンプルをもう一度撮り、`縦書き2分割` または `縦強調2分割` に `漢字` や近い候補が出るか確認する
- 2文字でなければ、字数に近い分割数を見る

追記:

- `BUILD 14` 実機診断で、`縦書き2分割` と `縦強調2分割` が `漢字` をOCR候補として出すことを確認した
- ただし最終結果は `活倭子` になった
- 次の修正点はOCRではなく辞書照合/候補順位づけ
- 現状のスコアは漢字数の多い候補を強く優先するため、長い誤候補が勝ちやすい
- 次は `漢字` のような短く確かな候補を勝たせるスコアへ直す

## 2026-06-27 BUILD 15

ユーザーから、標準カメラに比べてシランガナ内カメラの写りが悪い、シャッター反応が遅く撮った快感がない、結果画面で「もう一度」までスクロールしたくない、という指摘があった。

対応したこと:

- カメラを `builtInWideAngleCamera` 固定から、使える場合は `builtInTripleCamera` / `builtInDualWideCamera` を優先する構成へ変更
- 撮影設定を速度優先から `photoOutput.maxPhotoQualityPrioritization` に合わせた品質優先へ変更
- カメラ初期設定で連続AF、連続露出、連続ホワイトバランス、スムーズAFを有効化
- 撮影後のクロップ用画像を最大2400px / JPEG 0.92 から最大4096px / JPEG 0.98 へ変更
- ライブカメラ画面とクロップ画面のレトロ走査線を外し、文字を確認するときの見た目を素の画像に近づけた
- シャッター押下後の420ms待機を廃止し、即撮影に変更
- シャッター押下時に硬めのハプティックと白フラッシュを即時表示
- シャッターボタンの押下アニメーションを、ぬるい潰れ方から短くカチッとした反応へ変更
- 結果画面と失敗画面の「もう一度」を最上部に固定し、診断パネル下までスクロールしなくても撮り直せるようにした
- 辞書照合で、辞書にそのままある候補を強く優先し、`活倭子` のように1文字辞書語だけをつないだ合成候補を弱めた
- 診断見出しを `BUILD 15 診断` に更新
- `iphone15proMAX` 向けに署名ありDebugビルド成功、実機インストール成功。起動コマンドはiPhoneがロック中だったため未確認

次の確認:

- iPhoneを手で開いて `BUILD 15` を起動し、標準カメラとの差がどこまで縮まったか確認する
- シャッター押下直後の反応が「撮った」と感じられるか確認する
- 結果/失敗画面で「もう一度」が上に出て、すぐ撮り直せるか確認する
- `漢字` サンプルで、診断候補に `漢字` が出た場合に最終結果も `漢字` になるか確認する

## 2026-06-27 BUILD 16

BUILD 14時点のスクショで、OCR候補には `問題項目` が出ている一方、最終表示が `問問題項目` / `といまだいこうもく` になる例を確認した。これは現行BUILDの新規不具合ではなく、BUILD 14の参考スクショ。

対応したこと:

- `問問題項目` のように「余計な1字 + 正しい複合語」がOCR候補に入った場合、候補内の部分文字列から自然な複合語を探す処理を追加
- `問題` + `項目` のように2文字以上の辞書語で構成される候補を、1文字語を混ぜた長い合成候補より優先するよう調整
- 診断見出しを `BUILD 16 診断` に更新
- `iphone15proMAX` 向けに署名ありDebugビルド成功、実機インストール成功

次の確認:

- 旧BUILD 14スクショと同じ `問題項目` をBUILD 16で撮り、最終表示が `問題項目` / `もんだいこうもく` へ寄るか確認する
- BUILD 15で追加したカメラ画質・シャッター体感・「もう一度」上固定も同時に確認する

## 2026-07-01 BUILD 17

BUILD 16の実機スクショで、縦長の文字列が見えているのに `OCR候補なし` になり、最終表示が `者` に落ちる例を確認した。さらに `御国` が `翻`、`高群逸枝` が `枝` に落ちる例も確認した。

対応したこと:

- Vision OCRへ渡す画像パターンに、黒い文字部分だけを切り出して拡大する `文字だけ拡大` 系を追加
- 縦書き固定分割を最大14分割まで拡張
- 黒い文字の行のまとまりから自動で1文字ずつ横に並べ直す `縦文字自動分割` 系を追加
- 背景ムラ除去後の余白つき画像、文字だけムラ除去、複数しきい値の二値化を追加
- Vision OCRに `御国`、`御言葉`、`高群逸枝` などのカスタム語を渡すようにした
- 辞書側に `御国`、`御言葉`、`御心`、`御霊`、`高群逸枝` の補助辞書を追加
- 長いOCR候補から `者` / `枝` のような1文字だけを拾って確定する挙動を抑制
- 診断見出しを `BUILD 17 診断` に更新
- Simulator向け署名なしDebugビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功
- 起動確認はiPhoneロック中のため未了

2026-07-02追記:

- `iphone15proMAX` は Developer Mode enabled / paired / connected を確認。
- BUILD 17 を実機向けに再ビルドし、`xcrun devicectl device install app` で再インストール成功。
- 署名は `Apple Development: darkstarman2019@gmail.com (K74T5Z7BG6)`、プロビジョニングプロファイルは `iOS Team Provisioning Profile: com.yoshiakinagumo.shirangana`。
- 起動は iPhone 側で開発元がまだ信頼されていないため拒否された。次は iPhone の `設定 > 一般 > VPNとデバイス管理` で `Apple Development: darkstarman2019@gmail.com` を信頼してから、BUILD 17 の起動確認へ進む。

次の確認:

- `御国` で `翻` に落ちず、`御国` / `みくに` または `おくに` が出るか確認する
- `高群逸枝` で `枝` に落ちず、`高群逸枝` / `たかむれいつえ` が出るか確認する
- 長い縦書き文字列で `縦文字自動分割` や `文字だけ...` 系の診断画像に正しいOCR候補が出るか確認する
- まだ1文字だけに落ちる場合は、辞書ではなくVision OCR候補そのものを診断パネルで確認する

## 2026-07-02 BUILD 18

BUILD 17実機で、縦書き撮影後に `ヨミヲ シラベテイマス` の処理中画面で固まる/落ちる例を確認した。原因は、BUILD 17で縦書き診断画像を増やしすぎ、複数画像パターン × 複数Vision OCR試行を大量に走らせていたことが主因と判断。

対応したこと:

- OCRへ渡す前に、枠内画像を必ず長辺1200px級へ縮小し、高解像度画像をそのままVisionへ渡さないよう変更
- Vision OCRを全画像パターン並列実行から、順次実行へ変更
- 1画像あたりのVision OCR試行を、日本語accurateの1回に制限
- OCR画像パターンを最大8個に制限
- 3パターン以上確認し、有効な日本語候補が出た場合は残りOCRを早期打ち切り
- BUILD 17で追加した背景ムラ除去、反転、複数二値化などの重い診断パターンを通常処理から外した
- `身長` / `身体` / `身長と体` / `身長と身体` を補助辞書へ追加
- 診断見出しを固定文字列ではなく実際のBUILD番号表示へ変更
- BUILD番号を18へ更新
- Simulator向けビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功、起動成功

次の確認:

- まず `ヨミヲ シラベテイマス` で固まらず、結果または失敗画面へ戻るか確認する
- `身長と体` の縦書きで、`身` だけに落ちず、`身長` または `身長と体` に寄るか確認する
- `御国` / `高群逸枝` / 長い縦書き文字列を再確認する
- BUILD 18で安定したら、外した重い診断パターンは「深く調べる」ボタンなどに分離して戻す

## 2026-07-02 BUILD 19

BUILD 18実機で、縦書き `学級新聞` が `新` だけに落ちる例を確認した。固まりは改善したが、縦書き語を正しく拾えていない。

対応したこと:

- Visionカスタム語に `学級` / `新聞` / `学級新聞` を追加
- 補助辞書に `学級` / `新聞` / `学級新聞` を追加
- 縦長の切り抜きで最終結果が1文字だけになった場合、その1文字を正解として大きく表示せず、失敗画面＋診断へ戻す安全弁を追加
- BUILD番号を19へ更新
- Simulator向けビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功
- 起動確認はiPhoneロック中のため未了

次の確認:

- iPhoneを開いてBUILD 19を起動し、右上のBUILD表示が19になっているか確認する
- `学級新聞` で `新` だけを結果表示せず、診断へ回るか確認する
- OCR候補に `学級新聞` / `新聞` / `学級` が出るか確認する
- まだ出ない場合は、Vision OCRだけで縦書きを解く方向ではなく、撮影後UIで候補確認/手動補正を入れる方向に切り替える

## 2026-07-06 単体引き継ぎ入口

アプリ開発の共通台帳が大きくなり、シランガナだけ再開したい時にも他アプリの文脈を読みすぎる問題が出たため、単体引き継ぎ入口を追加した。

追加したもの:

- `🧩 シランガナ引き継ぎ（AI共通）.md`

使い方:

- 新しいAIや新しいチャットで `シランガナ` とだけ言われたら、まずこの単体引き継ぎを読む。
- `🧩 アプリ開発状況（AI共通）.md` の全文、戦略モード4ファイル、他アプリの記録は最初に読まない。
- シランガナ作業後は、単体引き継ぎとこの開発記録を更新する。大きな状態変更がある時だけ共通台帳のシランガナ節も更新する。

## 2026-07-06 BUILD 20

BUILD 19実機スクショで、固まらず診断に戻ること、`値` / `形` は結果表示できることを確認した。一方で、縦書きの `一部分が` では小さいふりがな寄りの `いちぶん` / `いちぶ` / `ん` を拾い、`場合は` では `場` や記号混じり候補に寄っていた。

対応したこと:

- OCR画像パターン上限を8から10へ増やした
- 縦書きの太い主文字列を優先して切り出す `主文字縦自動` / `文字だけ主縦自動` を追加
- `一!一部;分え` や `場△` のような記号混じり候補を「十分読めた」と扱わず、後続画像パターンまでOCRを続けるようにした
- Visionカスタム語に `一部` / `部分` / `一部分` / `場合` / `形` / `値` を追加
- 補助辞書に `一部` / `部分` / `一部分` / `場合` / `形` / `値` を追加
- BUILD番号を20へ更新
- Simulator向けビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功
- 起動確認はiPhoneロック中のため拒否された

次の確認:

- iPhoneを開いてBUILD 20を起動し、右上のBUILD表示が20になっているか確認する
- `一部分が` の診断で `主文字縦自動` / `文字だけ主縦自動` に `一部` / `部分` / `一部分` が出るか確認する
- `場合は` で `場合` / `ばあい` へ寄るか確認する
- `値` / `形` の成功例がBUILD 20で壊れていないか確認する
- まだふりがなだけ拾う場合は、撮影後UIで「ふりがなを避けて枠を細くする」案内、または候補選択UIに進む

## 2026-07-06 BUILD 21

BUILD 20実機スクショで、`派生語` / `雅歌` / `症候` は成功した。一方で、`不快表現` は `不現` のように縦書きの一部だけを拾い、`受動分詞` 系の画像では存在しない `旧人` を結果として出していた。ユーザー確認により、`旧人` は辞書結果として扱わないことにした。

対応したこと:

- OCR早期打ち切りの判定を強め、漢字2文字だけの候補では「十分読めた」と扱わないようにした
- 縦長切り抜きで、最終結果が2文字以下の漢字のみ、かつOCR候補内により長い漢字列がある場合は、その短い語を結果表示せず失敗画面＋診断へ戻す安全弁を追加
- 補助辞書側で `旧人` をブロックし、読み・意味候補を返さないようにした
- BUILD番号を21へ更新
- Simulator向けビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功
- 実機起動成功

次の確認:

- iPhoneで右上のBUILD表示が21になっているか確認する
- `不快表現` で `不現` を大きく結果表示せず、失敗画面＋診断へ戻るか確認する
- `受動分詞` 系の画像で `旧人` を結果表示しないか確認する
- `派生語` / `雅歌` / `症候` の成功例がBUILD 21で壊れていないか確認する
- `一部分が` / `場合は` / `学級新聞` も引き続き確認する

## 2026-07-06 BUILD 22

BUILD 21実機スクショで、`強意語` / `本文` / `差別表現以外` は成功した。一方で、`受動分詞` は末尾の `詞` を落として `受動分`、`引照個所` は末尾の `所` を落として `引照個`、`旧約聖書` は `旧日約聖書` から `約聖書` に寄る例を確認した。長い読みが結果画面で `...` 省略されるUI問題も確認した。

対応したこと:

- 補助辞書に `受動分詞` / `強意語` / `本文` / `引照個所` / `引照箇所` / `旧約聖書` / `差別表現以外` を追加
- Visionカスタム語に `受動分詞` / `強意語` / `本文` / `引照個所` / `引照箇所` / `旧約聖書` / `差別表現` / `差別表現以外` を追加
- OCR実測補正として `受動分` → `受動分詞`、`引照個` → `引照個所`、`引照箇` → `引照箇所`、`旧日約聖書` → `旧約聖書` を追加
- `受動分` / `引照個` / `約聖書` は単独の辞書結果として返さないようにブロック
- 結果画面の読みボタンを2行表示対応にし、長い読みが省略されにくいようにした
- BUILD番号を22へ更新
- Simulator向けビルド成功
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功
- 起動確認はiPhoneロック中のため未了

次の確認:

- iPhoneで右上のBUILD表示が22になっているか確認する
- `受動分詞` が `受動分` で止まらず、`じゅどうぶんし` へ補正されるか確認する
- `引照個所` が `引照個` で止まらず、`いんしょうかしょ` へ補正されるか確認する
- `旧約聖書` が `約聖書` に寄らず、`きゅうやくせいしょ` へ補正されるか確認する
- `差別表現以外` の長い読みが結果画面で読めるか確認する
- BUILD 21で成功していた `強意語` / `本文` / `差別表現以外` が壊れていないか確認する

## 2026-07-10 BUILD 23

BUILD 22までの通常OCRでは、画像パターンの上限10件が縦書き分割で埋まり、薄い文字に必要な `高コントラスト` / `文字だけ強調` が実際にはOCRへ渡らない場合があることをコードから確認した。

対応したこと:

- `高コントラスト` と `文字だけ強調` を、縦書き自動分割・固定分割より前に生成する順番へ変更。薄い印刷文字でも必ず補正パターンが先に評価される。
- 辞書照合スコアを調整。複数の2文字以上の語に自然に分割できる長い候補を強め、短い実在語の部分誤検出に負けにくくした。
- BUILD番号を23へ更新。
- Simulator向けDebugビルド成功。
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功。起動・撮影確認は未実施。

次の確認:

- iPhoneへBUILD 23を入れ、右上のBUILD表示が23になっているか確認する。
- 薄い文字で診断に `高コントラスト` と `文字だけ強調` が出るか確認する。
- `不快表現` のような長い語で、短い部分語より完全な候補が優先されるか確認する。

## 2026-07-10 BUILD 24

BUILD 23の実機スクリーンショットで、`池田亮司` は複数の画像パターンで正しくOCRできているのに辞書ヒットなし、`電子音楽` も正しく読めているのに `子音楽` / `しいんらく` へ落ちる例を確認した。原因はOCR精度ではなく、読点を含むOCR候補の辞書照合と、人名辞書の不在だった。

対応したこと:

- 辞書照合前に、空白・読点などの文字以外を除去して漢字・かなだけを照合するよう修正。`、電子音楽、` を `電子音楽` として扱う。
- `池田亮司` / `池田` を補助辞書・Visionカスタム語へ追加。
- BUILD番号を24へ更新。
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功。

次の確認:

- `電子音楽` が `でんしおんがく` と表示され、`子音楽` へ落ちないか。
- `池田亮司` が `いけだりょうじ` と表示されるか。
- 汎用的な人名まで扱うには、今後は人名辞書または候補選択・手入力導線が必要かを実測で判断する。

## 2026-07-10 BUILD 25

OCR候補と辞書候補を診断表示で眺めるだけでは、正しい候補があっても利用者が結果へ進めなかった。そのため、候補を選ぶ導線を追加した。

対応したこと:

- 辞書ヒットしたOCR候補を重複なく最大5件、「ほかの候補をえらぶ」に表示。
- 自動結果画面と失敗画面の両方から、候補ボタンを押してその語・読み・意味の結果画面へ進める。
- BUILD番号を25へ更新。
- `iphone15proMAX` 向け署名ありDebugビルド成功、実機インストール成功。

次の確認:

- 自動結果が不自然な撮影で、候補ボタンが表示されるか。
- 候補を押すと選んだ語の読みと意味へ切り替わるか。
- 辞書ヒットのない人名などでは、次段階として候補の手入力導線が必要か確認する。

区切り:

- 2026-07-10、BUILD 25の候補選択UI実装・実機インストールまで完了。ユーザー確認により、ここで一旦区切る。次回は候補ボタンの実機操作確認から再開する。

## 2026-07-10 エラー導線確認

7/10の軽めタスク「カメラ許可／拒否・辞書未読込・読みなしのエラー導線」を、BUILD 25のコードとSimulator向けDebugビルドで確認した。コード変更は不要。

- カメラ初回許可: iOS標準の許可ダイアログを要求する。
- カメラ拒否: 「設定でカメラの使用を許可してください。」と表示し、ボタンが「設定を開く」へ変わる。
- 辞書未読込: 同梱 `JMdict.sqlite` が無い場合は「読み辞書が見つかりません。」、開けない場合は「読み辞書を開けませんでした。」と表示する。
- 読みなし: 「読みが見つかりませんでした。」を表示し、OCR候補・辞書照合の診断と、辞書ヒット済み候補があれば候補選択を残す。
- 復帰: カメラ拒否以外は「もう一度」で撮影へ戻る。
- `xcodebuild` のiOS Simulator向けDebugビルド成功。

実機で残る最小確認は、設定でカメラを一度拒否した状態から「設定を開く」がシランガナの設定画面へ移ることだけ。

## 2026-07-10 BUILD 26

BUILD 25の実機確認で、横書きの `池田亮司` / `電子音楽` は正しく読めることを確認。一方、罫線に挟まれた縦書きの `重大` / `出身地` は、枠内画像・文字だけ拡大・高コントラストでも全文OCR候補がなく、1文字候補へ分裂した。診断だけで止めず、罫線対策を実装した。

対応したこと:

- 縦長画像の左右15%ずつを切り、中央70%の文字列を優先する `左右の罫線を避ける` 画像を追加。
- その罫線回避画像から主文字列を抽出し、上から下の文字を横一列へ並べ直す `罫線回避・縦文字再配置` を追加。
- 上記2画像をOCR候補の早い段階に置き、10パターン上限に入るようにした。
- Visionカスタム語へ `重大` / `出身地` を追加。
- BUILD番号を26へ更新。
- Simulator/実機Debugビルド成功。`iphone15proMAX` へインストール・起動成功。

次の確認:

- 同じ罫線つき縦書きの `重大` / `出身地` を再撮影する。
- 診断に `左右の罫線を避ける` / `罫線回避・縦文字再配置` が出るか確認する。
- 全文が読めなければ、その2画像でどの文字までOCR候補へ出たかを見る。

## 2026-07-15 BUILD 27

7/15の軽めタスク「BUILD番号・診断表示など開発者向け表示を公開版で隠す」に対応した。

対応したこと:

- 撮影画面右上の `BUILD 27` 表示をDebug版のみにした。
- 結果画面と失敗画面の画像パターン・OCR候補・辞書照合の診断パネルをDebug版のみにした。
- 公開版の失敗文言から「下の診断で確認」を外し、黄色い枠を合わせて撮り直す案内に変更した。
- 「ほかの候補をえらぶ」は利用者が正しい読みへ進むための機能なので、公開版にも残した。
- BUILD番号を27へ更新。
- Simulator向けReleaseビルド成功。
- Simulator向けDebugビルド成功。
- `iphone15proMAX` 向けの署名ありReleaseビルド成功。
- `iphone15proMAX` へRelease版をインストールし、起動成功。

公開前の最小確認:

- Release版を起動し、右上のBUILD番号が出ないことを確認する。
- 読み成功/失敗の両方で診断パネルが出ないことを確認する。
- 候補がある場合は「ほかの候補をえらぶ」が使えることを確認する。

## 現在の状態

最終ビルド:

- `BUILD 27`（公開版でBUILD番号・診断パネルを非表示。Debug版では開発確認用に維持。Simulator向けDebug/Releaseビルド成功、`iphone15proMAX` 向けReleaseビルド・インストール・起動成功）

最後に確認された状態:

- 撮影枠と選択枠は統一済み
- ズーム・移動はUIScrollViewベース
- 横幅・高さの枠調整あり
- OCR候補は複数照合
- OCR診断パネルを追加済み
- 撮影時のピンチズーム、タップでピント/露出合わせを追加済み
- OCR再試行強化と縦書き補助を追加済み
- カメラ画質改善、シャッター即時反応、「もう一度」上固定、辞書候補順位づけ修正を追加済み
- 部分文字列から自然な複合語を拾い直す辞書補正を追加済み
- 縦文字自動分割、文字だけ切り出し、補助辞書、1文字誤確定抑制を追加済み
- BUILD 18で、OCR前縮小、OCR順次実行、最大8パターン、1画像1試行、早期打ち切りにより固まり対策を追加済み
- BUILD 19で、`学級新聞` 補助辞書/カスタム語と、縦長画像で1文字だけを誤確定しない安全弁を追加済み
- BUILD 20で、縦書きの主文字列を優先して切り出す画像パターン、記号混じり候補で早期終了しない判定、実測語の補助辞書を追加済み
- BUILD 21で、漢字2文字だけの縦書き部分誤検出を早期確定しない判定、長いOCR候補の一部だけを短い語として結果表示しない安全弁、`旧人` の辞書ブロックを追加済み
- BUILD 22で、実測語の末尾1字落ち/誤挿入を補正する辞書前補正、`受動分` / `引照個` / `約聖書` のブロック、長い読みの2行表示対応を追加済み
- BUILD 23で、薄い文字向けの `高コントラスト` / `文字だけ強調` を縦書き分割より先に評価し、自然な複合語候補の照合スコアを強化
- BUILD 24で、辞書照合前の読点などを除去し、実測人名 `池田亮司` / `池田` の補助辞書・Visionカスタム語を追加
- BUILD 25で、辞書照合済みOCR候補を利用者が選んで結果へ進める導線を追加
- 薄い文字認識は未解決。
- 2026-07-10に BUILD 25 の実機Debugビルド、実機インストール成功。候補選択UIの実測待ち。
- 次は `BUILD 25` の実機撮影で、候補ボタンから正しい読みへ切り替えられることを確認する。

ユーザー判断:

実機で失敗サンプルを再撮影して確認する。

## 主要ファイル

- `work/Shirangana/Shirangana/Views/CameraScreen.swift`
- `work/Shirangana/Shirangana/Views/ImageCropScreen.swift`
- `work/Shirangana/Shirangana/Services/TextRecognitionService.swift`
- `work/Shirangana/Shirangana/Services/ReadingDictionary.swift`
- `work/Shirangana/Shirangana/Services/ImagePreparationService.swift`
- `work/Shirangana/Shirangana/Camera/CameraService.swift`
- `work/Shirangana/project.yml`
- `work/Shirangana/AppStore/metadata_ja.md`
- `work/Shirangana/README.md`

## App Storeに向けた残課題

- Apple Developer Programへの加入
- 正式なApp Store署名
- App Store Connect登録
- プライバシー文言の確認
- スクリーンショット作成
- サポートURL・マーケティングURLの準備
- OCR精度の改善
- 薄い文字問題の解決
- 縦書き実データでの検証
- 辞書ライセンス表示の最終確認

## 次回再開時の最初の一手

薄い文字の失敗原因を見える化する。

具体的には、`ここを読む`を押す前後で以下を保存・表示する。

- 実際に切り取った枠内画像
- 高コントラスト補正後画像
- 二値化画像
- Vision OCRの全候補
- 辞書照合に使った候補

これにより、失敗が次のどれかを判定する。

- そもそも枠内画像が違う
- 画像補正で文字が消えている
- Vision OCRが読めていない
- Vision OCRは読めているが辞書照合で落ちている
- 辞書にない語である
