---
種別: 出力仕様 / AI共通
作成日: 2026-08-09
役割: 説教原稿から礼拝投影用の PowerPoint（.pptx）を作るときの様式。[[📖 説教スキル（AI共通）]] の出力側。
正本の由来: 本人が実際に使っている `20250706創世記17-15-27「アブラハムは笑う」.pptx` の書式を読み取ったもの。
更新方針: 本人が新しいPPを渡してきたら、そちらを正本として書き換える。
---

# 🖥 説教スライド出力仕様（AI共通）

礼拝で会衆と**一緒に読む**ための投影スライド。説教のポイントや見出しは載せない。**聖句だけ**を出す。**実機は PowerPoint**（2026-08-09 本人確認）。

---

## 1. 何を載せるか

- **説教原稿に引用されている聖句を、出てくる順に、1箇所＝1枚**。
- 説教本文・見出し・要約・原語解説・図は**載せない**（口で語る）。
- **二度目以降の再引用も、原稿に出てくる順にスライドを作る**（2026-08-16 本人指示で変更）。~~既に出したスライドを使う（重複スライドは作らない）~~ ← 説教は原稿の順に進むので、投影も原稿の順に並べる。同じ聖句が二度出てくれば、スライドも二枚作る。
- 聖書本文は**全文**。`…` で省略しない。
- 本文の文字は**原稿に貼られたものをそのまま**使う。AIが訳し直さない・整えない。

## 2. 1枚に何節まとめるか

- **連続した節は1枚にまとめる**（一緒に読むので、読んでいる途中で画面を切らない）。
- ただし**1枚あたり約11行**を上限とする。超えるときは節の切れ目で次の枚へ送る。
- 目安の計算: 全角25文字＝1行。`ラベル ＋ 全角スペース2つ ＋ 本文` の長さで数える。
- 書名がまたぐとき（例 ホセア→イザヤ）は、続けて読む場面でも**別の枚**にする。

## 3. 書式（サンプルPPから読み取った実測値）

| 項目 | 値 |
|---|---|
| スライドサイズ | 12192000 × 6858000 EMU（16:9・13.333in × 7.5in） |
| 背景 | **黒**（テーマの `clrMap` で `bg1=dk1`） |
| 本文の色 | **白**（`tx1=lt1`） |
| 節ラベルの色 | **白**（2026-08-16 本人指示で変更。**スライドは全部の字を白に統一する**）。~~#006699~~ ← サンプルPPは青だったが、投影では色を分けない。色指定を外してテーマの `tx1=lt1` を継承させる。**紙（Word）側の青 #006699 は変えない** |
| 文字サイズ | **36pt 太字**（レイアウトの `lvl1pPr` 既定。個別指定なし） |
| 欧文フォント | 小塚明朝 Pro L |
| 日本語フォント | Meiryo UI |
| その他 | cs＝ＭＳ Ｐゴシック |
| プレースホルダー | 画面全面（0,0 → 12192000×6858000）／`ph type="body" idx="10"` |
| 段落の左右余白 | `marL="152400" marR="152400"` |
| レイアウト | `slideLayout1.xml` 一枚のみ。全スライドが同じものを使う |

**節ラベルの書き方**: `Mark 2:18` のように**英略記＋半角スペース**。ラベルと本文の間は**全角スペース2つ相当（半角スペース2つ）**。原稿側が「ルカの福音書18:12」と日本語書名で書いていても、スライドでは英略記に統一する（サンプルPPがそうなっているため）。

**段落構造**（1節＝1段落・3つのラン）:

1. ラベル（**色指定なし＝白**。2026-08-16 変更前は 006699）
2. `  `（半角スペース2つ、`xml:space="preserve"` を付ける）
3. 聖書本文（色の指定なし＝白を継承）

最後に空段落 `<a:p><a:endParaRPr .../></a:p>` を1つ置く。

## 4. ファイル名

`YYYYMMDD書名章-開始節-終了節「説教題」.pptx`

例: `20260809マルコ2-18-22「新しい皮袋」.pptx`
（`:` はファイル名に使えないので `-` にする。サンプルPPも同じ形。）

**保存先**: Vaultの `説教準備/`。

## 5. 作り方（手順）

**pptxgenjs で一から作らない。**組み立てはビルダーに任せる（サンプルPPを解体して中身を差し替える方式）。

- ビルダー: `scripts/sermon_slides_build.py`
- テンプレート: `scripts/sermon_slides_template.pptx`（本人が実際に使っている 2025-07-06 のPP）
- Claudeでは **`sermon-slides` スキルが自動で起動**する。中身はこのノートと同じ。

手順は3つ。

1. 原稿から聖句を出現順に拾って `verses.json` を書く。別々の引用箇所の境目に `{"break": true}` を挟む。連続節は break を挟まずに並べる。

```json
[
 {"label": "Mark 2:18", "text": "さて、ヨハネの弟子たちと…"},
 {"break": true},
 {"label": "Is. 58:3", "text": "『なぜあなたは…"},
 {"label": "Is. 58:4", "text": "見よ。…"}
]
```

2. ビルドする。枚ごとの行数と収録箇所が表示される。

```bash
python3 "<Vault>/scripts/sermon_slides_build.py" verses.json "<Vault>/説教準備/YYYYMMDD….pptx"
```

3. 検証する。`scripts/office/validate.py out.pptx --original sermon_slides_template.pptx` → `markitdown` で本文の欠け・並び → PDF化して画像で**あふれを目で見る**。

枚の割り方（11行の上限・break の扱い）はビルダーが持っている。手で数えない。

> [!warning] 検証環境のフォント置換に注意
> サンドボックスの LibreOffice には Meiryo UI が無いので、行の高さが実機と違う。**画像で「入っている」ように見えても、実機の PowerPoint では1〜2行はみ出すことがある**。上限11行はサンプルPPの最大枚（創世記17:23-26＝11行）から取った実測の安全線であって、レンダリング結果からではない。

## 6. 未確認

- サンプルPP（2025-07-06）の聖書本文は**新改訳第3版**（「サライ」「仰せられた」）。2026年以降の原稿は新改訳2017。スライドは**原稿に貼られた本文をそのまま**使うので、版の統一はAIの判断で行わない。

---

## 記録

- 2026-08-09 作成。`20260809マルコ2-18-22「新しい皮袋」.pptx`（16枚）を本仕様で出力。書式はサンプルPPの実測から。
- 2026-08-09 「毎週一発でできるように」との本人指示を受け、ビルダー `scripts/sermon_slides_build.py` とテンプレート `scripts/sermon_slides_template.pptx` を置き、Claude用スキル `sermon-slides` を登録。同日の16枚をビルダー経由で再生成し、同一の割り（11行上限）で validate 通過を確認。**最終確認: 2026-08-09（validate通過・画像目視。実機PowerPointでの表示は未確認）**
