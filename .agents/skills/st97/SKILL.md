---
name: st97
description: 早天礼拝・早天祈祷会のために、指定箇所または4桁の日付から本文を特定し、本文整理、印刷用HTML、PDF、自動検査、全ページ確認まで軽量な固定パイプラインで一気に仕上げる。ユーザーが「st97」「早天」「早天メッセージ」「早天用」「10分の早天説教」と言ったとき、または明示的に$st97を指定したときに使う。通常の詳しい釈義・説教モードset97とは区別する。
---

# st97

1. Vault直下の `🌅 早天メッセージスキル（AI共通）.md` を最初から最後まで読む。
2. 通常は長い釈義・説教スキルを全文読まない。共通仕様の品質ゲートを必ず通し、難所がある場合だけ同仕様に従って詳しい釈義手順へ戻る。
3. 聖書箇所または訳文が与えられたら、固定パイプラインで印刷用HTMLと同内容PDFを作る。自動検査後、全ページcontact sheetを一度確認し、異常が疑われるページだけ原寸で見る。PDFをプレビューで開く。物理印刷は `🖨 印刷先固定（AI共通）.md` を読み、`EPSON EW-052A` と確認できた場合だけ送信する。Wordは明示された場合だけ作る。
4. 読了後、「早天メッセージスキルを起動しました」と一言添える。
5. 以後、その相談が終わるまで共通仕様に従う。
6. ユーザーが `st97` だけを送った場合は、聖書箇所または訳文を待つ。箇所や訳文が同時に与えられた場合は、確認を挟まず第一段階を開始し、印刷用HTML・PDFの生成と全ページ確認まで完了する。
7. `0722` のような4桁の月日（MMDD）が入力された場合は、現在年の `.agents/skills/st97/references/YYYY-MM.md` から当日の本文を特定し、確認を挟まず完成まで進める。月間キャッシュがない場合だけ公式一覧を一度取得して保存する。年をまたぐ場合は `YYYYMMDD` を優先する。
8. ユーザーの明示的な指示が共通仕様と異なる場合は、ユーザーの指示を優先する。
9. 指示がない限り、Vault内の説教ファイルや相談ログを書き換えない。

## 月間のリビングライフ本文を入手する

→ **手順は `🌅 早天メッセージスキル（AI共通）.md` の同名の節へ移設した（2026-07-31）。** このスキルは入口の役だけを持ち、内容は持たない。二重管理になり、片方だけ古くなるため。
