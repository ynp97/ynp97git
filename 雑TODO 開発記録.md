---
種別: アプリ開発記録
アプリ: 雑TODO
更新日: 2026-08-10
---

# 雑TODO 開発記録

## 現在の正本

- ソース: `アプリ本体/ZatsuTodo`
- 実行アプリ: `/Applications/ZatsuTodo.app`
- Bundle ID: `com.ynp97.zatsutodo`
- バージョン: 2.5 / build 9
- データ: `~/Library/Application Support/ZatsuTodo/todos.json`
- 自動バックアップ: `~/Library/Application Support/ZatsuTodo/todos.backup.json`
- 2026-07-27修正前バックアップ: `~/Library/Application Support/ZatsuTodo/todos.pre-fix-20260727.json`
- ログイン起動: `~/Library/LaunchAgents/com.ynp97.zatsutodo.plist`

## 2026-08-10 TODO決定版 2.0

### 目的

雑TODOの投入しやすさを残しつつ、開いたら今日・今週・今月の順にやることが見え、ルーティーンとプールから選んだ今日の項目を区別できる一つのTODOアプリにする。

### 実装

- 起動時の正面を「今日」に固定し、今日→今週→今月のカードを縦に配置。
- 今日のカード内を「ルーティーン」と「今日に選んだもの・今日までのもの」に分離。
- 雑プール、ジャンル、期日、自由な目処、今日／今週／今月への移動を実装。
- 毎日と毎週（曜日指定）のルーティーンを実装。当日分の完了だけを記録し、次の該当日には再表示する。
- 旧JSONに新しい項目が無くても読める移行処理を追加。
- 旧目処の「今日中・今週中・今月中」だけを三段表示へ反映。長期目処は雑プールに残す。
- 三本立ての固定長期TODOは取り込まず、画面構成だけを参照。

### 確認

- Releaseビルド、署名、Info.plist検査に成功。
- `/Applications/ZatsuTodo.app` を2.0へ更新し、タイトルが「TODO」になることを確認。
- 旧データ76件を保持。旧「今日中」3件と「今週中」の項目が該当カードへ出ることを実機確認。
- 確認用の毎日ルーティーンを追加し、ルーティーン枠への表示と完了カウント `1 / 4` を確認。
- 確認用項目は除去し、保存データを76件へ戻した。
- 更新前バックアップ: `todos.before-v2-20260810.json`。

## 2026-08-10 TODO決定版 2.1

- 二段に重複して見えていたセグメント式タブを整理。画面切替は今日＝橙、雑プール＝灰、ルーティーン＝紫の独立ボタンに変更。
- 入力欄は通常、Enterで雑プールへ入れるだけにした。追加先・ジャンル・期日・ルーティーンは「追加設定」を開いたときだけ出す。
- 毎週ルーティーンは月〜日の丸ボタンを複数選択できる。旧 `weeklyDay` も新 `weeklyDays` へ読み込み時に移行する。
- `plannedDate` を追加。明日指定は翌日になると自動的に今日のカードへ上がる。各TODOの移動メニューにも「明日」を追加。
- 実画面で月・金の同時選択、タブの色分け、追加設定の開閉を確認。
- 日付判定を8月10日→8月11日で検査し、明日指定が当日は今週、翌日は今日になることを確認。
- 本人が登録済みの80件（ルーティーン7件）を変更せず保持。

## 2026-08-10 TODO決定版 2.2

- ジャンル札を固定色へ変更。仕事＝青、学校＝水色、教会＝橙、音楽＝紫、ポケカ＝赤、私用＝緑、未分類＝灰。
- 実画面で教会・学校・私用がそれぞれ別色になることを確認。

## 2026-08-10 TODO決定版 2.3

- 私用ジャンルを緑から茶色へ変更し、完了チェックの緑との重複を解消。実画面確認済み。

## 2026-08-11 TODO決定版 2.4

- 原因: 完了キーは `2026-08-10` として正しく保存されていたが、アプリを開いたまま深夜をまたぐとSwiftUIの再描画契機がなく、8月10日のチェック表示が残っていた。
- 30秒タイマーとアプリ再アクティブ通知で現在日時を更新し、当日のルーティーンと完了判定を再描画するよう修正。
- 更新後は8月11日火曜日、毎日ルーティーン6件が全て未チェックの `今日 0/6`。月曜限定ルーティーンは非表示。実画面確認済み。

## 2026-08-11 TODO決定版 2.5

- ジャンル色を `genre-colors-v1` として保存し、自由追加したジャンルにも任意色を保持できるようにした。
- 設定画面で新規ジャンルの色を選択可能。追加後も各行の色ボタンから変更可能。
- 既存7ジャンルの色は初回起動時に保存色へ移行。設定画面の表示を実機確認済み。

## 2026-07-27 フリーズ・強制終了不能の修正

### 本来の目的

雑TODOを普通に触っても固まらず、Macを再起動せずに終了でき、普段どおりTODOを追加・整理できる状態にする。

### 確認した事実

- フリーズ時の保存データは26件、JSONは正常。
- 実プロセスは `~/openclaw-apps/ZatsuTodo/.build/debug/ZatsuTodo`。
- LaunchAgentは `KeepAlive=true` で、強制終了しても同じ開発用プログラムを即再起動する構成だった。
- 7月25日に「ホバー時のタグ全展開による無限ループ」を固定高で直した記録があったが、実機では再発した。

### 実施した修正

1. 正本を `アプリ本体/ZatsuTodo` へ受け取った。
2. ホバー中に11個の目処タグを横展開するUIを廃止し、各行のクリック式メニューへ変更した。
3. 保存処理の手動 `objectWillChange` と全体アニメーションを外し、不要な再描画を減らした。
4. JSON保存時に `todos.backup.json` も更新するようにした。
5. `KeepAlive` を廃止した。
6. LaunchAgentは正式版 `/Applications/ZatsuTodo.app` をログイン時に1回開くだけにした。
7. 通常アプリとしてDockと強制終了から扱える構成にし、画面内「終了」と `⌘Q` を追加した。
8. 1.1 / build 2をReleaseビルドし、アドホック署名して `/Applications` へ反映した。

### 実機検証

- Releaseビルド成功
- plist検査・署名検査成功
- 正式アプリからの起動を確認
- 既存26件を表示
- タグメニュー開閉成功
- 確認用TODOを追加し、27件になって保存されたことを確認
- 確認用TODOを削除し、26件へ戻ったことを確認
- 画面内「終了」で終了し、2秒後も自動再起動しないことを確認
- 再起動後、`⌘Q`で終了し、2秒後も自動再起動しないことを確認
- 最後にアプリを再起動して使用可能な状態に戻した

## 2026-07-28 アイコン作成・Dock追加

### 経緯

雑TODOがDockから見当たらないとの相談。確認したところ、アプリ自体は起動できる状態だったが、Dockには登録されておらず、独自アイコンも未設定（Info.plistに`CFBundleIconFile`なし）だった。

### 実施内容

- アイコン: オレンジ地の角丸スクエアに黒文字で「雑」（本人指定「オレンジに、黒で雑と入れよう」）。`アプリ本体/ZatsuTodo/AppIcon.icns` / `AppIcon_1024.png` として保存。
- `Info.plist`に`CFBundleIconFile=AppIcon`を追加（ソース側・`/Applications/ZatsuTodo.app`側の両方）。
- `build_app.sh`を修正し、以後のビルドでも`Contents/Resources/AppIcon.icns`を自動コピーするようにした（今後`install_update.sh`経由で再ビルドしてもアイコンが消えない）。
- `アプリ本体/ZatsuTodo/セットアップ_アイコンとDock追加.command`を新規作成。ダブルクリックで①`/Applications/ZatsuTodo.app`へアイコン反映②Info.plist更新③Launch Services/Dockキャッシュ更新④Dockへ登録、まで一括実行。
- 実機で実行し、Dockにオレンジ地に黒い「雑」アイコンが追加されたことを確認済み。

### 注意

- `icon_candidate_1024.png`（オレンジ地・白いチェックリスト柄）は別セッションでの試作候補。本人が「オレンジに黒で雑」と明示指定したため不採用（削除はせず残置、必要なら後で消す）。
- Dock登録は`defaults write com.apple.dock persistent-apps`方式。今後Dockから外れた場合は同じ`.command`を再実行すれば復旧する。

## 再発時

- Macを再起動する前に、画面内「終了」または `⌘Q` を使う。
- 強制終了後に勝手に戻る場合は、LaunchAgentへ `KeepAlive` が再導入されていないか確認する。
- タグ操作で固まる場合も、ホバー式の全タグ展開へ戻さない。
- 既存の修正を継ぎ足す前に、発生直前の操作、プロセス、保存JSONの正常性を分けて確認する。

## 2026-07-27 1.1でもフリーズ再発 → 1.2で画面設計を全面変更

### 1.1の見立ての訂正

1.1で `KeepAlive` による強制終了後の即再起動は解決した。しかし、ユーザー実機で「開いて触っただけ」で再びフリーズしたため、「ホバー時のタグ全展開だけがフリーズ原因」という見立ては撤回した。

### macOSハング記録で確定した根本原因

- Version 1.1 / build 2が98秒間応答不能。
- メインスレッドはSwiftUIの `AttributeGraph` 更新から抜けられていなかった。
- 重いスタックは `LazySubviewPlacements`、`LazyStack.place`、`PointerRegionUpdater`。
- したがって、データ26件やJSON保存ではなく、次の組み合わせによるSwiftUIレイアウト更新ループが根本原因。
  - カスタム浮遊 `NSPanel`
  - 展開／縮小のたびに `NSHostingController.rootView` を差し替える構造
  - `LazyVStack`
  - 各行に多数あるホバー・ボタン・メニュー等のポインター領域

### 1.2で捨てたもの

- 浮遊バブル
- カスタム `NSPanel`
- ウインドウのアニメーション拡大／縮小
- rootViewの動的差し替え
- `LazyVStack`
- `onHover` 状態
- ホバーによる見た目・機能の切替

### 1.2の新構成

- 通常のSwiftUI `WindowGroup`
- 通常のMacウインドウ
- `ScrollView + VStack` の固定構造
- タグ、期限、サイズはクリック式メニュー
- 既存の入力、編集、完了、削除、3種の表示順、タグ編集を維持
- 既存JSON形式を変えず、26件をそのまま読み込み

### 1.2実機検証

- Releaseビルド・署名・インストール成功。
- 既存26件を表示。
- タグメニューを12回連続で開閉。
- まとめ／期限順／目処順を計5回切替。
- 上下へ計10回連続スクロール。
- 確認用TODOを追加して27件、削除して26件へ戻した。
- 全操作後も応答あり、CPU 0%。
- 2秒間のプロセス採取ではメインスレッドは正常なイベント待ち。旧版の `LazySubviewPlacements`／`PointerRegionUpdater` 更新ループなし。
