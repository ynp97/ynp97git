---
種別: アプリ説明書 / Chord Bridge
対象: Chord Bridge 0.9.2 (build 12)
作成日: 2026-07-24
関連: [[🧩 アプリ開発状況（AI共通）]] の「S. Chord Bridge」
---

# 🎹 Chord Bridge 説明書

CubaseとLogic Proの間で、コード進行・MIDI・オーディオを往復させるための受け渡しアプリ。

このアプリは**演奏や音は作らない**。CubaseとLogicの画面を自動で操作して、データを渡しやすくするだけ。実際に鳴らすのはLogicのSession Player、まとめるのはCubase。Chord Bridgeは「橋」。

---

## いちばん大事な前提（ここでつまずきやすい）

**LogicのリージョンはそのままChord Bridgeへドラッグできない。** Logicの中の「リージョン」はファイルではないため。必ず一度**MIDIまたはオーディオとして書き出してから**Chord Bridgeへ渡す。この書き出しを助けるのがChord Bridgeの「MIDIで受け取る／オーディオで受け取る」ボタン。

Chord Bridgeは**Cubaseとuse Logicの画面を自動でクリックして操作する**。そのため、

- CubaseとLogicが**起動していて画面に出ている**必要がある。
- 途中で別のウインドウをクリックしたり、キーを触ったりすると、自動操作がずれて失敗する。**ボタンを押したら手を離して待つ。**
- 動かないときは、たいてい後述の「アクセシビリティ許可」が切れている。

---

## 全体の流れ（5ステップ）

```text
① Cubaseでコード進行を作る
      ↓  Chord Bridge「①選んだトラックのコードを一つにまとめる」
② コードが1つのMIDIパートにまとまる → Logicへ渡す
      ↓  Logicで鳴らす（Session Player）
③ Session Playerに演奏させる
      ↓  Chord Bridge「MIDIで受け取る」or「オーディオで受け取る」
④ Logicから演奏を書き出す
      ↓  Chord Bridgeの受渡カードに読み込む
⑤ Cubaseへドラッグして戻す → ミックス
```

---

## 画面の見かた

Chord Bridgeを開くと、上から順に次のボタンがある。

**Cubase → Logic ｜ まずコードをまとめる**

- `① 選んだトラックのコードを一つにまとめる`
  Cubaseで横に並んでいるコードのトラックを、順番・位置・長さを保ったまま**1つのMIDIパート**にまとめる。

**Logic → Cubase ｜ 自動演奏を受け取る**

- `MIDIで受け取る`
  LogicのSession Playerの演奏を**MIDIノート**として書き出す。
- `オーディオで受け取る（WAV／AIFF）`
  Logicの音色・エフェクトを含めた**音そのもの**を書き出す。

**MIDI／オーディオをここへドロップ**（青い枠）

- 書き出したMIDI・WAV・AIFFファイルをここへドロップすると、受渡カードに読み込まれる。

**いちばん下の操作ボタン**

- `Logic Proを開く` … Logicを前に出す。
- `ファイルを選ぶ…` … ドロップの代わりにファイルを選んで読み込む。
- `ファイルをコピー` … 読み込んだファイルを複製する。
- `Finderに表示` … 読み込んだファイルの場所をFinderで開く。

---

## 使い方（手順）

### A. Cubaseのコードをまとめて、Logicへ渡す

1. Cubaseで、コード進行が入ったトラックを選ぶ。
2. Chord Bridgeの `① 選んだトラックのコードを一つにまとめる` を押す。**押したら手を離して待つ。**
3. コードが1つのMIDIパートにまとまる。
4. そのMIDIをLogicへ持っていき、鳴らしたい楽器（ベースやピアノなど）のトラックに置く。

### B. LogicのSession Playerに演奏させる

1. Logicで、まとめたコードのトラックにSession Playerを設定して演奏させる。
2. 気に入った演奏ができたら、そのSession Playerの**リージョンを1つだけ選択**する。

### C. Logicの演奏をChord Bridgeで受け取る

**MIDIノートで欲しいとき（音は自分で差し替えたい）**

1. Logicで受け取りたいSession Playerリージョンを**1つ選ぶ**。
2. Chord Bridgeの `MIDIで受け取る` を押す。**押したら手を離して待つ。**
   - Chord Bridgeが、選んだリージョンの位置を覚えてからMIDIに変換し、そのリージョンだけを書き出す。
3. デスクトップなどにMIDIファイルが書き出される。

**オーディオで欲しいとき（Logicの音そのままが欲しい）**

1. Logicで書き出したい範囲・リージョンを選ぶ。
2. Chord Bridgeの `オーディオで受け取る（WAV／AIFF）` を押す。
3. 書き出し画面が開くので、WAVまたはAIFFで書き出す。

### D. Cubaseへ戻す

1. 書き出したファイルをChord Bridgeの青い枠へドロップする（または `ファイルを選ぶ…`）。
2. 読み込まれたファイルを、Cubaseの戻したいトラックへドラッグする。
3. Cubaseでミックスする。

---

## うまくいかないとき（チェックリスト）

**「MIDIで受け取る」を押したのに、違うMIDIが書き出される**

- 受け取る前に、Logicで**目的のSession Playerリージョンが選択されているか**確認する。
- 0.9.1の古い版では「まとめたコードMIDI（Amin7など）」が書き出される不具合があった。**0.9.2で修正済み。** もしまだ起きる場合は、下の「バージョン確認」を見る。

**ボタンを押しても何も起きない／途中で止まる**

1. CubaseとLogicが**両方とも起動して画面に出ているか**。
2. ボタンを押したあと、**別のウインドウをクリックしていないか**（自動操作がずれる）。
3. **アクセシビリティ許可**が切れていないか（下記）。これがいちばん多い原因。

**アクセシビリティ許可の直し方（重要）**

Chord Bridgeは「アドホック署名」のため、**アプリを作り直す（再ビルドする）たびに許可がリセットされる。** スイッチがオンに見えても、それは古い版の登録が残っているだけで実際には効いていないことがある。オン・オフを切り替えるだけでは直らない。

正しい直し方:

1. アップルメニュー →「システム設定」を開く。
2. 「プライバシーとセキュリティ」→「アクセシビリティ」。
3. 一覧の中の **Chord Bridge を選んで「−」で削除する。**
4. 「＋」を押して、**今使っているデスクトップの Chord Bridge.app**（`デスクトップ/Chord Bridge.app`）を追加する。
5. スイッチをオンにする。
6. Chord Bridge を**完全に終了して、開き直す。**

**バージョン確認のしかた**

1. Finderでデスクトップの `Chord Bridge.app` を1回クリックして選ぶ。
2. `command + I`（情報を見る）を押す。
3. 「バージョン」が **0.9.2** ならOK。古ければ、最新ビルド（`アプリ本体/ChordBridgeMac/build/Chord Bridge.app`）をデスクトップへ上書きコピーする。

---

## 覚えておくこと（3行）

1. **Logicのリージョンは直接ドラッグできない**。必ずボタンで書き出してから渡す。
2. **ボタンを押したら手を離して待つ**。自動操作の途中で画面を触らない。
3. **動かないときはアクセシビリティ許可**。削除して追加し直す。
