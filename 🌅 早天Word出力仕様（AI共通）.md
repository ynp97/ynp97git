---
種別: 早天Word出力仕様 / AI共通
作成日: 2026-07-18
更新日: 2026-09-15
参照条件: st97で聖書箇所または訳文が与えられた場合。Word不要の明示がある場合のみ省略
---

# 🌅 早天Word出力仕様（AI共通）

> [!note] **早天のファイル名に `草稿` は付けない**（2026-09-12 本人確定）。AIが書いたものの頭に `草稿` を付ける規則（[[📄 説教原稿Word出力仕様（AI共通）]]§5）は**主日説教だけ**が対象で、早天は従来どおりの名前でよい。

## 目的

確定した早天ガイドを、ライブで必要箇所をすぐ見つけられるWord書類にする。内容を確定してから一回生成し、全ページを一回確認する。

## 書式は共通仕様に従う

用紙・余白・フォント・文字サイズ・聖書本文の色分け・生成と検証の手順・品質基準は、すべて **`📄 Word出力仕様（AI共通）.md`（全モード共通の正本）** に従う。ここでは早天固有の点だけを補う。

## 早天固有の点

- 内容の順序は第一段階の順「全体の流れと結論 → 段落の簡単なまとめ → 各節の本文 → 直下の背景・語句 → 段落ポイント → 全体のまとめ → 五段階の適用」を保つ。
- 早天は当日釈義する本文が中心。各節は「節番号だけ青 #006699＋本文テキストは黒太字、一段字下げ」で置く。他箇所を引用ブロックで挟むときだけオレンジ #ED7D31 にする（色分け・字下げの詳細は共通仕様を参照）。

## 生成（2026-09-04 追加）

**手で組み立てない。固定の道具がある。**

```
python3 scripts/st97_docx_build.py ".agents/skills/st97/tmp/<当日のpayload>.json"
```

- 入力は **印刷用HTMLと同じペイロードJSON**。したがって**Wordの内容と順序はHTML／PDFと必ず一致する**（別々に書き起こさないので、片方だけ古くなることが起きない）。
- 出力は `output/<payload名>_早天メッセージ.docx`。
- 順序は render_st97.py と同じ:表題 → 全体の流れと結論 → 段落の簡単なまとめ → 各節の本文 → 直下の背景・語句 → 段落のポイント → 全体のまとめ → 五段階の適用。
- 色分け・字下げ・用紙・余白・フォントは `scripts/docx_style.py` と `scripts/vault_docx_template.docx` が持つ（[[📄 Word出力仕様（AI共通）]] の実装）。**set97の釈義Word化と同じ土台を共有している。**
- **ペイロードの `ruby` は Word のルビとして入る**（HTMLの `<ruby>` と同じ内容）。ライブで読むときに効く。
- 作ったら PDF に変換して全ページを一度確認する（`soffice --headless --convert-to pdf` → `pdftoppm`）。

**書式を直したいときは、出来上がった .docx ではなく `scripts/docx_style.py` かテンプレートを直す。**

## 第二段階（10分メッセージ原稿）の紙は説教原稿ビルダーで作る（2026-09-15 確立）

**第一段階の本文整理（`st97_docx_build.py`）とは別系統。** 口頭で読む10分メッセージの原稿は、主日説教と同じ土台で組む。

```
python3 scripts/sermon_docx_build.py <manuscript.json> "output/YYYYMMDD書名章-節 「説教題」（早天10分）.docx"
```

- スタイルは `説教題` / `メッセージ本文` / `メッセージ聖書個所`（当日の中心テキスト）/ `引用聖書箇所`（他箇所）。改行と改ページはビルダーが持つ。
- **節ラベルは日本語書名のまま**（英略記は主日説教原稿だけ。[[📄 説教原稿Word出力仕様（AI共通）]] §4の但し書き）。
- **`草稿` は付けない**（早天は対象外。このファイル冒頭の注記）。保存先は `output/`。
- **分量の目安＝310字／分**（実測：2026-09-15の12分版が3,718字）。10分なら約3,000字。聖句の字数も数に入れる。
- **PDFはクラウド側で `soffice --headless --convert-to pdf`。** device_bash側のLinux VMでも soffice は動くが、どちらにも Meiryo UI と小塚明朝 Pro L が無いので**代替フォントで出る**（クラウド側は fontconfig で Noto Sans CJK JP を当てる。→ [[🌅 早天メッセージスキル（AI共通）]] と同じ font の落とし穴）。**実機Wordの見え方どおりに印刷したいときは、.docx をMacのWordから印刷する。** DOCX内部のフォント指定は変えない。
