---
種別: 運用ルール / AI共通
作成日: 2026-07-31
役割: 13モードそれぞれで「何を読み、どう進め、終了時に何を更新するか」の正本。`CLAUDE.md` / `AGENTS.md` から分離した（両方に同じ内容を二重に書くと、片方だけ古くなるため）。
---

# 🚦 モード別の読み方（AI共通）

> [!important] 使い方
> - 合図は前後の空白を除いて**完全一致**で判定し、その相談が終わるまでそのモードを継続する。
> - 該当モードの節**だけ**読む。ほかのモードの節は読まない。
> - **AIによって違う箇所には「Claudeの場合／Codexの場合」と明記してある。** 自分に当たる方に従う。
> - 行き先が分からない話題は [[🗂 話題別インデックス（AI共通）]] を引く。

## ① 戦略モード（`ynp97`）

読むものと進め方は `CLAUDE.md` / `AGENTS.md` の「戦略モードで必ず読む」節が正本。読んだら一言「ブリーフィングを読みました」と添える。会話の終わりに**書き戻しプロトコル**を実行する。

## ② 軽量モード（合図がないとき全部）

人生・信仰・創作・事業の相談でも、合図がなければこちら。**起動時の4ファイルは読まない。** 手早く対応し、トークンを使わない。相談ログへの追記もしない。

- **記録トリガー（例外）**: 「**記録して**」「ログして」等と言われたら、要点を [[🗒 相談ログ（追記式）]] に追記する。重要な決定・気づきに発展したと判断したら、**「これは記録しておきますか？」と一言だけ**確認してよい（勝手に長文を書かない）。

## ③ アプリ制作モード（`app97`）

戦略の4ファイルは**読まない**。コード作業に直行するためのモード。読んだら一言「アプリ台帳を読みました」と添える。

**読む順（2026-07-31改定）**

1. **対象アプリが決まっている** → `アプリ引き継ぎ/` のその**単体ファイル1本だけ**を読む。[[🧩 アプリ開発状況（AI共通）]] は開かない。
   - `シランガナ` / `Shirangana` / `しらんがな` → **正本は [[🧩 シランガナ引き継ぎ（AI共通）]]**（`アプリ引き継ぎ/C シランガナ.md` は補助）
2. **対象が未定・横断相談** → [[🧩 アプリ開発状況（AI共通）]] の**索引表だけ**を読む（約5,200文字）。そこから該当する単体ファイルへ進む。
3. 実体コード・開発記録は、必要になった分だけ読む。
4. 仕様の確認が要るときだけ [[🔭 検討中プロジェクト（オープンな問い）]] の**該当節のみ**。全文は読まない。

**進め方**: 構想を広げるより、**動くコード・直ったバグ・出せるビルド**を残すことに収束させる。「できました」と言うときは原則5の2行（`やったこと` ＋ `確かめ方（あなたの手で）`）を必ず添える。

**終了時**: 触ったアプリの**単体ファイルを更新する**。[[🧩 アプリ開発状況（AI共通）]] は索引なので、**行が増減したときだけ**直す。**詳細を索引へ書き戻さない。** 大きな方針変更があったときだけ相談ログにも1行残す。

> [!important] `ai97` との境界（2026-07-31確定）
> - **DSに実装させる話**（作業票を書く／成果物を検品する／バグを直す）＝ **`app97`**
> - **DSの設定・費用・モデル選定・環境構築・AI間の役割分担** ＝ **`ai97`**
> - 迷ったら、**成果物がコードなら `app97`**。

> [!warning] 引き継ぎファイルに必ず残すもの
> **★踏んではいけない地雷** ── 一度踏んだ失敗と、その理由。消さない。
> 実例＝0バイトのファイルを「Saved」と表示していた（screen-recorder）／日付形式を確認せず `##` 見出しを捨てて162行が消えた（日記アプリ）／設計図に日本語で書いたフォルダ名を、DSが自分のfixtureに合わせて英語で実装した（同）。
> **理由が書いてあるコードや記述を「読みにくいから」で削らない。**

## ④ 説教モード（`set97`）

戦略・アプリ系のファイルは**読まない**。釈義→説教の流れと本人の文体だけをそろえる。読んだら一言「釈義・説教スキルを起動しました」と添える。

1. **[[📖 釈義スキル（AI共通）]] と [[📖 説教スキル（AI共通）]] を対で起動する。** 流れは「釈義（本文の確定）→説教（メッセージ化）」。**解釈上の問いが出たら [[📖 構造分解スキル（AI共通）]] へ。**
   - **Claude / Cowork の場合**: exegesis スキルが自動起動する。
   - **Codex など、スキルを持たないAIの場合**: 自動起動しないので、**必ずこの2ファイルを読んで**その指針に従う。`📖 釈義スキル（AI共通）.md` は exegesis スキルの正本コピー。
2. [[📌 事実と前提（AI参照用）]] のうち、**文体・振る舞いの注意のみ**参照（本人の詩的表現を引用しない等）。事業・人物の事実部分は読み込まない。

**要点**: 本文に根のない主張（読み込み）は指摘する／忖度せず、懸念は結論を出す前に必ず述べる（最終判断は本人）／「節ごとに」と言われたら一節ずつ仕上げ、**指示があるまでファイルは書き換えない。**

> [!important] ★`set97` の入り口 ── **箇所を聞く。決めるのはAIではない**（本人指示・2026-08-17。旧「何をやるかを聞き返さない」を差し替え）
> **`set97` と言われたら、最初にこれだけを言う。**
>
> > **今日はどこの箇所を釈義しますか。**
>
> - **返事が来るまで、ファイルを読まない。** 進行表も相談ログも開かない。合図は「これから説教の作業をする」という宣言であって、箇所の指定ではない。
> - **箇所を受けたら、着手の前にVaultを探す。** 先行して作ってあるものがあるかないかを確認し、**あった／なかったを報告してから**進む。探す先＝`釈義/` ／ `説教準備/` ／ `アーカイブ/説教/`（同一箇所・並行箇所）／ [[📖 説教箇所の進行表（AI共通）]] の照合表（✅／❌）。
> - **進行表は参考であって、決定者ではない。** 「次はここのはず」は言ってよいが、それを根拠に着手しない。
> - 箇所が決まったあとは、**選択肢を出して止まらない**（「原稿ですか、スライドですか」等）。旧規則のこの部分は生きている。
> - 原則4（高負荷警告）の対象にしない（`st97`・`tr97` と同じ扱い）。
> - 変わらないもの＝**成果物ファイルは指示があるまで書き換えない**（叩き台はチャットに出す）／懸念は結論の前に述べる。
>
> **由来（2026-08-17）**: 本人が `set97` とだけ打った段階で、Claudeが進行表と**自分が同じ日に書いた相談ログの引き継ぎ**を根拠に「マルコ3:1–6」と決め、一言も出さずに二十回近くファイルを読み進めた。本人は箇所を言うつもりで合図を出していた。**自分で書いたメモを翌スレッドで権威として引き、「規則に従った」と称した**のが問題の芯。旧規則（2026-08-15）は「選択肢を出しすぎて止まる」のを防ぐためのものだったが、**箇所そのものをAIが決める運用**にまで広がっていた。**止めたかったのは足踏みであって、本人が行き先を決めることではない。**

> [!important] ★**終了時**（2026-08-17 新設。12モードで④だけ欠けていた）
> **「ファイルは指示があるまで書き換えない」は成果物ファイルの話であって、記録ファイルは対象外。** 二つを分ける。
>
> | | 例 | 扱い |
> |---|---|---|
> | **成果物ファイル** | `釈義/` `説教準備/` の .md / .docx / .pptx、叩き台 | **指示があるまで書き換えない**（従来どおり。叩き台はチャットに出す） |
> | **記録ファイル** | [[🗒 相談ログ（追記式）]] ／ [[📖 説教箇所の進行表（AI共通）]] | **終了時に必ず書く。指示を待たない。** |
>
> **㋐ [[📖 説教箇所の進行表（AI共通）]]** ── 説教が済んだ行を「済」にし、次の行を足す（同ファイルの更新方針どおり）。「現在地」の3行も直す。**釈義を直したら照合表（✅／❌）も動かす。**
>
> **㋑ [[🗒 相談ログ（追記式）]]** ── 先頭に1ブロック。**持ち越しには必ずこの一行を入れる。**
>
> > **次回は〈箇所〉。釈義は〈済／❌不一致あり＝照合から〉。**
>
> **★この一行が、このモードの生命線。** 進行表は「次はどこか」は教えるが「まず釈義からか」は言わない。**この行が無いと、次のAIは進行表だけを見て成果物（説教叩き台）の側から入り、❌の釈義の上に説教を積む。**
>
> **由来（2026-08-17）**: 8/09のブロック末尾に「次回8/16はマルコ2:23–28（**釈義ファイルに本文の誤りあり。要照合**）」の一行があったため、8/10が釈義照合から始まり、叩き台は5日後の8/15になった（成功例）。**一方 8/10〜8/16 に set97 のブロックが1本も書かれず**、マルコ3:1–6 に同じ一行が無かったため、8/17のClaudeは釈義を飛ばして説教叩き台（約6,900字）を書いた。**釈義が❌だと自分で確かめた直後に、その上へ積んでいる。**
>
> **★釈義を凍結してから説教に入る**（2026-09-04 差し替え。旧「釈義の日と説教の日は、できれば分ける」）
>
> **説教の叩き台に入る前に、釈義ファイルを保存し、「本稿の来歴」節を書き終える。以後、説教作業の中で釈義ファイルを書き換えない。** どうしても直す必要が出たら、来歴節に日付つきで「説教作業中に発見して直した」と明記する。
>
> **守っているもの**: 説教の結論が先に出ると、釈義がその結論を支える形へ静かに寄っていく（AIの癖）。しかも書き換えられた釈義ファイルは完成品として整合しているので、**あとから順序の逆転を検出できない。**
>
> **なぜ日付ではなく凍結にしたか（2026-09-04・本人と検討）**: 旧規則は「日を分ける」だったが、①翌日のセッションでも釈義ファイルを開いたまま説教を書けば同じことが起きる、②実際に寄りを防いでいるのは①〜④の照合手順と相談ログの持ち越し一行のほうで、どちらも機械的に確認できる、③日付規則は守ったかどうかを後から検証できない、④土曜に箇所が決まった週はコストが読めない。**凍結ならファイルに証拠が残り、同じセッションで通しても構わなくなる。**

## ⑤ 早天メッセージモード（`st97`）

戦略・アプリ・音楽制作系ファイルは**読まない**。読んだら一言「早天メッセージスキルを起動しました」と添える。

**[[🌅 早天メッセージスキル（AI共通）]] を最初から最後まで読み、そこに書いてある手順に従う。これが正本。** 完成条件・固定パイプライン・品質ゲート・五段階の適用・PDF確認基準・月間本文の取得手順は、すべてそのファイルにある。**ここには重ねて書かない**（2026-07-31に三重化を解消）。

このモードだけの補足は2つ。

- **通常の `st97` 一件では、続行確認を挟まない。** HTML・PDF生成を含むという理由だけで原則4（高負荷警告）の対象にしない。
- **Wordは明示されたときだけ作る。** その場合は [[📄 Word出力仕様（AI共通）]]（全モード共通の正本）と [[🌅 早天Word出力仕様（AI共通）]] を読む。
- **指示があるまで説教ファイル・相談ログを書き換えない。**

## ⑥ 音楽制作環境モード（`q97`）

Cubase、Logic、DAW、MIDI、オーディオ、プラグイン、音楽制作に関するMac/PC操作・周辺機器接続を扱う。戦略・アプリ・説教系ファイルは**読まない**。

[[🎛 音楽制作環境引き継ぎ（AI共通）]] を読み、索引から該当する個別記録だけを参照する。操作中は**推測で設定値を断定せず**、スクリーンショットや実機表示を優先する。終了時は、再利用できる確定設定・解決手順・つまずき・未解決を同ファイルへ更新する。

## ⑦ AI関連モード（`ai97`）

Claude、Codex、DeepSeek、Gemini、ローカルAI、外部API、OpenClaw、Obsidian連携、AI間の役割分担・記憶・費用・安全性まで、AI関連全体を扱う。戦略の4ファイルは**読まない**。読んだら「AI関連モードを起動しました」と添える。

`.agents/skills/ai97/SKILL.md` に従い、最初に [[🤖 AI関連引き継ぎ（AI共通）]] **だけ**を読む。そこから相談に関係する正本の該当節だけ参照する。過去経緯が必要な場合だけ相談ログを固有名詞で検索する。終了時は、変更内容に応じて [[🗂 話題別インデックス（AI共通）]] ／ [[🤖 Obsidian×ローカルAI引き継ぎ（AI共通）]] ／ [[🔌 外部AI連携（起動プロンプトと記憶プロトコル）]] の**該当正本だけ**を更新する。

**ローカルAIの位置づけ**: 継続して育てる本線の一つだが、目的はローカル化そのものではなく、**費用を気にせずAIを実用に使うこと**。ローカルと外部APIを分断せず、目的に応じて役割分担する。

## ⑧ 動画編集モード（`fcp97`）

Final Cut Proを中心に、早天・賛美の動画編集、Silenci、Motion、タイトル、書き出し、公開前確認を扱う。戦略・アプリ・説教・音楽制作・AI関連の台帳は**読まない**。読んだら「動画編集モードを起動しました」と添える。

最初に [[🎬 動画編集引き継ぎ（AI共通）]] だけを読み、現在地・固定ワークフロー・未解決を確認する。キー操作や症状別の対処が必要なときだけ [[🎬 FCP早見メモ]] の該当節を見る。操作中は画面状態を**推測で断定せず**、スクリーンショットや実機表示を優先し、**一度に一操作ずつ**進める。終了時は、実機で成功した操作・失敗した手順・確定設定・次回の一手を動画編集引き継ぎへ、細かい操作知識はFCP早見メモへ反映する。

## ⑨ 英訳モード（`tr97`）

戦略・アプリ・説教・音楽制作・AI関連・動画編集の台帳は**読まない**。読んだら一言「英訳モードを起動しました」と添える。

**[[📝 英訳スキル（AI共通）]] を読み、そこに書いてある指示本文に従う。これが正本。** 指示の全文・運用上の確認事項・ESVの扱いは、すべてそのファイルにある。**ここには重ねて書かない。**

- **Claude / Cowork の場合**: `message-translate` スキルが起動する。中身は上記ファイルと同一。
- **Codex など、スキルを持たないAIの場合**: 自動起動しないので、**必ず上記ファイルを読んで**その指針に従う。

このモードだけの補足は3つ。

- **合図は一度でよい。** `tr97` と言われたら、その相談が終わるまで継続する。以後は日本語文が貼られるたびに、確認や前置きを挟まず英訳本文だけを返す。「英訳しますか」と聞き返さない。
- **原則4（高負荷警告）の対象にしない。** 長文でも、翻訳という理由だけで続行確認を挟まない。
- **記録を残さない。** 相談ログ・事実と前提・検討中プロジェクトへの書き戻しは行わない（②軽量モードと同じ扱い）。ただし訳語の方針・禁止表現など次回も効く決めごとが出たら、[[📝 英訳スキル（AI共通）]] の「補足」へ1行追記する（原則9）。

## ⑩ 物販・オークションモード（`auc97`）

ヤフオク・eBay・Discogs・ポケカ・古物商まわりの**実務**を扱う。戦略・アプリ・説教・音楽制作・AI関連・動画編集の台帳は**読まない**。読んだら一言「物販モードを起動しました」と添える。

最初に [[💴 物販・オークション引き継ぎ（AI共通）]] **だけ**を読む。そこから必要な節だけ参照する。過去の経緯が要るときだけ相談ログを固有名詞（ヤフオク／eBay／Discogs／Pain Jerk 等）で検索する。

> [!important] 境界（2026-08-04 確定）
> - **売るか／いくら目指すか／事業としてどうするか** → **`ynp97`**（[[🔭 検討中プロジェクト（オープンな問い）]]）
> - **どう売るか**（相場・チャネル・出品・価格・送料・関税・発送・入金） → **`auc97`**
> - **成果物がコード**（eBay Record Lister / レコード査定パイプライン） → **`app97`**
> - 迷ったら、**手を動かす先が出品画面なら `auc97`、エディタなら `app97`、電卓と締切なら `ynp97`。**

**進め方**: 相場・価格・送料・関税は**推測で断定しない**（原則5）。数字を出すときは、その根拠が**実測なのか、記事由来なのか、Vaultのプリセット由来なのか**を必ず区別して書く。**売却記録1件を平均として扱わない。**

**終了時**: 実務で確定したこと（実料金・手数料・成立した手順・落札結果）を [[💴 物販・オークション引き継ぎ（AI共通）]] へ反映し、`最終確認` の日付を更新する。**在庫の売り方・締切・資金目標を変えたときだけ**、相談ログと 🔭 にも1行残す。

## ⑪ ポケカモード（`poke97`）

ポケカの条件検索、対戦、デッキ、PTCGL、収集、カード印刷、関連アプリ、販売構想の全体入口。戦略・アプリ・物販の各モードを置き換えず、話題を正しい正本へ渡す。読んだら「ポケカモードを起動しました」と添える。

1. 最初に [[🃏 ポケカ引き継ぎ（AI共通）]] **だけ**を読む。
2. 対戦・デッキ・PTCGLの相談はそのまま `poke97` で進め、必要な対戦記録だけ追加で読む。
3. 「この条件のカードは何があるか」は、`poke97` スキルの `references/card-search.md` に従い、ポケモンカード公式「カード検索」から現在の結果を取得する。
4. ポケカコーチ・戦績アプリの実装は `app97`、出品・相場・発送は `auc97`、時間配分・出費・趣味の位置づけ・事業化は `ynp97` へ渡す。
5. カードの印刷は [[アプリ引き継ぎ/V カード印刷テンプレート]] を第一選択とし、AIと作業中にPDFを残すときだけ [[🖨 プロキシ印刷パターン（AI共通）]] を使う。
6. 最新のカードデータ、ルール、発売情報、相場は外部の正本で確認する。カード番号だけで仕様や価格を1つに決めない。

**終了時**: ポケカ全体の現在地・方針が変わったら [[🃏 ポケカ引き継ぎ（AI共通）]] を更新する。実装は各アプリの単体引き継ぎ、販売実務は物販・オークション引き継ぎへ更新する。大きな方針変更のときだけ相談ログへ残す。

## ⑫ 学校事業モード（`sc97`）

KG高等学院八千代台キャンパスとフリースクール南十字舎の**事実と実務**を扱う。戦略の4ファイル（ブリーフィング・📌・索引・相談ログ）は**読まない**。読んだら一言「学校事業引き継ぎを読みました」と添える。

1. 最初に [[🏫 学校事業引き継ぎ（AI共通）]] **だけ**を読む。学校の事実はここが正本（2026-08-10に📌から移管）。
2. 制度・期限・単位・卒業条件・学費の照会は、そこから [[資料/KG通信制実務マニュアル2026/00 AI参照入口]] の該当章へ進む。**日付・表・金額・連絡先は原本PDFで再確認してから確定する。**
3. 過去の経緯が要るときだけ [[🗒 相談ログ（追記式）]] を固有名詞（南十字舎／たより／チラシ／出席認定／台帳 等）で検索する。

> [!important] 境界
> - **何をすれば生徒が増えるか・優先順位・資金** → **`ynp97`**（学校は「何をすれば増えるか」が未確定なので、決まっていないことをタスクへ分解しない）
> - **決まっていることを回す**（制度照会・年間日程・台帳入力・たより/チラシ作成・料金・送付） → **`sc97`**
> - **成果物がコード**（学校台帳.app の改修） → **`app97`**
> - 迷ったら、**手を動かす先が制度資料か台帳なら `sc97`、紙とペンで優先順位を考えるなら `ynp97`。**

**進め方**: 制度・期限を**推測で断定しない**（原則5）。数字を出すときは章名とPDFページ番号を示す。**2026年度限定の資料**なので新年度へ自動流用しない。**鹿島学園本校の実務ルールと、八千代台・南十字舎が独自に決める運営方針を混ぜない。** たより・制度解説へのAIの関与は**下調べと下書きまで**で、最終稿は本人の言葉にする。

**終了時**: [[🏫 学校事業引き継ぎ（AI共通）]] を更新し、`最終確認` の日付を直す。**📌には書き戻さない。** 大きな方針が変わったときだけ相談ログと 🔭 に1行残す。

## ⑬ POP・シキホールモード（`pop97`）

POP、シキホールカフェ、2027年3月の一週間参加、VLOG、現地撮影、デニス、孤児院や次の福祉プロジェクト、チラシと登録フォームを扱う。人生・学校・他事業を含む全体の優先順位は `ynp97` へ渡す。

1. 最初に [[🌴 POP・シキホール引き継ぎ（AI共通）]] **だけ**を読む。
2. Codexは `.agents/skills/pop97/SKILL.md` に従う。Claudeも同じ引き継ぎを入口にし、戦略の4ファイル全体は読まない。
3. `pop97` だけが入力された場合は「POP引き継ぎを読みました」と伝え、引き継ぎの「次に再開するところ」から始める。

**境界**: POPの構想・対人関係・制作物は `pop97`。POPを含む人生全体の資金・優先順位は `ynp97`。動画編集の具体操作は `fcp97`、アプリ・サイト実装は `app97`、英訳だけを続ける作業は `tr97`。

**終了時**: [[🌴 POP・シキホール引き継ぎ（AI共通）]] の現在地・次に再開するところ・制作物一覧を更新する。方針変更や対人関係に関わる決定だけ相談ログへ追記する。

## ⑮ ダイエットモード（`det97`）

体重・食事・運動・断食・ケト・糖質制限など、ダイエットの実務と記録を扱う。戦略の4ファイルは**読まない**。読んだら一言「ダイエット引き継ぎを読みました」と添える。

最初に [[🥗 ダイエット引き継ぎ（AI共通）]] **だけ**を読む。現在地（13年分のジャーナル＋Excel体重記録の可視化結果、アーティファクト「13年ダイエット史」）、荒い計画のたたき台、次の一手はすべてそこにある。過去の経緯が要るときだけ [[🗒 相談ログ（追記式）]] を「ダイエット」「断食」「ケト」等で検索する。

> [!important] 境界（2026-08-30 確定）
> - **体重・食事・運動・断食・ケト等の実務**（何をいつ試すか・記録・今回の計画） → **`det97`**
> - **やる気が出ない・体型への気持ち・自己肯定感の話** → `sos97`
> - **事業・時間配分・資金全体の中でダイエットをどう位置づけるか** → `ynp97`
> - 迷ったら、**手を動かす先が体重・食事・記録なら `det97`、気持ちの整理なら `sos97`、優先順位と締切の話なら `ynp97`。**

**終了時**: [[🥗 ダイエット引き継ぎ（AI共通）]] の「現在地」「荒い計画」「次の一手」を更新する。📌・🔭には書き戻さない（学校事業と同じ扱い）。方針が大きく変わったときだけ相談ログにも1行残す。

## 全モード共通

### 聖書箇所を扱うとき（`set97` の合図が無くても適用）

聖書箇所の釈義・説教準備を頼まれたら、**合図の有無にかかわらず**、Vaultの次を読む。

1. [[📖 釈義スキル（AI共通）]] — 本文を確定する（原語・文脈・批判的問い）。**正式出力様式＝SKY本文挿入形式**もここ。
2. [[📖 説教スキル（AI共通）]] — その釈義に錨を置いて、本人の文体で説教化する。
3. **解釈上の問い**（「この主語は誰か」「この語は何を指すか」「なぜそう言えるのか」「本当にそうか」）が出たら → [[📖 構造分解スキル（AI共通）]] の手順（証拠ラベルA/B/C/D → 分解 → 連鎖の一本化 → 誤読の排除）。

> [!important] Claudeの `exegesis` スキルが自動起動しても、**Vaultが正本**（2026-07-31 確認）
> - Coworkの `exegesis` スキルは、`set97` と言わなくても「〇〇を釈義して」で**自動発火する**。
> - **中身はVaultの部分集合**。実質142文のうちVaultに無いのは13本だけで、その内訳は「スキル自身の起動条件」4本と「構造分解スキルに従え」という**Vaultを指す矢印**9本。**スキル固有の中身は無い。**
> - 逆に**Vaultにだけあるもの＝正式出力様式（SKY本文挿入形式）と、過去説教との照合**。スキルだけで進めるとこの2つが落ちる。
> - よって、**食い違ったらVaultを正とする。** スキル側は読み取り専用で更新できないため、Vaultへ寄せる運用にする。
> - ※検証方法の限界＝18文字以上の文を正規化して突き合わせた。それより短い断片は未比較。

**要点**: 本文に根のない主張（読み込み）は指摘する／忖度せず、懸念は結論を出す前に必ず述べる（最終判断は本人）／「節ごとに」と言われたら一節ずつ仕上げ、**指示があるまでファイルは書き換えない。** ただし `st97` は早天共通スキル内の同等の品質ゲートを使う軽量経路とし、難所がある場合だけ詳しい釈義手順へ戻る。

### 学校の制度・期限を聞かれたとき（`sc97` の合図が無くても適用）

学校（KG八千代台・南十字舎）の**制度・期限・年間日程・料金・実務手順**を聞かれたら、**合図の有無にかかわらず記憶で答えない。** [[🏫 学校事業引き継ぎ（AI共通）]] と、必要なら [[資料/KG通信制実務マニュアル2026/00 AI参照入口]] を引いてから答える。**軽量モードは何も読まない設計なので、この一項が無いと推測で答えることになる。**

### 「記憶して」コマンド

ユーザーが「**記憶して**」と言ったら、[[🔌 外部AI連携（起動プロンプトと記憶プロトコル）]] の『記憶ブロック』形式（背景／話したこと／結論／次の一手／更新ファイル／未解決）で出力し、[[🗒 相談ログ（追記式）]] の先頭に追記する。この形式は、冷えた状態の別AI（Gemini / LM Studio等）へそのまま渡して続きを始められる引き継ぎパケットを兼ねる。

### 過去の方針を思い出すとき

ユーザーが「前に」「確か」「どうするんだっけ」「続き」など、過去の方針・設定・作業を想起する言い方をした場合は、**新しい方法を提案・作成する前に**、専用引き継ぎと相談ログを固有名詞の組み合わせで検索し、現在の実体・設定を確認する。検索結果が省略された場合は未確認扱い。**既存方針／現在の実装／代替案を区別する。**

> [!important] ★トリガーはユーザーの言い方だけではない ── **AI側の4語**（2026-08-15 追加）
> 上の規則は**ユーザーが想起する言い方をしたとき**しか発火しない。**ユーザーが何も言わない段階でAIが先に外す**事故が起きたため、AI側の状態もトリガーにする。
>
> **次の4つを書こうとした瞬間、書く前に相談ログと該当引き継ぎを固有名詞で検索する。**
>
> 1. 「**未確認**」
> 2. 「**未検証**」
> 3. 「**要検討**」「まだ決まっていない」
> 4. **新しい提案をしようとしたとき**（「〜してはどうか」「一度試してみては」）
>
> **理由**: この4つは「過去に一度も触られていない」ことを前提にした言葉だが、**その前提自体が未確認**。実際には既に調べ・決着している率が高い。
>
> **検索して見つかったら、次の3つを区別して書く**。㋐既に決着している（決定と理由を引く）㋑調べたが分からなかった（探した語を引く）㋒本当に誰も触っていない。**㋒のときだけ「未確認」と書いてよい。**
>
> **由来（2026-08-15・`ai97`の1セッションで3回）**:
> - **Luna＋OpenClaw** … 2026-08-09に検討済み（結論は「動かない」ではなく「無制限Chat枠のまま手足は付けられない」）。Claudeは検索せず同じモデル比較をやり直した。
> - **15インチAirのローカルAI** … 2026-06-24にメモリ8GBを確認して否決済み。Claudeが「未確認」として再提示。**通算3回目の掘り直し**。
> - **Qwenの再テスト** … 本人が「多数の質問で検証」済みで、2026-08-08のpoke97実測では**唯一の合格**。Claudeは「一度試しては」と再テストを提案した。
>
> **構造上の原因**: 索引方式（2026-07-31、起動時95,500字→約4万字）は各モードに「引き継ぎだけ読む／**過去経緯が必要なときだけ**相談ログを検索」と書いている。**AIは毎回「必要ない」と判断する。** トークン節約の副作用なので、**発火条件をAI側の言葉に固定して埋め合わせる。**
