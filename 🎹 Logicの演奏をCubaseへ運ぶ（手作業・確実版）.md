---
種別: 手順書 / DAW間の受け渡し
対象: Logic Pro → Cubase（Session Playerの演奏を運ぶ）
作成日: 2026-07-24
関連: [[🎹 Chord Bridge 説明書]] / [[🧩 アプリ開発状況（AI共通）]] の「S. Chord Bridge」
メモ: Chord Bridgeの「Logicから受け取る」が不安定なため、アプリを使わずLogic標準機能だけで確実に運ぶ手順。
---

# 🎹 Logicの演奏をCubaseへ運ぶ（手作業・確実版）

Chord Bridgeの自動操作は、Logicの画面をメニュー名で当てて操作する方式のため壊れやすい。
ここでは**アプリを一切使わず、Logic自身の書き出し機能**で運ぶ。こちらが本命。

やりたいことは2通り。目的で選ぶ。

- **MIDI（音符）で運ぶ** … Cubase側で楽器・音色を差し替えたいとき。おすすめ。
- **オーディオ（音そのもの）で運ぶ** … Logicの音色とエフェクトごと欲しいとき。

---

## 【解決済み・2026-07-24】1本のベースが3トラックに割れる問題

### 症状
Logicのベース(Session Player「Bass Player」)を書き出してCubaseへドラッグすると、
1本のはずのベースが「メインの動き＋一番低い音＋SysExデータ」の**3トラックに割れて**張り付いた。

### 根本原因（書き出したMIDIの中身を実解析して確定）
- 書き出しファイルは **SMFフォーマット0・トラックは1本**。
- しかしベースの音符が **MIDIチャンネル 1・2・3 の3つにまたがって**書かれていた（音符45個・音域48〜74）。これはLogicのSession Playerがそういう作りで音を出すため。
- **SysExイベントは0個**。Cubaseで見えた「SysExデータ」トラックは実際のSysExではなく、余ったチャンネルにCubaseが付けた名前だった。
- Cubase側の設定が、この複数チャンネルを**チャンネルごとに別トラックへ割っていた**。犯人はこの設定ひとつ。

### 一度きりの解決（Cubase設定・これで毎回直る）
Cubase → メニュー「Cubase」→ **環境設定** → 左の一覧 **MIDI → MIDIファイル** →「読み込みオプション」で:

1. **「ファイルタイプ 0 の場合はチャンネルを分割」のチェックを外す** ← これが根本。
2. **「ドロップされたファイルを1つのパートとして読み込む」にチェックを入れる**（より確実に1トラック1パートで入る）。
3. 下の「適用」→「OK」。

以後、同じMIDIをドラッグすると **1本のベース = 1トラック** で入る。（2026-07-24、本人がこの設定変更を実施し解決を確認。最終確認: 2026-07-24）

### 取り込み後、パートが少し長いとき
- 末尾の余りを切るだけ: 矢印(オブジェクト選択)ツール＋スナップON で、パート右端を左へドラッグ。
- 演奏ごと尺を詰める: 矢印ツールを「サイズ変更:タイムストレッチを伴う」モードにして右端をドラッグ。
- 範囲へきっちり: 範囲選択→Pキーで左右ロケーターを合わせ、スナップで端を吸着。

---

## ルート1｜MIDI（音符）で運ぶ ← まずこれ

### 手順

1. **Logic**で、運びたいSession Playerのリージョンを**1つクリックして選ぶ**（枠が明るくなる）。
2. そのリージョンを**右クリック**（またはControlキー＋クリック）。
3. メニューの中から **「変換」→「MIDIリージョンに変換」** を選ぶ。
   - Session Playerが実際に弾いた音符が、編集できるMIDIリージョンに変わる。
   - ※Logicの版によって表記が「Session PlayerリージョンをMIDIリージョンに変換」などと少し違うことがある。**「変換」と「MIDI」が入っている項目**を選べばよい。
4. できた**MIDIリージョンを、Logicのアレンジ画面からデスクトップへ直接ドラッグ**する。
   - リージョンをつかんで、Logicの外（デスクトップ）まで引っぱって離す。
   - デスクトップに `.mid` ファイルができる。
5. その `.mid` を **Cubase**の置きたいトラックへドラッグする。
6. 鳴らしたい音源（HALion等）をそのトラックに立てれば、Cubaseの音で鳴る。

### コツ・注意

- **テンポを合わせる**。MIDIには「何BPMか」の情報が入る。CubaseのプロジェクトのテンポがLogicと違うと、長さや位置がずれて聞こえる。まずLogicと同じテンポにする。
- Logicから**Cubaseへ直接ドラッグ**もできることがあるが、アプリ間ドラッグは不安定。**一度デスクトップに出す**方が確実（Chord Bridgeの受渡カードも、結局この「一度ファイルにする」を代わりにやっていただけ）。
- 選択が外れていると4でドラッグできない。3の直後、変換されたリージョンが選ばれた状態のままドラッグする。

---

## ルート2｜オーディオ（音そのもの）で運ぶ

### 手順（バウンスで確実に）

1. **Logic**で、運びたいリージョンを選ぶ。
2. 右クリック → **「バウンス（その場で）」**（Bounce in Place）を選ぶ。
   - Session Playerの音色・エフェクト込みで、その場にオーディオリージョンができる。
3. できた**オーディオリージョンをデスクトップへ直接ドラッグ** → `.wav` / `.aif` ができる。
4. その音声ファイルを **Cubase**のトラックへドラッグする。

### 別のやり方（メニューから書き出す）

1. リージョンを選ぶ。
2. 上部メニューの **ファイル → 書き出す → 「リージョンをオーディオファイルとして…」** を選ぶ。
3. 保存場所（デスクトップ）と形式（WAV/AIFF）を決めて書き出す。
4. Cubaseへドラッグ。

### 注意

- オーディオは後から音色を変えられない。**音を確定してから**運ぶ。
- サンプルレート（44.1k/48k等）をLogicとCubaseで合わせておくと安全。

---

## どうしても詰まったら（切り分けの順番）

1. **リージョンが選べているか**（枠が明るいか）。選べていないと、変換もドラッグもできない。
2. Session Playerのままでは音符が空に見える。**必ず先にMIDIリージョンへ変換**してからドラッグ。
3. デスクトップに `.mid` / `.wav` が**実際にできているか**を目で確認。できていれば、あとはCubaseへドラッグするだけ。
4. できていなければ問題はLogic側（選択・変換）。できているのにCubaseに入らなければ問題はCubase側（ドロップ位置・テンポ）。**ここで前半・後半のどちらが悪いか切り分けられる。**

---

## Chord Bridgeの位置づけ（今の結論）

- **Cubase → Logic（コードをまとめる）** 側は、まだ使える可能性がある。
- **Logic → Cubase（受け取る）** 側は方式が脆く、環境依存で機能しないことがある。**当面はこの手作業ルートを本命にする。**
- 自動化を作り直すかどうかは、この手作業で不便が続くか見てから判断する。
