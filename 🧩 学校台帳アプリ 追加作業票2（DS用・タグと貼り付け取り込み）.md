---
種別: 追加作業票 / 外部AI（DS＝OpenClaw＋DeepSeek）向け
作成日: 2026-07-28
対象: `~/openclaw-apps/SchoolLedger`（初版は実装済み。これはその続き）
渡し方: このファイルだけを渡す。Vaultの他は読ませない。
---

# 学校台帳アプリ 追加作業票2 ── タグと貼り付け取り込み

> [!danger] 実データは渡さない・触らない
> このアプリは氏名と住所を扱う。**動作確認は必ず架空の名前で行う。** 実際の名簿・住所を外へ出さない。

## 0. 前提：モデル側はもう直してある

`Models/DataModels.swift` と `ViewModels/DataStore.swift`、`ViewModels/StatisticsCalculator.swift`、`Views/MonthlyView.swift` は**すでに更新済み**。以下は事実として扱い、作り直さないこと。

- `SchoolLedgerData` に **`var tags: [String]`** が入った。タグ一覧はここに保存される（固定enumは廃止）。初期値は `TagOptions.defaults`＝`データパレット / 名指し請求 / 自社サイト / 飛び込み / 紹介 / チラシ / ネット / その他`。
- 古い `data.json`（tagsが無い）も読めるよう、`SchoolLedgerData` に `init(from:)` を追加済み。
- `ImportedSend` に **`var tag: String`**（既定 `"データパレット"`）を追加済み。
- `DataStore` に **`addTag(_:)` / `removeTag(_:)` / `tagUsageCount(_:)`** を追加済み。
- `Person` の保存キーは **`source` のまま**（既存データを壊さないため）。**画面では「タグ」と表示する。** `sourceDetail` は「メモ」として表示する。
- 統計の `sourceStats` は `data.tags` を回すように変更済み。一覧に無いタグも拾う。

## 1. タグとは何か（間違えないように）

**タグ＝その人がどこから来たか。1人に1つだけ。** 複数付けない。

| タグ | 意味 |
| --- | --- |
| **データパレット** | 鹿島学園本校への問い合わせ一覧。そこから**当校が通えそうな範囲の人を選んで**資料を送る。**向こうは当校を選んでいない。** ラベル管理から取り込む人は基本これ |
| **名指し請求** | 当校を名指しでの請求。**鹿島からメールで来る。** 返送時は当校のパンフだけでなく**鹿島自体の書類もまとめて送る** |
| **自社サイト** | kg-yachiyo.com / minamijuji.com から。2サイトは分けない（いずれ統合するため） |
| **飛び込み** | 直接の来訪・電話 |
| **紹介** | 中学校・教育支援センター・在籍家庭などからの紹介 |
| **チラシ** / **ネット** / **その他** | それぞれ |

**本人が自由に増やせること**が要件。プルダウンで選び、その場で新しいタグを足せる。

## 2. やること

### 2-1. タグの付け替えと一覧管理

- **取り込んだ発送データ（`importedSends`）の各行にタグを表示し、その場で変えられるようにする。** ラベル管理から来る人は既定で「データパレット」だが、名指し請求の人が混ざるので付け替えが要る。行ごとのプルダウンでよい。
- **複数行をまとめて付け替えられる**と楽（チェックを入れて「選んだ行のタグを変える」）。
- **タグ一覧を管理する画面**を作る。追加・削除・並べ替え。削除するときは `tagUsageCount()` で使用数を出し、「〇人に使われています。一覧から外しますか？（その人のデータは変わりません）」と確認する。
- 問い合わせ（`Person`）側のタグ表示・編集も、同じプルダウン＋新規追加で統一する。

### 2-2. ★貼り付け取り込み（新規・これが本命）

**飛び込み・紹介・自社サイトの人はJSONに乗ってこない。** そこで、テキストを貼り付けて一括登録できるようにする。

- 「発送・在籍」タブか新しいタブに、**大きな貼り付け欄**を置く。
- メールやWordからコピーした**雑なテキスト**を貼れる前提。氏名・郵便番号・住所・電話・日付らしきものを行単位で拾う。1人1行でも、複数行で1人でもよいように、**空行かタブ区切りを区切りの手がかり**にする。
- 読み取った結果を**表で見せる**。列＝氏名／郵便番号／住所／初回日／学年／タグ。
- **表の各セルを手で直せる**こと。読み取りは完璧でなくてよく、**直せることのほうが大事**。
- 表の上に**タグをまとめて指定するプルダウン**を置く（例：「今日の紹介3件」を全部「紹介」にして一括登録）。
- 「この内容で登録」を押すと `Person` を作る。**登録時に `events` へ「新規問い合わせ」を1行入れる**（`addPerson` が既にやっている）。
- 既に同じ氏名＋郵便番号の `Person` がいる場合は**警告を出して、選べるようにする**（登録する／飛ばす）。勝手に消さない。

### 2-3. 統計に「タグ別」を出す（表示名の修正）

- 統計タブの「経路別」という表示を**「タグ別」**に直す。計算側（`sourceStats`）はもう対応済みなので、**ラベルの文字を直すだけ**。
- 表の列は 件数／見学実施／入学・入会／入学率。

## 3. 作らないもの

- 距離帯の推定（ラベル管理アプリが正。触らない）
- 自動でのタグ判定（人が選ぶ）
- 複数タグ（1人1つ）
- クラウド同期・ログイン・外部API

## 4. 受け入れ確認（ここまで自分で確認してから完了と言うこと）

架空データで全部通ること。

1. **`swift build -c release` が通る。**
2. **`bash build.command` で `.app` ができ、ダブルクリックで起動する。**（素の実行ファイルだとファイル選択パネルが開かない。必ず `.app` で確認する）
3. タグを新しく1つ追加でき、プルダウンに出て、選んで登録できる。
4. アプリを終了して再起動しても、追加したタグが残っている。
5. 取り込んだ発送データの行のタグを「データパレット」→「名指し請求」に変えられ、再起動後も残る。
6. **貼り付け取り込み**：架空の3人分のテキストを貼って、表に出て、1セル直して、タグを「紹介」にまとめて指定し、3人登録できる。
7. 同じ人をもう一度貼ったとき、重複の警告が出る。
8. 統計タブに「タグ別」の表が出て、件数と入学率が計算される。
9. 10回連続で起動・終了しても固まらない。

## 5. 完成したら報告してほしいこと

- どこまで実装したか、実装しなかったものとその理由
- `swift build` と `.app` 起動の結果
- 受け入れ確認1〜9のどれが通ってどれが通らなかったか
- 詰まった点

---

*初版の設計図は `🧩 学校台帳アプリ 設計図（DS作業票）`。この作業票はその続きで、内容が食い違う場合はこちらが新しい。*
