---
種別: 更新ログ / 履歴（追記のみ）
作成日: 2026-07-31
役割: [[🎛 音楽制作環境引き継ぎ（AI共通）]] の更新履歴。**現在の設定を知るために読む必要はない**（現在の設定は本体の「現在の確定設定」が正）。「いつ何を変えたか」をたどるときだけ開く。
---

# 🎛 音楽制作環境 更新ログ

> [!info] 使い方
> 設定や手順を変えたら、**本体を直したうえで**ここに1ブロック追記する。過去分は消さない。
> 2026-07-31に本体から分離（`q97` 起動時に毎回読む必要がないため）。

### 2026-07-11

- `q97` 音楽制作環境モードを新設。
- Cubase 15・nanoKEY Studio Bluetooth MIDI RemoteとXYパッドの確定設定を登録。

### 2026-07-14

- BFD3 Core Libraryの再インストール先と、旧TRANSCEND参照による二重表示の原因を確認。
- Cubase用 `BFD3簡易版_スネア・ハット重視.drm` を作成したが、Cubase上で全行Kickになり使用不可。ファイル構造の推測による再生成は中止。
- 正常版 `BFD3.drm` を正本として再作成。Map/出力/チャンネルは完全保持し、Orderとマップ名だけを変更した `BFD3_ESSENTIAL_Snare_HiHat.drm` をデスクトップへ配置。
- Cubase実機で `BFD3_ESSENTIAL_Snare_HiHat.drm` が正常に読み込めることをユーザーが確認。失敗時は継ぎ足し修正より「正本から最初にやり直す可能性」を先に探る、という作業原則を追記。
- ユーザー要望により、全音を種類別に整理した `BFD3_ALL_SOUNDS_SORTED.drm` をデスクトップへ配置。
- 今後の1音単位の並べ替えに備え、最新版・正本・Orderのみ変更する手順・検証条件を記録。

### 2026-07-23

- CubaseからLogicへコード進行を渡すMacアプリ `Chord Bridge` の試作版を作成し、デスクトップへ配置。
- Cubase側は新規MIDIトラック作成と「コードをMIDIに変換」まで自動化。受け取った `.mid` / `.midi` をLogicへドラッグできる受渡カードを実装。
- Swift/AppKitでビルドし、アドホック署名・Info.plist検査・デスクトップ版の起動を確認。
- 現在開いている実プロジェクト `BURN` を変更しないため、Cubase自動操作の本番実行は行っていない。テストプロジェクトでの実機確認を次回課題とする。
- 追加要望として、Logic Session Playerの自動演奏をMIDI化し、Chord Bridgeへ直接受け取る機能を次の実装対象に登録。Logic Pro 12.3を確認したが、起動処理中のUIタイムアウトにより実装前のメニュー検証は次回へ持ち越し。
- ユーザー方針により、実プロジェクトを使う最終検証は手動で行うことに変更。Chord Bridge 0.2へ双方向UI、Logic Session Player→MIDI変換、Logic選択MIDI書き出し、失敗時の安全な手動案内を実装し、デスクトップの旧版を更新。
- Cubase本体の背面に隠れる問題を受け、0.2.1で常時手前表示へ修正。Vault内完成版は起動済み。デスクトップ上書きはユーザー承認後も承認サービスエラーが続いたため保留。
- ドラッグ不要化のため0.3へ更新し、MIDIファイルのクリップボードコピー機能を追加。DAW側でのCommand-V受入可否は手動検証待ち。
- 複数コードが一つしか渡らない問題に対応し、0.4で変換後の全MIDIパートを時間順のまま一つへ自動結合する処理を追加。再署名によりアクセシビリティ再許可が必要。
- 実画面確認により、対象はCubaseコードトラックではなく、`Stage-73 V2 01`／`Modular V3 01` 上にすでに並んでいたコード名付きMIDIパートと確定。新規トラック作成方式をやめ、選択トラック上の全イベントを一つへ結合する0.6系へ整理。
- 0.7では `のり` の有効判定がCubaseの閉じたメニュー上で誤判定する問題を除去。しかしメニューを閉じたままのクリックはCubaseに無視されることが実機で判明。
- 0.8では `編集` メニューを実際に開いてから全イベント選択と `のり` を押すよう修正。`BURN` の2トラック各8コードで、一つの連続MIDIパートになることを目視確認。
- デスクトップの `Chord Bridge.app` を0.8（build 9）で上書きし、アクセシビリティ登録を更新。デスクトップ版からCubaseを操作できることも確認済み。

### 2026-07-24

- Chord Bridge 0.9（build 10）へ更新し、Logic→CubaseでMIDI／オーディオを選べるUIと、MIDI・WAV・AIFF等を扱える共通受渡カードを実装。デスクトップ版を上書きし、完成版だけを起動、アクセシビリティ許可も更新した。
- Logic Pro 12.3の実メニュー経路を確認。MIDIは右クリック直下の `選択範囲をMIDIファイルとして…`、オーディオは `書き出す → 1個のリージョンをオーディオファイルとして…`。
- Logicの右クリックメニュー項目は通常のAXツリー外に出るため、自動保存画面オープンは未解決。現行0.9を完成扱いにしない。次回はLogicのキーコマンド設定を使う別経路で仕切り直す。
- Chord Bridge 0.9.1（build 11）へ更新。Dock用の専用アイコン（`C7`／音符の橋／`コード`）を生成・組み込み、デスクトップ版を上書きし、Dockの固定アプリ一覧へ登録した。アプリ本体のアイコン表示は正常。Dockのみ白いキャッシュ表示が残り、macOS再ログイン／再起動後の表示確認が未完了。

### 2026-08-19

- 症状: Cubase 15本体は起動するが、プロジェクト（`BURN LOGICO Glitch` など）を開こうとすると読み込みが終わらず固まる（強制終了が必要）。起動時も「初期化中: オーディオシステム」で毎回15〜20秒程度止まる。
- 切り分け1: セーフモードで「すべてのサードパーティー製プラグインを無効化」して起動すると、同じプロジェクトが正常に開いた。→ サードパーティー製プラグインのどれかが原因と判明。
- 切り分け2: `/Library/Audio/Plug-Ins/VST3/Glitch2.vst3`（2026-07-29ごろ導入したIllformed Glitch 2、無料版）を発見。Finderで検索したところ `~/Downloads/Glitch_2_1_5_Mac_Free` 系のフォルダに展開済みの `.vst`（VST2）と `.component`（AU）の残骸もあったが、これらはダウンロードフォルダに残っているだけで実際のプラグインフォルダには影響なし。実際にスキャンされるのは `/Library/Audio/Plug-Ins/VST3/Glitch2.vst3` の1つだけ（重複なし、ユーザーレベル `~/Library/Audio/Plug-Ins/VST3/` にも重複なし）。
- `Glitch2.vst3` をデスクトップへ退避（削除ではなく移動、管理者パスワード不要）してから開いたところ、「プラグイン "Glitch2"（Insert 8、チャンネル "BFD3 01"）が見つかりません」という警告が一瞬で出るようになり、MixConsoleの読み込み（チャンネルごとの進捗表示）も進み始めた。以前は同じ場面で数十秒〜無反応のまま固まっていたため、Glitch2.vst3が読み込み時にハングしていた可能性が高い。
- ただし、この後ユーザーが自分で再現テストしたところ「Glitch2を抜いても同じ固まり方をした」との報告あり。Glitch2単独が原因と決めつけるのは早計。
- 並行して、Cubase Hub画面のオーディオドライバー表示が「Steinberg UR44C／未接続」だったことが判明。ユーザー確認によりUR44C本体は物理的に接続されていた（＝ケーブル抜けではない）。USBの抜き差しでいったん改善したとの報告あり。
- 新たな手がかり: UR-Cシリーズ（UR44C含む）はCubaseのオーディオドライバー選択に「Steinberg UR44C」と「Steinberg UR44C (High Precision)」の2種類が出る仕様（Cubase⇔ドライバー間を32-bit Integer解像度で処理するかどうかの違い、[UR22C操作マニュアル日本語版](https://download.steinberg.net/downloads_hardware/UR-C/Manuals/UR22C_Operation_Manual_Japanese.pdf)参照）。プロジェクトが保存時に選んでいた側と、現在Cubaseが選択している側が食い違うと「接続されているのに未接続表示」になる可能性がある。`スタジオ → スタジオ設定... → オーディオシステム` でどちらが選ばれているか未確認のまま中断。
- 未解決: UR44C認識問題の根本原因（High Precisionモードの不一致かどうか）は次回確認予定。ユーザーは一度別のAIにも相談している（詳細不明）。
- 追記: ユーザーが暫定的にオーディオインターフェースを **MOTU M2** に切り替えたところ快適に動作。UR44C側（本体かYamaha Steinbergドライバー）に問題がある可能性がさらに濃厚になった。UR44C自体の原因究明はまだ未解決（次回、実機診断や別ドライバーとの相性を確認する）。当面はMOTU M2運用で作業継続可。
- アップデート確認: セッション中に見た起動画面は15.0.20（Jun 1 2026ビルド）表示だったが、ユーザー確認により**Cubaseは実際には最新の15.0.30が導入済み**（2026-06-03リリース、[告知](https://forums.steinberg.net/t/cubase-15-0-30-maintenance-update/1033958)）。Cubase側の更新はUR44C問題の対策としては効果なし＝原因から除外。残る手がかりはYamaha Steinberg USBドライバー側。最新は**V3.1.7**（2024-07更新、UR44C含むUR-Cシリーズ対応、[Yamaha公式](https://usa.yamaha.com/support/updates/yamaha_steinberg_usb_driver_for_mac.html)）。Steinberg側の配布ページはV3.1.3（2022年）のまま更新が遅れているため、ドライバーはYamaha公式から取得する方が確実。次回は現在入っているドライバーのバージョンを確認し、V3.1.7未満なら更新した上でUR44Cを繋ぎ直して再現するか試す。
- ドライバー実機確認: `/Applications/Yamaha Steinberg USB Control Panel.app` の情報を見ると **バージョン3.1.4**（著作権表記2009-2022、変更日 令和8年1月3日）。最新のV3.1.7より古かった。→ **ユーザーが試した結果、ドライバー更新でも解決せず。ドライバーのバージョンは原因ではないと判明（候補から除外）。**

#### UR44C問題 現在の状態（2026-08-19時点で中断）

- **症状**: Cubase 15でUR44Cを使うと、オーディオシステム初期化が異常に長く、プロジェクトが開けずに固まる。MOTU M2に交換すると快適に動作する。
- **除外済みの原因**:
  - Cubaseのバージョン（15.0.30・最新で発生）
  - Yamaha Steinberg USBドライバーのバージョン（更新しても解決せず）
  - Glitch2.vst3（退避しても同じ固まり方が再現。ただし読み込みが速くなる効果はあった＝無関係とも言い切れない）
  - USBケーブルの物理的な抜け（接続済みの状態で発生）
- **未確認の候補**:
  - `スタジオ設定 → オーディオシステム` で「Steinberg UR44C」と「Steinberg UR44C (High Precision)」のどちらが選択されているか。プロジェクト保存時の選択と食い違うと不整合が起きる可能性（[UR22C操作マニュアル日本語版](https://download.steinberg.net/downloads_hardware/UR-C/Manuals/UR22C_Operation_Manual_Japanese.pdf)）
  - UR44C本体のハードウェア不良・USBポート/ケーブルの品質
  - macOSのバージョン（macOS 26系）とUR44Cの相性
  - Cubaseの環境設定に残った古いデバイス情報（セーフモードの「プログラム設定を初期化」で消える範囲）
- **当面の運用**: MOTU M2で作業継続。UR44Cは使わない。
- 次にこの件を再開するときは、上の「未確認の候補」から進める。

### 2026-08-20

- 症状: UR44CをやめてMOTU M2に替えた後も、**Cubaseが全体的に重い**。M5・32GB・2TBの機体としては明らかに遅すぎる、という相談。
- 原因: `スタジオ → スタジオ設定... → オーディオシステム → 詳細設定` で **「マルチプロセッシングを有効化」がオフ**になっていた。この状態ではCubaseはCPUコアを1個しか使わないため、どれだけ高性能なマシンでも全体が重くなる。連動して**「ASIO-Guardを有効化」も選択不可（グレーアウト）**になっており、両方とも無効だった。
- 対処: ①「マルチプロセッシングを有効化」をオン → ②選択可能になった「ASIO-Guardを有効化」をオン（レベルは標準）→ ③「適用」。適用後、ASIO-Guardレイテンシー表示が `0.000 ms` から `170.667 ms` に変わり、有効化を確認。**ユーザー確認により動作は通常の速さに回復。**
- いつオフになったかは不明。UR44Cのトラブル対応の過程で誰か（または別のAI）が切った可能性もあるが未確認。
- 再発防止のため [[🎛 音楽制作環境引き継ぎ（AI共通）]] の「現在の確定設定」冒頭に専用セクションを新設し、「つまずきから引く」にも1行追加した。**次に「Cubaseが重い」と言われたら、プラグインやメモリを疑う前にまずここを見る。**
- 未対応（既知の状態として記録）: バッファーサイズが大きい（入力88.417ms／出力85.917ms＝4096サンプル相当）。ミックス作業ではこのままで問題ないが、演奏・録音する日は `スタジオ設定 → M2 → コントロールパネル` で256〜512へ下げる必要がある。今回は変更していない。
- 補足: Glitch2は再インストール済みで、この日はプロジェクト内で正常に動作していた（画面で確認）。

#### UR44C問題の再検証（マルチプロセッシング修正後）

- マルチプロセッシングを有効化した状態でUR44Cを再テストした結果、**起動までは明確に速くなったが、ドライバーをUR44Cに切り替えた時点でスタック**。
- **これは大きな前進。** フリーズの発生点が「プロジェクトを開くとき」から**「オーディオドライバーを切り替える瞬間」**へ絞り込まれた。この結果、以下が原因候補から外れる。
  - プラグイン全般（Glitch2含む）— プロジェクトを開く前に固まるので無関係
  - プロジェクトファイルの破損 — 同上
  - CPU負荷・マルチプロセッシング設定 — 修正後も再現
- 残る問題は **UR44Cのドライバー初期化そのもの**。
- **次回の決定的テスト（順番厳守）**: UR44Cを繋いだら、**Cubaseを開く前に**「Audio MIDI設定」を開いて機器一覧を見る。
  - UR44Cが**出ない** → macOSレベルで認識できていない。Cubaseは無関係で、ドライバー／ハード／USB接続の問題。Cubaseが固まるのは「存在しない機器の初期化を待ち続けている」ためと説明がつく。
  - UR44Cが**出る** → macOSは見えているのにCubaseだけ固まる＝ASIO層かCubase側の問題。このときサンプルレートとクロックソースの表示も控える。
  - ※Cubaseを先に開くと固まって何も分からなくなるので、必ずAudio MIDI設定が先。
- 参考（2026-08-20時点のAudio MIDI設定の状態、UR44C未接続時）: VT430／iPhone15proMAXのマイク／M2／BlackHole 2ch／MacBook Proのマイク／MacBook Proのスピーカー／Microsoft Teams Audio／集約デバイス「会議録音入力」「会議用音声出力」。

#### ★最有力候補: 仮想オーディオドライバーと集約デバイス（2026-08-20 判明）

- **ユーザー証言: BlackHole 2ch／Microsoft Teams Audio／集約デバイス「会議録音入力」「会議用音声出力」は、UR44Cが普通に使えていた頃には入っておらず、最近追加したもの。** 導入時期と不具合の発生時期が一致する。
- **想定される機序**: UR-Cシリーズのドライバーは、UR44Cを**複数のオーディオデバイスとしてmacOSに見せる**仕様（Music／Voice／DAWの各チャンネルに別アプリの音を割り当てられる機能。マルチストリーム対応）。ここに集約デバイスや仮想ドライバーが絡むと、CoreAudio側がUR44Cの一部を掴んだままになり、**CubaseのASIOドライバーが排他アクセスを取ろうとして永久に待つ**。これは「ドライバー切り替えの瞬間に固まる」という観測症状と一致する。MOTU M2が問題なく動くのは、M2が単一デバイスとして見えるため（と推測）。
- **検証手順（この順で）**:
  1. UR44Cを繋いだら、**Cubaseを開く前に**「Audio MIDI設定」でUR44Cが出るか確認。
  2. 左の一覧から「会議録音入力」「会議用音声出力」をそれぞれ選択し、**メンバー一覧にUR44Cが含まれていないか**確認。含まれていればチェックを外す。
  3. それでも固まるなら、**集約デバイス2つを一時的に削除**（`−`ボタン）して再テスト。後でいつでも作り直せる。
  4. さらにダメなら、BlackHole 2ch／Microsoft Teams Audioのアンインストールも検討。
- 2か3で解決したら原因確定。結果を必ずここへ追記すること。

#### 検証結果: macOSはUR44Cを正常認識（2026-08-20）→ 仮想ドライバー説はほぼ否定

UR44Cを接続した状態でAudio MIDI設定を確認した結果:

- **Steinberg UR44Cが正常に一覧表示される**（入力8／出力6、トップレベルのデバイスとして）。
- **クロックのソース: 内部** → 外部クロック待ちでハングする線は消滅。
- **フォーマット: 48,000 Hz / 6 ch 32ビット整数** → M2と同じ48kHzでサンプルレート不一致もなし。
- 入力チャンネルとして DAW Mix1 L/R、DAW Mix2 L/R、FX Bus L/R がすべて見えている。
- 集約デバイス「会議用音声出力」のメンバー一覧にUR44Cは載るが**「使用」チェックは外れている**（掴んでいない）。

**→ 結論: 本体・USBケーブル・ドライバー・macOS認識はすべて正常。ハード故障説と、BlackHole／Teams Audio／集約デバイスが原因という説は、ほぼ否定された。** 邪魔しているならこの時点で認識に異常が出るはず。集約デバイスの削除は不要と判断。

#### ★次の最有力候補: High Precision ドライバーの選択

- Audio MIDI設定でUR44Cのフォーマットが **「6 ch 32ビット整数」** と表示された。一方、Cubaseの `スタジオ設定 → オーディオシステム → 詳細設定` の**プロセッシング精度は 32 bit float**。
- UR-Cシリーズには、Cubase⇔ドライバー間を**32-bit integer解像度で処理するための別ドライバー「Steinberg UR44C (High Precision)」**が用意されている（[UR22C操作マニュアル日本語版](https://download.steinberg.net/downloads_hardware/UR-C/Manuals/UR22C_Operation_Manual_Japanese.pdf)）。デバイスが32ビット整数で見えている事実と符合する。
- **次にやること**: Cubaseのドライバー選択で「Steinberg UR44C」と「Steinberg UR44C (High Precision)」の**両方を試す**。片方で固まったら強制終了してもう片方を選ぶ。
- **コツ**: プロジェクトを開かずHub画面の状態でドライバーを切り替える。固まってもダメージが小さい。
- 結果は必ずここへ追記すること。

#### 【重要な訂正】「未接続」表示の読み違い（2026-08-20）

**このログの上の方に書いた「UR44C／未接続」を根拠にした推論は、前提が間違っていた。以下が正しい理解。**

- Cubase Hub上部の並びは `オーディオドライバー [Steinberg UR44C ▼]　🔊 [未接続 ▼]` の**2つの別々のドロップダウン**。
- 右側の「未接続」は**デバイスの接続状態ではなく、メイン出力をどのポートへ繋ぐかの割り当て設定**。ドロップダウンを開くと選択肢は `DAW Mix1 L + R` / `DAW Mix2 L + R` / `FX Bus L + R` / `未接続` で、**UR44Cの出力ポートがすべて正常に列挙されている**。
- **つまりCubaseはUR44Cを認識できており、入出力ポートも見えている。**「未接続」は単に出力先が未割り当てなだけ。
- → 本ログ内の「Cubaseがデバイスを開けていない」「存在しない機器の初期化を待っている」といった記述は**誤り**。同様に、この表示を根拠にドライバー・ハード・USB接続を疑った推論も前提が崩れている。
- なお**ユーザー指摘のとおり、この出力割り当ては起動後にいつでも変更できるものなので、起動時のハングやプロジェクト読み込みの停止とは無関係**。ここを直しても解決しない。

#### 検証結果: High Precisionは無関係（2026-08-20）

- ドライバー選択を「Steinberg UR44C (High Precision)」→「Steinberg UR44C」（素）へ切り替えて比較した。**どちらでも表示・挙動に差はなし。** High Precision説は否定。

#### 未確認の観察（次回ここから）

- ユーザーより「**そもそも（ドロップダウンで）選ぶことが困難**」との報告。UR44Cを選んでいる間だけCubaseのUI操作が効きにくい可能性がある。
- もしこれが「反応が遅い／押しても効かない」という意味なら、**Yamahaドライバーが周期的にCubaseのメインスレッドをブロックしている**と考えられ、①起動時「オーディオシステム初期化」での長時間停止、②プロジェクト読み込みでのハング、③メニュー操作の不能——の3つすべてを1つの機序で説明できる。M2で起きないこととも符合する。
- ただし**この点は未確認**（UI操作が遅いのか、単に操作しづらいのかを切り分けていない）。次回はまずここを確認する。
- **現時点の実用的な結論: UR44Cの原因は未特定。MOTU M2で作業を継続する。** 深追いのコストが見合っていないため、UR44Cは当面使わない。

#### ★★ 決定的な観察: ハングはCubase内部ではなくシステム全体（2026-08-20）

**ユーザー報告**:
- 出力割り当てのドロップダウンは**そもそも選択できない**。触るとぐるぐる（ビーチボール）が出て進まない。
- そのとき**Cubaseだけでなく、Claude（別アプリ）の書き込みにも障害が出た**＝ハングがCubaseのプロセス内に閉じていない。

**これが意味すること**: macOSの音声デーモン **`coreaudiod`（システム常駐プロセス）が、UR44Cへのアクセス時に固まっている**と考えられる。coreaudiodはシステム全体の音声を司るため、これが詰まると音声に触れる全アプリが巻き添えになる。

**この機序で、今日観測したすべてが1つに説明できる**:
- 起動時「初期化中: オーディオシステム」での長時間停止
- プロジェクト読み込みでのハング
- ドロップダウンが選択できない
- **他アプリまで巻き込まれる**（← Cubase内部の問題では説明不可能。これが決め手）
- MOTU M2では一切起きない（別ドライバーなので）

**→ 問題はCubaseではなく、Yamaha Steinberg USBドライバーとこの環境（macOS 26）の間にある。** Cubase側の設定・プラグイン・プロジェクトを疑う必要はもうない。

**次回の手順**:
1. 応急処置: ターミナルで `sudo killall coreaudiod`（音声デーモンのみ再起動。再起動不要、パスワード要）。詰まった状態が解けることがある。
2. 決定的な切り分け（**Cubaseを一切使わない**）: `システム設定 → サウンド` で出力先をUR44Cにして、YouTubeや音楽を再生する。
   - **固まる／音が出ない** → Cubaseは完全に無関係。ドライバーとOSの問題で確定。
   - **普通に鳴る** → CoreAudioの基本動作は生きており、Cubaseが使う経路だけの問題。
3. 上記1の結果なら、**上書き更新ではなく、TOOLS for UR-C付属のアンインストーラーで完全削除 → 3.1.7をクリーンインストール**。壊れた状態は上書きインストールでは直らない。
4. それでもダメなら、`システム設定 → 一般 → ログイン項目と機能拡張 → ドライバ機能拡張` でYamahaのドライバ機能拡張が正常に有効化されているか確認（macOS 26のDriverKit。半端な状態だとデバイスアクセス時にハングする）。

**補足（訂正）**: 出力割り当てが原因ではないというユーザーの指摘は正しかった。実際の順序は「割り当て項目に触る → CoreAudioがUR44Cへ問い合わせる → そこで固まる」であり、割り当て設定そのものは無関係。
