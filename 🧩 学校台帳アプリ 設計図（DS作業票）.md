---
種別: 設計図 / 外部AI（DS＝OpenClaw＋DeepSeek）への作業票
作成日: 2026-07-27
作成: Claude（戦略モード ynp97）
渡し方: この1ファイルだけをDSへ渡す。Vaultの他のファイルは読ませない（トークン節約と情報保護のため）。
---

# 学校台帳アプリ 設計図（DS作業票）

> [!danger] 実データは絶対に渡さない
> このアプリは氏名・住所を扱う。**DSへ渡してよいのはこの設計図だけで、実際の名簿・住所・問い合わせ内容は一切渡さない。** テストデータはDSが架空の名前で作る。動作確認も架空データで行う。
> 保存先も、他AIと共有しうるObsidian Vault内には置かない。理由は「6. 保存」に書く。

---

## 1. これは何か

八千代台で運営している学校事業（フリースクール南十字舎／KG高等学院八千代台キャンパス／学研教室）の、**問い合わせ・発送・在籍を記録して統計を取るMacアプリ**。使うのは運営者1人。

**作る理由**は2つ。

1. これまで記録が面倒で続かなかった。**アプリにして入力が楽しく速ければ続く**、というのが本人の見立て。だから「1件30秒で入る」が最優先の性能要件。
2. 1年ぶん貯まると資料になる。特に知りたいのは次の3つ。
   - **どの経路から来た人が実際に入学まで進むか**（＝どこに労力を割くか決まる）
   - **見学のときの手応えが、実際の成立と合っているか**（＝自分の勘の検証）
   - **住所のどこまでが反応するゾーンか**（＝商圏の実測。いまは「八千代台周辺」という感触しかない）

## 2. 絶対条件（ここを外すと作り直し）

- **毎日同じように確実に起動して、普通に閉じられること。** 過去に、常駐プロセスや特殊なアプレット化が原因で急に起動不能になった事故がある。単純で壊れにくい作りにする。
- **データが消えないこと。** 過去にブラウザのlocalStorageが保存領域の分岐で消えた事故がある。だから**実ファイル保存**にする（「6. 保存」）。
- **オフラインで動くこと。** 外部API・課金・クラウド同期・ネット接続を必要としない。
- **1件30秒。** 入力はプルダウンとボタン中心。自由記述は最小限。毎回タイピングさせない。

## 3. 技術構成

- **SwiftUIのmacOSアプリ**（単一ウインドウ）。同じVaultの `出席簿.app`・`ZatsuTodo` と同じ路線。
- 保存は**実ファイルのJSON**。
- 外部ライブラリなし。ネット接続なし。
- ビルドして `/Applications/学校台帳.app` に置ける形にする。Dockに入れて使う。

## 4. データモデル

JSONのトップレベルは次の4つの配列を持つ。

```json
{
  "version": 1,
  "people": [],
  "mailings": [],
  "enrollments": [],
  "importedSends": []
}
```

### 4-1. people（問い合わせ＝人／家庭）

**数え方のルール（既存の運用基準に合わせる。変えないこと）**
- **1家庭・1生徒を1件**とする。
- 同じ家庭が電話・メール・見学予約で何度連絡しても、**同じ人として1件**（重複レコードを作らない）。
- 兄弟姉妹でそれぞれ入学の可能性がある場合は、**生徒ごとに別レコード**。

```json
{
  "id": "uuid",
  "firstContactDate": "2026-09-03",
  "displayName": "山田（母）",
  "studentGrade": "中3",
  "targets": ["南十字舎", "KG高校部"],
  "source": "紹介-中学校",
  "sourceDetail": "八千代台西中 進路指導",
  "address": { "postal": "276-0032", "text": "八千代市八千代台東1-2-3" },
  "linkedSendId": "importedSends内のid（あれば）",
  "events": [],
  "nextAction": "9/10に折り返す",
  "nextActionDate": "2026-09-10",
  "note": ""
}
```

- `studentGrade` の選択肢：`小1`〜`小6` / `中1` / `中2` / `中3` / `高1` / `高2` / `高3` / `高校中退` / `その他`
- `targets` は**複数選択可**（学研は南十字舎やKGと併用されるため、排他にしないこと）：`南十字舎`（小中の居場所） / `KG高校部`（高卒資格） / `学研教室` / `未定`
- `source` の選択肢（固定リスト。運用で「どこで知りましたか」を必ず聞く）：
  `電話` / `メール` / `問い合わせフォーム` / `サイト` / `Googleビジネスプロフィール` / `鹿島本校から` / `紹介-中学校` / `紹介-高校` / `紹介-教育支援センター` / `紹介-塾` / `紹介-在籍家庭` / `紹介-その他` / `チラシ` / `南十字舎だより` / `Instagram` / `Facebook` / `その他` / `不明`
- `address` は任意。**入れなくても登録できること**（電話問い合わせで住所を聞かない場合があるため）。

### 4-2. events（人の中の履歴。ここが設計の中心）

**ステージが進むたびに1行足す。上書きしない。** これにより「いつ見学に来たか」「そのときの手応え」「実際にどうなったか」が全部残り、後から突き合わせられる。

```json
{
  "date": "2026-09-10",
  "stage": "見学・個別相談実施",
  "feel": 4,
  "comment": "母親が熱心。本人は無口だが教室を一周した"
}
```

> [!warning] 2026-07-28改定：ステージを8段階→6段階へ変更
> `stage` の選択肢（**改定後の6段階。以後はこちらが正**）：
> `送付` / `電話・メールあり` / `見学予約` / `見学・個別相談実施` / `体験` / `入学`
>
> - `資料請求`は`送付`に統合。データパレット・名指し請求・自社サイト・飛び込み・紹介・チラシ・ネットなど**経路（`source`）を問わず、こちらの最初の動きは`送付`で統一**する。
> - `保留`／`見送り`は廃止。判断できないことが多いため、**現ステージがそのままその人の状態**とする（無理に望みあり/なしを判定しない）。
> - 旧8段階（`新規問い合わせ` / `資料請求` / `見学・個別相談予約` / `見学・個別相談実施` / `体験` / `入学・入会` / `保留` / `見送り`）は参考として残すが、新規実装はしない。
- `feel`（**手応え**）は **1〜5** の整数。UIは星かボタン5つ。**入力必須にしない**（0＝未記入）。
  - 5＝決まりそう／4＝手応えあり／3＝どちらとも／2＝薄い／1＝ほぼない
- `comment`（**印象**）は1行の自由記述。**そのときの空気を残すための欄**なので、長文を強制しない。
- 人の「現在のステージ」は、**events の最新日付のものから自動的に決まる**。人のレコードに現ステージを別途持たない（二重管理でズレるため）。

### 4-3. mailings（発送）

```json
{
  "id": "uuid",
  "date": "2026-07-28",
  "kind": "紹介者送付",
  "targetType": "中学校",
  "count": 10,
  "note": "第一便"
}
```

- `kind`：`紹介者送付` / `資料請求への返送` / `南十字舎だより` / `チラシ` / `その他`
- `targetType`：`中学校` / `高校` / `教育支援センター` / `塾` / `在籍家庭` / `見学に来た家庭` / `その他`
- 1件ずつではなく**まとめて件数で記録**する（「7/28に中学校へ10件」で1レコード）。

### 4-4. enrollments（在籍。月末に1行）

```json
{ "yearMonth": "2026-07", "minamijuji": 12, "kg": 0, "gakken": 0, "note": "" }
```

- 基準日は **2026年6月30日**。以後の増減はそこからの純増で見る。
- 月末時点で実際に所属している人数。

### 4-5. importedSends ── **2026-07-28廃止。`people`へ統合**

> [!danger] この節は旧設計（参考用）。以後は実装しない。
> `importedSends`を`people`と別の箱にして後から手動で紐づける、という二階建て構造をやめる。
>
> **新方式**：ラベル管理からの送付リストを取り込んだ瞬間に、1件ずつそのまま`people`のレコードとして登録する（`source`＝データパレット等・初回イベント＝`送付`・`zone`はラベル管理の推定値をそのまま`Person`の項目として保持）。
>
> 理由：あとで反応があった人を送付済みリストから探して手動で紐づける作業（下の「突き合わせ」）が本人の感覚に合わなかった（[[🗒 相談ログ（追記式）]] 2026-07-28参照）。全員を同じ`people`台帳に置き、タグとステージで絞り込めば、経路別・反応の有無・距離帯など好きな角度から統計できる。`matchedPersonId`のような別レコードとの紐づけ自体が不要になる。
>
> 下の元の設計（`importedSends`という別テーブル＋`matchedPersonId`での突き合わせ）は経緯の記録として残すが、**新規実装では使わない。**

**（旧設計・参考）ここが「誰が来て、誰が来なかったか」「どこまでが反応ゾーンか」の土台になる。**

```json
{
  "id": "uuid",
  "name": "山田太郎",
  "postal": "276-0032",
  "address": "八千代市八千代台東1-2-3",
  "sentAt": "2026-07-15",
  "batch": 3,
  "zone": { "station": "八千代台", "trainMin": 0, "walkMin": 8, "totalMin": 8, "band": "0-10分", "accuracy": "町名一致" },
  "matchedPersonId": null
}
```

- `zone` は**ラベル管理アプリが推定した結果をそのまま受け取る**。
  **距離や所要時間の推定ロジックを、このアプリで作り直さないこと。** ラベル管理側が正で、こちらは受け取って集計するだけ。二重に実装すると必ず数字がズレる。
- `matchedPersonId` … その人から問い合わせが来たら、people のレコードと結びつける。`null` のままなら「送ったが反応しなかった人」。

## 5. ラベル管理アプリからの取り込み

既存の別アプリ「ラベル管理」（`アプリ本体/ラベル管理/資料請求ラベル管理.html`、単体HTML）に、**資料を発送した人の氏名・住所・推定距離帯がすでに入っている**。

- 保存場所：ブラウザの `localStorage["label_app_v2"]`
- 送付済みの配列：`state.sent = [{ name, postal, address, at }]`（`at` は `toLocaleString("ja-JP")` の文字列）
- 既出エントリ：`state.seen = [{ name, postal, address, at, batch }]`
- 距離推定に使っている：`state.stations` / `state.geo` / `state.settings`（京成八千代台を基準に、電車時間・駅までの徒歩/自転車・道路補正係数から距離帯を出す辞書とルール）

> [!important] 取り込みの方式（DSはここを実装しない）
> SwiftUIアプリからChromeのlocalStorageは直接読めない。そこで**ラベル管理側に「送付済みをJSONで書き出す」ボタンを別途追加する**（この作業票の範囲外。運営側で対応する）。
> **DSが実装するのは、その書き出されたJSONファイルを読み込む取り込み機能だけ。**
>
> ファイルを選ぶダイアログで取り込み、`name + postal` が同じものは重複として追加しない。
>
> **2026-07-28改定：取り込み先は`importedSends`ではなく`people`。** `sends[]`の1件ずつを、そのまま`Person`として登録する（`source`＝既定「データパレット」・`events`の1件目＝`stage:"送付"`・`zone`は`Person`の項目としてそのまま保持）。別テーブルへの一次保存や後からの紐づけ作業は行わない。

> [!warning] 取り込むJSONの実際の形（2026-07-28 訂正）
> 初版のこの欄に「`importedSends` と同じ配列」と書いたのは**誤り**。ラベル管理が実際に出すのは**包んだオブジェクト**で、しかも**nullが入る**。この記述のせいで初回実装の取り込みが動かなかったので、以後はこちらを正とする。
>
> ```json
> { "exportedAt": "...", "source": "資料請求ラベル管理", "dictNote": "...",
>   "settings": { "mode": "bike", "road": 1.3 }, "count": 12,
>   "sends": [ { "id": "...", "name": "...", "postal": "...", "address": "...",
>                "sentAt": "2026-07-15", "sentAtRaw": "2026/7/15 14:30:00",
>                "batch": null,
>                "zone": { "station": null, "trainMin": null, "walkMin": null,
>                          "totalMin": null, "band": "判定不能", "accuracy": "辞書に一致なし" },
>                "matchedPersonId": null } ] }
> ```
>
> - **`batch` は null になる**（送付済みだが `state.seen` に記録がない人。実際には大半がこれ）。
> - **住所が辞書に一致しないと `zone` の `station / trainMin / walkMin / totalMin` が全部 null**（`band` は `"判定不能"`）。
> - `sentAtRaw` / `dictNote` / `settings` は使わなくてよい（無視する）。
> - **素の配列も受けられるようにしておく**（将来の手貼り用）。
> - Swiftの合成 `init(from:)` は **null と欠けたキーの両方で失敗する**ので、`decodeIfPresent` で受けて既定値へ落とすこと。

### 突き合わせ（マッチング）── **2026-07-28廃止（旧設計・参考）**

> [!danger] 実装しない
> 上の「4-5. importedSends」の改定と同じ理由で、この紐づけ作業自体が不要になった。取り込み時点で`people`として登録するため、後から探して紐づける手順は発生しない。

- ~~取り込み後、people の各レコードに「送付済みリストから紐づける」ボタンを置く。~~
- ~~押すと、displayName や住所の一部で候補を絞って一覧表示し、選んで紐づける。自動マッチは曖昧なのでやらない。最終判断は人がする。~~
- ~~紐づくと、その people は「送って、反応があった人」になる。紐づかない importedSends は「送ったが反応がなかった人」として集計される。~~

## 6. 保存

- 保存先：`~/Library/Application Support/SchoolLedger/data.json`
- **Obsidian Vaultの中には置かない。** Vaultには他AIと共有するフォルダがあり、Git管理される可能性もある。氏名・住所を含むデータをそこに置かない。
- 変更があったら**即座に保存**する（「保存ボタンを押し忘れて消えた」を起こさない）。
- 起動時に読み込む。ファイルが無ければ空で作る。
- **自動バックアップ**：保存のたびに `data.json` を `backups/data-YYYYMMDD.json` へ日付単位で残す（1日1個で上書き、30日分保持）。
- 画面のどこかに、最後に保存した日時を小さく出す。

## 7. 画面

タブは4つ。起動時は「今月」を開く。

### 7-1. 今月（ホーム）

- 大きく：**今月の問い合わせ件数** / **今月の発送件数** / **在籍合計**
- **12月までの10人増に対する進捗バー**（2026年6月30日の在籍を基準にした純増）
- **ファネル**（今月ではなく全期間）：新規問い合わせ → 資料請求 → 見学予約 → 見学実施 → 体験 → 入学・入会 の各段階に到達した実人数を横棒で表示
- **今日やること**：`nextActionDate` が今日以前の人を一覧（放置を防ぐ）
- **「＋問い合わせを登録」の大きなボタン**（ここが一番押される）

### 7-2. 問い合わせ

- 一覧（現ステージ・最終更新日でソートと絞り込み）
- 新規登録フォーム：初回日（今日が既定）／呼び名／学年／希望先（複数選択）／経路／住所（任意）
  - **登録した瞬間に events へ「新規問い合わせ」が1行入る。**
- 人を開くと、履歴が時系列で並び、下に**「ステージを進める」**があって、ステージ・日付・手応え（星5つ）・ひとことを入れて追加する。
- **入力が速いこと。** ステージを進めるのは3タップ＋星1タップで終わるようにする。

### 7-3. 発送・在籍

- 発送の記録（日付・種類・宛先区分・件数・メモ）と一覧
- 在籍の月末入力（南十字舎／KG／学研）と推移
- **ラベル管理から取り込む**ボタンと、取り込んだ送付済みリストの一覧（紐づけ済み／未紐づけが分かる）

### 7-4. 統計

**ここが1年後の資料になる。**

1. **経路別の成果**（一番大事）
   - 経路ごとに：問い合わせ件数 / 見学実施まで来た人数 / 入学・入会まで来た人数 / **入学率**
   - 入学率 ＝ その経路で `入学・入会` イベントを持つ人数 ÷ その経路の問い合わせ人数
2. **ファネルの転換率**
   - 各段階に到達した実人数と、前段階からの転換率
   - 「見学まで来た人のうち何割が入るか」を目立たせる
3. **手応えの答え合わせ**
   - `見学・個別相談実施` のときに付けた `feel`（1〜5）ごとに、その後 `入学・入会` に至った割合
   - 自分の勘が当たっているかが分かる
4. **★反応ゾーン**
   - `importedSends` の `zone.band`（距離帯）ごとに：**送付数 / 反応数（＝people に紐づいた数） / 反応率 / 見学到達数 / 入学到達数**
   - 同じものを**市区町村＋町名**でも出す
   - これで「どこまで送る意味があるか」が数字で出る
5. **月次推移**
   - 問い合わせ数 / 発送数 / 在籍数を月ごとに折れ線か棒で
   - 発送した月と、問い合わせが来た月のズレも見たい（送ってから反応までのラグ）
6. **CSV書き出し**：上のどの表もCSVで出せるようにする（後で表計算で見るため）

## 8. 作らないもの（範囲外）

- クラウド同期・複数端末・共有機能
- 外部API（地図・ジオコーディング・メール送信）
- ログイン・パスワード
- 距離や所要時間の推定ロジック（ラベル管理側が正）
- 自動マッチング（人が選ぶ）
- 印刷・宛名ラベル（ラベル管理アプリの担当）

## 9. 受け入れ確認（DSはここまで自分で確認してから完了と言うこと）

架空のテストデータで、次が全部通ること。

1. アプリを起動→終了→再起動して、**入力したデータが残っている**。
2. `~/Library/Application Support/SchoolLedger/data.json` が実際に作られ、中身がJSONとして読める。
3. 問い合わせを1件登録するのに**30秒以内**で終わる。
4. 同じ人にステージを4段階進めて、履歴が4行並び、現ステージが最新のものになっている。
5. 手応え（星）とひとことが、ステージごとに別々に残っている。
6. ラベル管理形式のJSON（架空データ）を取り込み、人と紐づけられる。
7. 統計タブで、経路別の入学率・ファネル・距離帯別の反応率が数字で出る。
8. CSVが書き出せる。
9. アプリを10回連続で起動・終了しても、固まったり起動しなくなったりしない。

## 10. 完成したら報告してほしいこと

- 実際のファイルの場所（アプリ本体・data.json）
- 起動方法
- 実装しなかった項目があれば、その理由
- 詰まった点

---

*関連（DSには渡さない。運営側の参照用）: `🧩 アプリ開発状況（AI共通）` ／ `学校事業 問い合わせ台帳` ／ `学校事業 月間問い合わせ・在籍内訳カウント基準` ／ `🗓 三本立て進行表（7月27日〜8月20日）`*
