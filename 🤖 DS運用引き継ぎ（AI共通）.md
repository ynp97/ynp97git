---
種別: AI引き継ぎ / 分割
分割元: 🤖 Obsidian×ローカルAI引き継ぎ（AI共通）.md（2026-07-31に分割。**本文は一字も変えていない**）
役割: DeepSeek（OpenClaw）へ実装を委任するときの手順・受け取り・検品・ルール配置。`ai97` と `app97` の境界＝**実装を委任する話は `app97`／設定・費用・モデル選定は `ai97`**。
---

# 🤖 DS運用引き継ぎ（AI共通）

> [!important] 現役の手順
> **9.5.4**（成果物の受け取り）／**9.5.5**（検品4層）／**9.5.7**（AGENTS.mdの置き場所）／**9.5.8**（夜間無人実行）は、いま使っている手順。使うたびに実行結果で確かめ、成功したら「最終確認: 日付」を更新する。
> 9.5〜9.5.3 は経緯の記録。番号は当時のまま（時系列順に並んでいない）。
>
> **★価格は 9.8 が最新（2026-08-15）。9.5 に書いてある単価（V4 Pro 入力$0.43／出力$0.87）は古い。** 費用の話をするときは 9.8 を見る。

## 9.5 安価APIとの比較検討（2026-07-18・検証は明日）

> [!important] ローカルAIを選んだ動機は「値段」であって機密ではない（本人確認 2026-07-18）
> AIが「機密のためにローカル」と書いていたのは推測であり誤り。**動機は費用**。したがって、十分安いフロンティアAPIが出た場合、ローカルという手段は目的（費用を気にせずVaultを読ませる）に対して見直しの対象になる。CLAUDE.mdの原則「本来の目的へ戻る／手段を目的化しない」の適用対象。

- **比較対象**: `OpenClaw`（エージェント型ツール、OpenAI互換API）＋ **DeepSeek V4 Pro**。設定は baseUrl `https://api.deepseek.com/v1`、モデル名 `deepseek-v4-pro` / `deepseek-v4-flash`。
- **価格（2026-07-18確認）**: DeepSeek V4 Pro＝入力 **$0.43/M**・出力 **$0.87/M**。Claude Opus 4.8＝入力 $5.00/M・出力 $25.00/M。**入力で約11.6倍、出力で約28.7倍の差**。入力50万＋出力10万トークンのセッションなら、DeepSeek約$0.30（約45円）／Opus約$5.00（約750円）。従量課金なので使わなければゼロ。
- **DeepSeek V4 Proのスペック**: 2026年4月リリース。SWE-bench Verified 80.6%。MoE（総1.6T／アクティブ約49B）、コンテキスト100万トークン。2026年5月に75%値下げが恒久化。
- **★重要な制約：画像は非対応（2026-07-18確認）**: **公開APIはテキスト専用**。visionはチャット画面（chat.deepseek.com）限定で、APIドキュメントに画像コンテンツタイプもvisionエンドポイントも無い。2026-04-30に「Thinking with Visual Primitives」の技術レポートはあるが、一般提供のAPIではない。→ **レコード査定など「スクリプトから大量の写真を回す」用途には現時点で使えない**。今後API対応する可能性はあるので、組む前に再確認すること。
- **結論としての役割分担（能力と経済性にもとづく／機密が理由ではない）**:
  - **テキスト・コード・推論の下働き** → 安価API（DeepSeek等）。重い開発作業をここへ逃がせば、週間使用量の圧が戦略相談側から減る。
  - **大量の画像処理（レコード査定・出品写真）** → **ローカルのQwen3-VLが依然有利**。1枚ごとの課金が乗らない限界費用ゼロが効く。三層設計（バーコード/OCRで一次仕分け、重いAIは難物だけ）と整合。
  - **Vaultの戦略・記録・ファイル書き換え** → Cowork（Claude）。Vaultマウント・ynp97運用・画面操作を含む環境の問題であり、モデル単体の優劣ではない。※原理的にはOpenClaw側でVaultを読み書きできれば代替の余地はある＝明日の検証項目。
- **エージェント枠組みの比較（2026-07-18調査）**: 候補は `OpenClaw` / `OpenManus` / `Manus`。
  - **OpenClaw**（2026年1月登場、オープンソース・自己ホスト、無料ソフト＋API従量課金）＝**採用候補**。DeepSeek公式APIドキュメントに「Integrate with OpenClaw」、OpenClaw公式にもDeepSeekプロバイダページがあり、**両方向から公式に文書化**されている。DeepSeekプラグインがV4のthinking特有の `reasoning_content` 引き回しを自動補完する＝V4 Proに現に対応している証拠。
  - **OpenManus**（FoundationAgents／MetaGPTコミュニティ、MIT、Python）＝**実質停止と判断**。スター56.4k・フォーク9.8kと数字は大きいが、これは2025年3月のManus熱狂の残り火。**最終リリース v0.3.0＝2025-04-10、最終コミット＝2026-01-04（約6か月半前）**、未処理Issue 343件・PR 200件。2025年7〜11月は動いていたが11月中旬でほぼ停止。DeepSeek V4（2026年4月）より前に止まっており、V4の作法に対応している見込みは低い。※設定 `config/config.toml` に `[llm.vision]` の別枠があり「テキストと視覚で別モデル」は構造的に可能、という点だけは利点。
  - **Manus**（商用・月額サブスク＋クレジット制）＝2026年3月に「My Computer」でローカル対応。コスト構造が違う。
  - **選定理由**: 明日確かめたいのは**モデルの実力**であって枠組みのデバッグではない。未知の変数を増やさない（CLAUDE.md「確認済みの事実と推測を分ける／症状と根本原因を区別する」）。
- **より軽い先行テスト（枠組み導入前でも可）**: 戦略4ファイルをDeepSeekのチャットに貼り、ynp97相当の指示で「忖度せず材料を並べられるか／捏造しないか」を見る。ここで落第なら枠組み選びの問題自体が消える。
- **明日の検証（2026-07-19予定）**: OpenClaw＋DeepSeekで `/ynp97` 相当を実行し、次を既存ベースラインと突き合わせる。①戦略4ファイルを正しく読むか ②応答時間（**ベースライン: Qwen3.6=約3分29秒／Qwen3 30B=約64秒**）③**捏造しないか（メガリザードンXexの再現テストを使う）** ④日本語の機微・忖度しない吟味ができるか。
- **判断への含意**: ローカルQwenには捏造の実績がある（メガリザードンXexで収録セット・カード番号を創作し、指摘後も撤回せず詳細を増やした）。安さのためにその不確かさを引き受ける理由は、安価フロンティアAPIの登場で薄くなる。一方でローカルは限界費用ゼロ・オフライン・外部サービスの都合に左右されない、という固有の利点を保つ。**検証結果次第で `📌 事実と前提` の「ローカルAIは継続して育てる一つの本線」の記述を見直す**。今は変更しない。
- **★検証結果（2026-07-20実施）｜メガリザードンXexテスト＝DeepSeek合格**: OpenClaw＋DeepSeek（API・ウェブ取得あり）にメガリザードンXexの相場を聞き、応答をClaude(Cowork)が検索で裏取りして採点した。
  - **事実関係＝全部正確**: インフェルノX（M2、2025-09-26発売）、RR 013/080、SR 094/080、SAR 110/080、MUR 116/080——すべて実在と一致。Qwen型の創作（2014年Phantom Forces・53/108）は一切なし。※MA 223/193のみ採点側でも未検証。
  - **核心＝取得と記憶の区別ができる**: 「ほとんどのサイトはbot対策でブロックされ取得できなかった」と申告し、取得できたカードラッシュ分だけを実データとして提示。旧XYカードは「記憶があります」「リアルタイム確認できず」と自分から不確かさを宣言。Qwenの失敗（指摘後も撤回せず詳細を増やす）の正反対の行動。
  - **粗が一つ**: 記憶ベース部分でXY4と「ワイルドブレイズ／フラッシュファイヤー」を混同（実際はワイルドブレイズ=XY2・英語版Flashfire、XY4=ファントムゲイン）。ヘッジ付きなので捏造ではなく「不正確だが正直」。
  - **運用ルール**: DeepSeekの**取得データは信頼度高め、記憶ベース発言は要裏取り**。日本語もこの応答を見る限り自然で実用水準。
  - **残検証**: `/ynp97`相当（戦略4ファイル読解）は未実施だが、Vault非接続運用が確定したため戦略相談はDS層の仕事ではなく、**実質不要と判断してよい**（やるなら参考値として）。

### 9.5.1 Codex→OpenClaw委任テスト（2026-07-25・ZatsuTodo）

- **目的**: Codexが監督役となり、OpenClaw＋DeepSeekへ小さなコード修正を委任すると、Codexの週間使用量・総トークンを節約できるか確認した。
- **対象**: `~/openclaw-apps/ZatsuTodo`。既存TODOの目処タグを、解除を挟まず別タグへ直接付け替えられるようにする限定修正。Codexが現在地・過去会話・未完状態を特定し、完成条件を一つの作業票にして新規セッションへ渡した。
- **実行構成**: OpenClaw `main` agent／実際のモデルは `deepseek-v4-pro`（CLIで確認。以前の記録にある既定Flashではなかった）／thinking medium。DSはコード調査と編集まで行い、`CompactRow`へタグ直接切替を追加した。`swift build`へ進む直前に停止し、ユーザー指示でセッションを中断。`/Applications`への配置・再起動は未実施で、作業フォルダの変更だけが残っている。
- **実測**: OpenClawセッション約3分44秒、入力43,216・出力6,507、セッション集計 `totalTokens` 約43,692、推定API費用 **$0.1235**。これとは別にCodex側で台帳検索、ソース確認、過去セッション抽出、作業票作成、監視、停止原因調査のトークンを使用した。
- **判定**: この規模の小修正では、Codexが直接修正するより**総トークンは増えた可能性が高い**。OpenClawの約24,720文字のシステムプロンプト、約20,659文字のツール定義、台帳全文・長いSwiftファイル読込みが固定費になった。Codex週間枠を一部外へ逃がす効果はあるが、総使用量節約にはならない。
- **運用結論**: 小さなバグ修正はCodex直行。OpenClaw＋DS委任は、数時間級の実装、大量の定型変更、独立して検品できる作業に限定する。再試験するなら、台帳全文を読ませず関係するコード断片と完成条件だけを渡す超軽量経路を設計する。`swift build`等の安全な開発コマンドで止まる問題も解決してから行う。

### 9.5.2 15インチMacをDS実装機へ分離（2026-07-29）

- **役割分担**: 現在のMac＝設計・指示・検品。15インチMac＝初期化後のDS実装専用機。
- **受け渡し**: 既存GitHubを使用する。まず既存の個人アカウント配下へ非公開リポジトリ `ds-apps` を作り、設計書と実装コードの往復に必要な最小構成だけを置く。新しい大がかりな管理方式は先に作らない。
- **新規Googleアカウント**: `ynp97apps15@gmail.com` を作成済み。作成中、MacだけWireGuard経由、iPhoneは通常回線という状態では、QR→SMS後にGoogleが `error/3` を返した。MacのWireGuardを切り、同じiPhone・同じ楽天回線・同じ番号で再実行すると成功した。
- **検証済み注意**: PCとスマホをまたぐGoogle確認では、開始前にPC側のVPN／WireGuardを切り、両端末の接続元を通常の同一ネットワークへそろえる。
- **次の順序**: ①`ds-apps`を作成、②15インチMac内の退避要否を確認、③初期化、④必要最小限の開発環境とDSを設定、⑤リポジトリを取得して実装機として動作確認。
- **実態確認（2026-07-30）**: ①〜⑤は既に完了済みだった（本ノート未更新のまま先行していた）。専用アカウント `ynp97apps15` が実装機として稼働中。`ds-apps`リポジトリは非公開で作成済み、`main`に初期構成（README・.gitignore・docs/scripts・作業票テンプレート）がコミット済み。`ds/deepseek-pro`ブランチで最初の成果物（DeepSeek Pro API クライアント＆CLI、TypeScript）が2026-07-30にpush済み。作業票では「未ビルド・動作確認未実施」。
- 最終確認: 2026-07-30（実態確認）

### 9.5.4 15インチMac(DS実装機)からの成果物の受け取り手順（2026-07-30）

現在のMac側（設計・指示・検品担当）が、15インチMacの作業を確認・取得するための一般手順。GitHub操作は本人が不慣れなため、毎回この手順を最初から説明しない。

1. **新着ブランチ確認**: `https://github.com/ynp97/ds-apps/branches/all` を開く。15インチ側がpushすると `ds/アプリ名` ブランチが増える。
2. **作業票を読む**: 該当ブランチの `docs/作業票/*.md` を開き、変更内容・ビルド結果・動作確認・懸念点を確認する。
3. **コード取得**: ブラウザ（ログイン済み状態）で `https://github.com/ynp97/ds-apps/archive/refs/heads/ds/アプリ名.zip` を開くとzipがダウンロードされる（Desktop/Downloadsに保存）。sandboxのcurl/gitは`api.github.com`や`release-assets.githubusercontent.com`がネットワーク許可リストで塞がれているため使えず、認証済みブラウザ経由が現状唯一の経路。
4. **Vaultへ配置**: 展開し、中の `apps/アプリ名/` フォルダだけを `アプリ本体/アプリ名/` へ移動（他アプリと同じ並び）。
5. **ビルド確認**: ターミナルで `cd` し、README記載の手順（多くは `npm install && npm run build`）を実行してエラーがないか確認する。
6. **取り込み判断**: 動作確認後、`ds/アプリ名` を `main` へマージするかは本人判断。急がずPR作成だけに留めてもよい。

- 最終確認: 2026-07-30（deepseek-proで手順を実地確認。screen recorderなど今後のアプリでも同じ手順を使う）

> [!important] 2026-07-31 更新 — **このMacから直接 clone / push できる。zip経由は不要になった**
> `gh` (GitHub CLI) がこのMacに入っており、**`ynp97` でログイン済み**（`repo` `workflow` `gist` `read:org` 権限、keyring保存）。したがって上の手順3（ブラウザでzipを落とす）は**もう必要ない**。
>
> 実際に screen-recorder の修正を、このMacから直接 push できた（2026-07-31、`a905a95..3bcfa99`）。
>
> **初回だけ実行する設定**
> ```
> gh auth setup-git
> git config --global user.name "Yoshiaki Nagumo"
> git config --global user.email "yoshiakinagumo@gmail.com"
> ```
> （`gh auth setup-git` を先に走らせないと push で認証に失敗する。`user.name`/`user.email` は未設定だったので入れた。）
>
> **以後の受け取り・書き戻し**
> ```
> cd ~ && gh repo clone ynp97/ds-apps      # 初回だけ
> cd ~/ds-apps && git checkout ds/アプリ名 && git pull
> ```
>
> **注意（サンドボックスとの区別）**: 塞がれているのは**Claude側のサンドボックス**のネットワークであって、**このMacのターミナルは塞がれていない**。手順3の「認証済みブラウザ経由が唯一の経路」という記述は、Claudeのサンドボックスから見た話をMac全体の制約と取り違えていた。**Claudeができないこと＝Macができないこと、ではない。**
>
> **★踏んではいけない地雷（2026-07-31）**: 上書きする前に、必ず `git diff --stat` と `git log -- <ファイル>` で**向こうに何が入っているか**を確かめる。screen-recorder では、`ds-apps` 側の `main.swift` が移設時の179行の旧版のままで、こちらの375行版と別物だった。確認せずに push していれば、どちらが正か分からなくなっていた（今回は向こうにコミットが移設1件しか無く、安全策も入っていないことを確認してから上書きした）。
>
> - 最終確認: 2026-07-31（実際に clone → 編集 → commit → push まで通した）

### 9.5.5 検品を4層に分けてトークンを使わない相互チェックにする（2026-07-30）

**問題**: 15インチMac(DS)の成果物をChatGPT/Codexで検品すると、一番高い週間枠を検品に使うことになり、DSへ逃がした意味が消える。

**方針**: 相互チェックをAI↔AIに限定せず、コストで4層に分ける。判断力の要らない部分を無料の機械チェックへ寄せる。

- **A層 機械チェック（トークン0・費用0）**: build・型チェック・テスト。実際は「チェック能力不足」ではなく**完成条件が緩い**のが穴だった（作業票に「未ビルド・未実施」と書いたままpushできていた）。作業票に実コマンド出力を貼らせて塞ぐ。
- **B層 GitHub Actions（実質無料）**: pushごとに自動実行。非公開リポジトリはFreeプランで月2,000分。UbuntuランナーはTS/Nodeなら使い切らない。macOSランナーは倍率が高いので、Swift検査が必要になったら15インチMac自身をセルフホストランナーにする（セルフホストは課金対象外）。
- **C層 DS↔DS相互チェック（1円未満）**: 記憶のない別DeepSeekセッションが差分をレビュー。渡すのは差分と完成条件だけで、台帳全文は渡さない（9.5.1の固定費の教訓）。Codex週間枠に触らない。実装したセッションに自己検品させない。
- **D層 本人・Codex**: マージ判断と、A〜Cが赤を出したときだけ。

**足枷にしない設計**: ブランチ保護（必須ステータスチェック）は意図的に設定しない。赤✗は掲示であって門ではない。`[skip ci]` で検査自体を飛ばせる。「急ぎ」指示でDSはC層を省略してよい（A層は省略しない）。急いで自分で直すときはCodex直行で `アプリ本体/` を触り、ds-appsは経路に入らない。

**残る懸念**: DSがDSを検品すると同系列モデルゆえ盲点が相関する。お金・実データ・教会向け成果物は本人のゲートを残す。またチェックを増やすと読む物が増えるため、縛りはトークンよりレビュー時間になる可能性がある（A/B層の利点は出力が合否1ビットであること）。

**成果物**: `ds-apps配置用/`（Vault内）に `AGENTS.md`／`作業票テンプレート.md`／`check.yml`／`配置手順.md` を作成済み。ds-appsへの配置先は配置手順.mdの表のとおり。置けば15インチ側は `git pull` だけで受け取る。

**配置完了（2026-07-30）**: 3ファイルをmainへ直接コミット済み。`AGENTS.md`（root）、`docs/作業票テンプレート.md`（置き換え）、`.github/workflows/check.yml`（新規）。check #1 は緑✓で20秒（mainに`apps/`が無いため「検査対象なし」で正常終了）。

**次にやること**: 既存の `ds/deepseek-pro` ブランチはワークフロー追加前に切られているため、そのままでは自動検査が走らない。mainを取り込むか、mainへマージした時点で検査対象になる。以後mainから切る新ブランチは最初から対象。

**screen-recorder 取り込み完了（2026-07-30）**: PR #1（`ds/screen-recorder` → `main`、15ファイル・304追加・0削除）をチェック2件成功・コンフリクトなしで確認しGitHub上でマージ。ブランチは削除せず残した。コード本体は先に `アプリ本体/screen-recorder/` へ配置済み。これが最初のDS成果物の受け入れ第1号。

**GitHubへファイルを置く手順（検証済み・トークン安い経路）**: Webの**エディタページ（`/new/`）は使わない**。Chrome拡張のスクリプト注入がタイムアウトし、重いCodeMirrorへYAMLを1文字ずつ打つと自動インデントで崩れる。代わりに**`/upload/<branch>/<パス>` ページ＋ファイルアップロード**を使う。存在しないディレクトリでもURLに書けば作られる（`.github/workflows` で確認済み）。Vault内で完成させたファイルをそのままアップロードするので、内容が1バイトも変わらない。

- 最終確認: 2026-07-30（設計・配置・自動検査の初回実行まで確認済み）

### 9.5.6 リポジトリ側のルールは「どのリポジトリを使うか」を統治できない（2026-07-30）

**起きたこと**: 9.5.5のルールを`ds-apps`へ入れた直後、15インチMacは`ds-apps`を使わず、`ynp97apps15/screen-recorder`という**別リポジトリを公開設定で**新規作成してpushした（Swift + ScreenCaptureKitのmacOS画面録画アプリ、コミット1件、README・作業票なし）。結果、AGENTS.md・作業票テンプレート・check.ymlが**どれも適用されなかった**。未ログイン状態で取得できることを確認したので、公開は事実。

**根本原因**: 検品ルールを`ds-apps`の中だけに置いた。DSがそれを読むのは`ds-apps`で作業しているときだけなので、「そもそもどのリポジトリで作業するか」という一段上の判断は統治できない。鶏と卵。

**対策**: 「アプリごとに新規リポジトリを作らない。成果物は必ず`ynp97/ds-apps`の`ds/アプリ名`ブランチへ置く」を、リポジトリ内ではなく**15インチ側の常設指示（OpenClaw/Codexのシステムプロンプトまたはローカルの指示ファイル）**へ置く。理由は①検品ルールが効く、②本アカウント`ynp97`の所有下に残り15インチのアカウントに依存しない、③受け取り手順が一本化される。

**権限の境界**: `screen-recorder`の所有者は`ynp97apps15`なので、`ynp97`でログインしているこのMacからは公開設定を変更できない。可視性変更は15インチ側（所有者アカウント）でしか実行できない。削除はせず、Privateへ変更するに留める。

**渡した指示**: `ds-apps配置用/15インチへの指示_screen-recorder移設.md`（移設手順・A層／C層・作業票・可視性変更・完成条件チェックリスト）。

- 最終確認: 2026-07-30（指示を作成。移設と非公開化は15インチ側で未実施）

### 9.5.7 OpenClawの常設ルールはワークスペース直下のAGENTS.mdへ置く（2026-07-30・公式確認）

**公式仕様（https://docs.openclaw.ai/concepts/system-prompt 、確認日2026-07-30）**:

- OpenClawはワークスペース直下の次のファイルを、新規セッションのシステムプロンプト（Project Context）へ**自動注入**する: `AGENTS.md` / `SOUL.md` / `TOOLS.md` / `IDENTITY.md` / `USER.md` / `HEARTBEAT.md` / `BOOTSTRAP.md`（新規ワークスペースのみ）/ `MEMORY.md`（存在時）。
- **サブエージェントには `AGENTS.md` と `TOOLS.md` だけが注入される**（他は文脈を小さく保つため除外）。C層のレビュー用サブセッションにもAGENTS.mdは届く。
- ワークスペースの場所は `~/.openclaw/openclaw.json` の `agents.defaults.workspace`。
- サイズ上限: 1ファイル20,000文字（`agents.defaults.bootstrapMaxChars`）、全体60,000文字（`bootstrapTotalMaxChars`）。超過分は印つきで切り詰められるので**注入ファイルは短く保つ**。

**結論**: 9.5.6の穴（「どのリポジトリを使うか」を統治できない）は、**15インチのワークスペース直下AGENTS.md**へ書けば塞がる。リポジトリ内ではなくここが正しい置き場所。

**渡し方**: 内容を `ds-apps` の `docs/15インチ常設ルール.md` へコミット済み——と記録したが、**実際にはローカルにも`ds-apps配置用/`にもファイルが存在せず、9.5.5のような配置完了の裏付け（実コミットの確認）も無かった。** 2026-07-30、15インチへ一行指示を出したところ「見つからない」と報告があり、未配置だったことが判明。要精査だった手順を「実際に配置してから伝える」に訂正する。

```
ds-appsをpullして、docs/15インチ常設ルール.md の内容をワークスペース直下のAGENTS.mdへ追記して
```

**この一行方式を今後の標準にする**: 長い指示は毎回ds-appsへ置き、15インチへは「あれを読んで実行して」と一行だけ渡す。端末間で長文を運ばない。メール・コピペ不要。**ただし一行を送る前に、ds-apps側にファイルが実在することを（9.5.5のアップロード手順で）確認してから送る。**

- 最終確認: 2026-07-30（「コミット済み」の記録が誤りと判明。`ds-apps配置用/15インチ常設ルール.md` を新規作成し、配置手順.mdに追加。ds-appsへのアップロードと15インチへの再指示は未実施）

**顛末（2026-07-30）**: 一行指示の再送後、15インチは`ds/screen-recorder`へ正しくpush（別リポジトリを作らなかった＝常設ルールが効いた証拠）。ただし作業票なし・旧リポジトリを「Private」ではなく「アーカイブ」にする、の2件が未達で発覚。`ds-apps/AGENTS.md`へ「作業票ファイル自体が無いpush禁止」「指示された操作を代替手段に勝手に置き換えない」を追記して修正コミット。screen-recorderの残作業だけ個別に再指示したところ、作業票（実ビルド出力あり）とPrivate化（今度は本当に404・公開リポジトリ0件で確認）は完了。ただし作業票内に2件の不正確な自己申告（Actions結果を「未確認」と書いたが実際は緑で2回実行済み／C層省略の理由を「急ぎ指示」としたが急ぎとは言っていない）が残った。実害は小さいためこの回は良しとし、**次回以降のDS作業でAGENTS.md追記が効いているか（指示なしで作業票を作るか、指示を額面通り実行するか）を観察する**方針にした。追加の手直しはしない。

### 9.5.8 DSの夜間無人実行（2026-07-30・初回）

**目的**: 本人が寝ている間にDSへ実装を走らせる。止まって朝まで待機、を起こさせない。

**止まる原因は2つあり、対処が違う**。

1. **`tools.exec.ask=always`（シェルのたびに承認待ち）** ← 停止要因になると想定した項目。
   - **★実測（2026-07-30夜）＝15インチでは承認プロンプトが一度も出ず、そのまま走り続けた。** 対処は何も要らなかった。
   - **原因＝前提の取り違え。** Vaultに記録されていた `tools.exec.ask=always` は**M5側のOpenClaw（2026-07-19構築）の設定**であり、**15インチは2026-07-29の別インストール**なので同じとは限らなかった。ノートの記述を機械の区別なく引き写したのが誤り。**設定値は「どの機械の記録か」を必ず確認する。**
   - 実務上の帰結: **15インチはシェルが承認なしで動く**。空の隔離機なので方針としては許容だが、事実として認識しておく（同一LAN上の他機へは触らせない指示を作業票へ入れてある）。
   - 参考（今後`always`の機械で外す場合）: 設定ファイルを直接書き換えるより、**最初の承認プロンプトで `Allow once` ではなく常時許可側を選ぶ**方が安全。書き換えるなら先に `cp ~/.openclaw/openclaw.json ~/openclaw.json.bak`、`grep -n "ask" ~/.openclaw/openclaw.json` で実際の書かれ方を確認してから。
2. **`AGENTS.md` の「迷ったら作業票へ書いて止める」** ← 常設ルールなので消さない。**作業票側に「この票に限りAGENTS.mdより優先」と書いて上書きする。**
   - 上書きしてよい条件＝**その作業に業務上の判断（本人しか知らない区分）が含まれず、技術的選択しか残っていないこと**。閲覧専用UIのような回はこれに当たる。実データの分類・業務区分が絡む回では上書きしない（学校台帳の教訓）。

**止まらせないために作業票へ書いた規則**: 技術的細部は自分で決めて「自分で決めたこと」へ記録／エラーは別のやり方を最大3回試し、駄目なら最小限の代替実装で次のタスクへ／「本人の確認が必要」で終わらせず、いったん実装してから要確認と書く／全部終わったら追加機能を作らずテストを増やす。

**実データを渡さない工夫**: 15インチには本物のデータが無い。**架空のテストデータをDS自身に作らせ、それだけで開発・確認させる**。本物への接続は本人が後から行う。これで実データがDeepSeekへ渡らない。

**送る前の確認**: ①ds-appsに指示ファイルが実在するか（9.5.7の教訓）②15インチが`ds-apps`を`clone`済みか③**Macがスリープしない設定か**（スリープすると処理も止まる）。

**★画面共有の訂正**: 9.5.3の宿題は「**すべてのユーザ**から必要ユーザーだけへ**戻す**」であって、切ることではなかった。2026-07-30に「閉じておくと安心」という助言で**全部オフ**になり、**M5から15インチを遠隔操作する経路も失われた**。遠隔運用を続けるなら**特定ユーザーのみ許可へ戻す**のが本来の落としどころ。

**GitHubへの配置でつまずいた点（次回説明を省く）**: 「コミット」＝保存・確定。ドラッグしただけでは入らず、緑の `Commit changes` を押して初めて保存される（確認ウィンドウでもう一度押す場合あり）。**アップロード時にファイル名は変更できない**ので、Vault側の名前のまま上げ、一行指示側でそのパスを指す。URLはブラウザのアドレス欄へ入れる（ターミナルに貼ると `no such file or directory`）。

- 最終確認: 2026-07-30（指示ファイルの配置と一行送信まで。完走したかは翌朝 `ds/diary-viewer` ブランチで確認する）

### 9.5.3 Air初期設定で判明した標準経路と再発防止（2026-07-29）

- **本来の目的**: Airを、本人が別端末間でコマンドを転記せず使えるDS実装機にすること。`openclaw`コマンドを直すこと自体は目的ではない。
- **失敗した経路**: 初期化直後のAirで、公式の現行導入経路を先に調べず、既存MacのHomebrew＋Node＋CLI構成を前提に`openclaw gateway status`、パス確認、ローカル配布スクリプトへ進んだ。AirにはHomebrew・Node・npm・OpenClawが入っておらず、`.zprofile`に存在しない`/opt/homebrew/bin/brew`の設定行だけが残っていた。別端末への手入力を繰り返させ、本人が継続不能になった。
- **上位経路（今後の標準）**:
  1. 対象MacにChatGPTデスクトップアプリ（Codex統合済み）を入れ、Air側のCodexが環境設定・診断・検品を直接行う。
  2. OpenClawは公式Macアプリの通常インストールを第一経路とし、初回に`This Mac`を選んでユーザー領域のNode・CLI・Gatewayを自動セットアップする。Homebrew／ターミナルは自動セットアップ失敗時だけの復旧経路。
  3. 現在のMacからAirへはmacOS画面共有を使用し、本人によるコマンド中継を廃止する。
  4. OpenClaw＋DeepSeekは大量実装、Air側Codexは設定・監督・故障対応・検品に分ける。Codexだけでは週間使用量をDSへ逃がす目的は達成しない。
- **確認済みのAir状態**: 画面共有接続先`192.168.68.52`／GitHub新規アカウント`ynp97apps15`でログイン済み／`ynp97/ds-apps`へのpush権限取得済み／OpenClaw 2026.7.1 Macアプリを導入し、初回の`Connect your AI`画面まで到達。AIアカウント／DeepSeek APIキー設定は未完了。
- **セキュリティ持ち越し**: 画面共有を一時的に「すべてのユーザ」にした。安定接続確認後、必要なAirユーザーだけへ戻す。
- **再発防止**: `AGENTS.md`／`CLAUDE.md`／`.agents/skills/ai97/SKILL.md`へ「対処法の前に問題を消す上位経路を探索」「別端末への二度目の転記依頼禁止」「製品導入は現行公式経路を先に確認」を追加。
- 最終確認: 2026-07-29

## 9.6 OpenClaw（DS）を画像対応にする＝Qwen3-VLへセッション内切替（2026-07-21 成立・実画面確認）

OpenClawの既定モデル `deepseek-v4-flash` はテキスト専用で画像を見られない。スクショを見せて相談したいとき（例: FCP操作を教わる）は、**同一セッション内でローカルのQwen3-VLへ切り替える**。DeepSeek（クラウド）は画像時もVRAMを使わないので、テキスト＝DeepSeek／画像＝ローカルVLの二枚看板が両立する。

**前提**: LM Studioに `Qwen3 VL 30B A3B Instruct`（`qwen3_vl_moe`, MLX, 約17GB）がダウンロード済み（確認 2026-07-21）。

**設定（`~/.openclaw/openclaw.json`）**: DeepSeekは触らず、LM Studioプロバイダを追加するだけ。純正バックアップ（`openclaw.json.bak` 等）も自動で残る。
- `models.providers` に `lmstudio` を追加：`baseUrl: http://localhost:1234/v1` ／ `apiKey: "lmstudio"`（ダミー・ローカルは認証なし）／ `api: "openai-completions"` ／ `params.preload: false`（LM StudioのJIT/手動ロードに任せる）／ モデル `id: "qwen3-vl-30b-a3b-instruct-mlx"`、`input: ["text","image"]`、`contextWindow: 32768`。
- `agents.defaults.models` に `"lmstudio/qwen3-vl-30b-a3b-instruct-mlx": { "alias": "Qwen3-VL" }` を追加。`model.primary` はDeepSeekのまま。
- モデルの正確な`id`は `curl http://localhost:1234/api/v1/models` の `"key"` で確認できる（今回は `qwen3-vl-30b-a3b-instruct-mlx`）。書式の正本は [OpenClaw LM Studio doc](https://docs.openclaw.ai/providers/lmstudio)。

**使う手順（毎回）**:
1. LM Studioで `Qwen3 VL 30B A3B Instruct` をLoad。`preload:false`なので**手動ロードが確実**。ロード時の **Context Length は 16384〜32768**（画像1枚は約1〜2千トークン。数枚でも十分）。「Remember settings for …」にチェックしておくと次回自動。
2. **32GBメモリでは最大コンテキスト（262144）は不可**。本体17GB＋巨大KVキャッシュで溢れる。16384（他アプリ併用時）〜32768が安全圏。もっと長くしたいときだけ `KV Cache Quantization` をON。
3. OpenClawで**新しいセッション**を作る（画像は履歴を食うので使い回さない）。左下のモデルを **Qwen3-VL** に切替。**スクショは1枚ずつ**貼って質問。
4. 画像用が済んだらテキストはDeepSeekへ戻す。

**つまずきの正体（2026-07-21）**: 最初に失敗したのは「未ロード」が主因（LM Studioが `Select a model to load` のままだった）。コンテキスト長不足ではなかった。以後は「まずLM Studioで実際にロードされたか（モデル名が表示されるか）」を最初に確認する。

## 9.8 DeepSeek API 値上げ（2026-08-17 適用）とモデル選定の見直し（2026-08-15 調査）

> **番号について**: 9.7 は分割元の [[🤖 Obsidian×ローカルAI引き継ぎ（AI共通）]]（「新モデル・重要更新の検知ルール」）で使用済みなので、次の空き番号 9.8 を使った。**費用の正本はこの節。**

> [!important] **9.5の原則がDS自身に返ってきた**
> 9.5冒頭に「ローカルAIを選んだ動機は値段であって機密ではない。十分安いフロンティアAPIが出た場合、ローカルという手段は目的に対して見直しの対象になる」と本人確認で記録してある。
> **DSを選んだ動機も同じく値段だった。** よって値段の優位が薄れた今、同じ原則がDSに適用される。**ただし今回の調査では「今すぐ乗り換える」という結論には達していない。以下は材料。**

### 事実（DeepSeek公式・2026-08-13発表）

- **適用開始＝2026-08-17 01:00（日本時間）／16:00 UTC 8月16日。** V4-Pro正式版のリリースに合わせた改定。
- **通常時（＝オフピーク）の100万トークン単価**

| モデル | 入力（キャッシュヒット） | 入力（キャッシュミス） | 出力 |
|---|---|---|---|
| DeepSeek-V4-Flash | $0.007 | $0.22 | $0.66 |
| DeepSeek-V4-Pro | $0.022 | $0.66 | $1.98 |

- **ピーク時は通常の2倍。ピーク＝日本時間 10:00〜13:00 と 15:00〜19:00。**
- **2026-08-23 00:00（北京時間）から週末ルールを変更。** 北京時間の土曜・日曜は終日オフピーク単価となり、時間帯による2倍課金はない。日本時間では原則 **土曜01:00〜月曜00:59** が週末オフピークに当たる。初回だけは施行が北京時間の日曜00:00なので、**2026-08-23（日）01:00〜8月24日（月）00:59（日本時間）**。平日のピーク／オフピーク区分は従来どおり。
- 公式が示す従来比（V4-Pro）＝入力 約12.1倍（ヒット）／約3.0倍（ミス）、出力 約4.5倍。
- 出典: [DeepSeek公式 News 2026/08/13](https://api-docs.deepseek.com/news/news260813) ／ [ITmedia 2026-08-13](https://www.itmedia.co.jp/aiplus/article/2608/13/2000000533/)。**公式は価格表を画像で出しているため、数表はITmedia経由で読んだ。**
- 週末ルールの出典: DeepSeek TeamからAPI利用者へ届いた料金改定通知（2026-08-23受領）。公式Pricingページも確認したが、確認時点では検索取得内容に週末ルールの記載を確認できなかった。

> [!warning] ★台帳の価格が二重に古かった（2026-08-15 発見）
> - **9.5（2026-07-18記録）**: V4 Pro 入力$0.43／出力$0.87。
> - **`ds-apps配置用/配置手順.md` C層**: V4 Flash 入力$0.14／出力$0.28。
> - しかし公式の「従来比」から逆算すると、値上げ直前のProは入力$0.22／出力$0.44になり、**9.5の記録と合わない。** 7/18〜8/13の間にさらに値下げがあったのか、記録が別ティアのものだったのかは**未確認**。
> - **教訓**: 従量課金の単価は台帳に書いた時点で腐る。**単価を根拠に設計を書くときは、確認日と出典URLを必ず添える**（9.5・配置手順.mdはどちらも欠けていた）。

### この構成への影響

- **夜間無人実行（9.5.8）はピーク帯を外している。** 日本時間の深夜はオフピークなので、2倍の側は踏まない。**何も変えなくてよい。** 逆に日中に思いつきでDSを回すと2倍側に入る。
- **週末の日中実行は、2026-08-23以降は2倍にならない。** 急ぎでない日中作業を週末へ回せるなら節約になる。ただし既存の夜間無人実行はもともとオフピークなので、定常運用を変える必要はない。平日の日中実行は引き続き2倍に注意する。
- **効くのは出力側。** 入力より出力の倍率が大きい。作業票で「変更点だけ書く／散文の説明を書かない」と縛るのが、そのまま最大の節約になる。
- **C層（9.5.5）が値上げをまともに受ける唯一の層。** 「記憶のない別セッション」が前提なので、原理的にキャッシュヒットしない。入力25k・出力5kと置くと **旧 約0.7円 → 新 約1.3円（ピーク時 約2.6円）**（1ドル150円換算）。**「1円未満」という記録は外れるが、C層をやめる理由にはならない。**
- **9.5.1の実測への当てはめ**: ZatsuTodoセッション（入力43,216・出力6,507・推定$0.1235、`deepseek-v4-pro`）は、公式倍率を当てると**約3〜4倍**。ピーク時ならさらに2倍。

### 他モデルとの比較（2026-08-15 時点）

SWE-bench Pro は llm-stats のベンダー自己申告集計（[Morph まとめ](https://www.morphllm.com/swe-bench-pro)、2026-08-10読み）。

| モデル | 入力/出力（$/M） | SWE-bench Pro |
|---|---|---|
| DeepSeek V4-Flash（新価格） | 0.22 / 0.66 | 52.6% |
| DeepSeek V4-Pro（新価格） | 0.66 / 1.98 | 55.4% |
| **GPT-5.6 Luna** | **0.20 / 1.20** | **62.7%** |
| Gemini 3.7 Flash（2026-08-14公開・年内半額） | 0.75 / 3.75 | （3.6 Flashで58.7%） |
| Claude Haiku 4.5 | 1 / 5 | 39.5%（Scale標準） |
| Claude Sonnet 5（9/1から3/15） | 2 / 10 | 63.2% |
| Claude Opus 5 | 5 / 25 | 未計測 |

- **★いちばんはっきりした変化＝DS V4-Flashの入力$0.22は、GPT-5.6 Lunaの$0.20より高い。** キャッシュヒット（$0.007）以外では、DSはもう最安ではない。一晩の実行（入力300万・出力30万と仮置き）で **DS 約130円 / Luna 約145円**。差は15円で、Pro scoreは10ポイント違う。
- **Gemini 3.7 Flash の半額は2026年内限定**。2027年に倍へ戻る前提の値段なので、常設の足場にはしにくい。
- **Claudeは価格帯が一桁上。** DSの代わりではなく、DSが外したときに入る側。

### 乗り換えコストは思ったより低い（ただし未実行）

- **OpenClawはOpenAI互換API。** 9.6でLM Studioプロバイダを `~/.openclaw/openclaw.json` へ追加した実績がそのまま使える（`models.providers` に baseUrl / apiKey / api を足し、`agents.defaults.models` にエイリアスを置く）。
- **`AGENTS.md`・作業票テンプレート・`check.yml`・`ds-apps` の受け渡しは、どれもモデルに依存しない。** 資産はモデルではなく運用側にある。モデルを替えても全部そのまま生きる。
> [!important] ★Luna＋OpenClawは2026-08-09に検討済み（2026-08-15 本人指摘で発見）
> **結論は「動かない」ではない。** 相談ログ 2026-08-09（AI=Codex・ai97）と [[📌 事実と前提（AI参照用）]] に記録がある。
> - **モデル自体をOpenClawで使うことは可能。**
> - できないのは**無制限Chat枠のまま手足を付けること**。ChatGPT OAuthはCodex側の制限枠へ、APIキーは従量課金になる。「Sign in with ChatGPT」は本人確認であって、モデル利用枠を外部アプリへ渡す仕組みではない。
> - **したがって上の比較表の $0.20/$1.20（API従量課金）という前提は、この記録と矛盾しない。** ただし「無料のLunaでDSを置き換える」という筋は最初から無い。
> - **同じモデル比較を6日前にCodexが済ませていた**（GPQA 92.3% vs 90.1%、SWE-Bench Pro 62.7% vs 55.4%、BrowseComp 83.3% vs 83.4%）。2026-08-15のClaudeは相談ログを検索せずに同じ比較をやり直した（原則2㋒の違反）。
> - **そのとき出ていた読み筋**: 「無制限LunaはあくまでChat、DSはMaximum＋OpenClawの手足がある。**実務はモデル差より実行環境の差が大きい。**」──DSの値打ちはモデルではなく手足と隔離機の側にある、という見方。今回の判断でもここが効く。

- **未検証（残っているもの）**: OpenAI側のプロンプトキャッシュ割引率。実際に `openclaw.json` へOpenAIプロバイダを足して Luna を走らせた実績（接続可能という記録はあるが、実行はしていない）。

> [!danger] 壁は変わらない ── モデルを替えても15インチAirは残す
> 15インチAirの理由は**①OpenClawの暴走隔離 ②DeepSeekのデータ管理**の二つ（[[🧭 実行環境の能力表（AI共通）]] ㋑）。**モデルを替えても①は消えない。** 効率を理由にAirの廃止を候補に挙げない（2026-08-01に一度やった失敗）。
> また Luna も外部API（OpenAI）なので、**実データ（出席・住所録・日記）を渡さない運用は同じまま。**

### ベンチマークの読み方（注意）

- **SWE-bench Verified は 2026年2月に OpenAI が汚染を理由に使用をやめている。** 9.5に記録した「V4 Pro＝SWE-bench Verified 80.6%」は、もう順位としては読めない数字。
- SWE-bench Pro も、Datacurve の監査で**採点の約3分の1が誤り**（誤って合格8.5%・誤って不合格24%）、Claude Opus 4.6/4.7 が `.git` 履歴から正解を読む「CHEATED」判定を12%以上受けた、と報告されている（Scale未確認）。
- **数ポイント差は誤差。10ポイント差なら意味がある、くらいの粗さで読む。**

### 判断（2026-08-15 時点）

**8/17までに何かする必要はない。** 値上げ後1か月の実費を見てから決められる。先に決めておくとよいのは乗り換えの引き金のほうで、「月◯円を超えたら」「夜間実行の朝やり直しが月◯回を超えたら」を数字で置いておくと、そのときに悩まずに済む。**引き金の数字は未設定。**

- 最終確認: 2026-08-23（週末終日オフピーク化の利用者向け通知を確認。公式Pricingページも確認したが、週末ルールの掲載は検索取得内容では未確認。**実際の請求額での検証は未実施**）
